{
  "name": "redact-secret-reference-logging",
  "private": true,
  "type": "module",
  "description": "Reference architecture: application logging with pino. Installs only released registry packages, pinned exactly by package-lock.json.",
  "scripts": {
    "smoke": "node smoke.mjs"
  },
  "dependencies": {
    "@redact-secret/adapter-pino": "0.1.1",
    "@redact-secret/core": "0.1.0-beta.8",
    "pino": "10.3.1"
  }
}
