# The tests inject their core, and the golden-path qualification installs the
# candidate core itself, so the adapters' @redact-secret/core peer is never
# auto-installed here: package-lock.json holds only the adapters.
legacy-peer-deps=true
