{
  "findings": {
    "2b9ef0a7f775c6ea": {
      "classification": "false_positive",
      "line": 101,
      "path": "conformance/benchmark-regressions.ts",
      "rationale": "ID is a linear-time anchored identifier grammar (`^[a-z][a-z0-9]*(?:-[a-z0-9]+)*$`): the hyphen separator is disjoint from the adjacent `[a-z0-9]` class, so there is no ambiguous split point for the engine to backtrack over. The generic ReDoS heuristic reports the grouped repetition without accounting for that disjointness.",
      "rule_id": "sast.rules.javascript.lang.security.audit.detect-redos"
    },
    "40d73307ddfd68b7": {
      "classification": "false_positive",
      "line": 111,
      "path": "conformance/benchmark-regressions.ts",
      "rationale": "ID is a linear-time anchored identifier grammar (`^[a-z][a-z0-9]*(?:-[a-z0-9]+)*$`): the hyphen separator is disjoint from the adjacent `[a-z0-9]` class, so there is no ambiguous split point for the engine to backtrack over. The generic ReDoS heuristic reports the grouped repetition without accounting for that disjointness.",
      "rule_id": "sast.rules.javascript.lang.security.audit.detect-redos"
    },
    "e39f6215b256d436": {
      "classification": "false_positive",
      "line": 276,
      "path": "assessment/schema.ts",
      "rationale": "CASE_ID_PATTERN is a linear-time anchored identifier grammar whose repeated character classes and hyphen separators are disjoint, and this call additionally caps the candidate at 64 characters before matching. The generic ReDoS heuristic reports the grouped repetition without accounting for those constraints.",
      "rule_id": "sast.rules.javascript.lang.security.audit.detect-redos"
    },
    "6d155b3cbd8bd6ce": {
      "classification": "false_positive",
      "line": 310,
      "path": "assessment/schema.ts",
      "rationale": "IDENTIFIER_PATTERN is a linear-time anchored grammar: each separator is outside the adjacent alphanumeric class, so the engine has no ambiguous partition to backtrack over. The value is reviewed assessment-fixture metadata rather than attacker-controlled request input.",
      "rule_id": "sast.rules.javascript.lang.security.audit.detect-redos"
    },
    "ef16500f06b8e4e4": {
      "classification": "false_positive",
      "line": 311,
      "path": "assessment/schema.ts",
      "rationale": "IDENTIFIER_PATTERN is a linear-time anchored grammar: each separator is outside the adjacent alphanumeric class, so the engine has no ambiguous partition to backtrack over. The value is reviewed assessment-fixture metadata rather than attacker-controlled request input.",
      "rule_id": "sast.rules.javascript.lang.security.audit.detect-redos"
    },
    "d421b316637ae022": {
      "classification": "false_positive",
      "line": 351,
      "path": "assessment/schema.ts",
      "rationale": "CASE_ID_PATTERN is a linear-time anchored identifier grammar whose repeated character classes and hyphen separators are disjoint, and this call additionally caps the candidate at 64 characters before matching. The generic ReDoS heuristic reports the grouped repetition without accounting for those constraints.",
      "rule_id": "sast.rules.javascript.lang.security.audit.detect-redos"
    },
    "f45192e1ccbd9a5b": {
      "classification": "false_positive",
      "line": 367,
      "path": "assessment/schema.ts",
      "rationale": "IDENTIFIER_PATTERN is a linear-time anchored grammar: each separator is outside the adjacent alphanumeric class, so the engine has no ambiguous partition to backtrack over. The value is a repository-owned workload-profile algorithm identifier, not attacker-controlled request input.",
      "rule_id": "sast.rules.javascript.lang.security.audit.detect-redos"
    },
    "41781bc1b1a9a126": {
      "classification": "false_positive",
      "line": 413,
      "path": "assessment/schema.ts",
      "rationale": "CASE_ID_PATTERN is a linear-time anchored identifier grammar because its alphanumeric runs and hyphen separators are disjoint. This validator processes assessment result identifiers produced by repository-owned runners, and the generic ReDoS heuristic cannot infer the grammar's non-overlapping transitions.",
      "rule_id": "sast.rules.javascript.lang.security.audit.detect-redos"
    },
    "ac9ec490099dcbe6": {
      "classification": "false_positive",
      "line": 717,
      "path": "crates/secret-scan-core/examples/assessment_adapter.rs",
      "rationale": "The process arguments are collected only to serialize the assessment invocation as JSON provenance. They are never interpreted as a command, passed to a shell, or used to launch another process, so the command-argument injection class reported by this generic rule is absent.",
      "rule_id": "sast.rules.rust.lang.security.args"
    },
    "bd6f826f125bd4aa": {
      "classification": "false_positive",
      "line": 353,
      "path": "scripts/check-python-package.py",
      "rationale": "The URL is built from a fixed, hardcoded registry host (pypi.org, crates.io, or registry.npmjs.org) with only a package/crate name path-appended; the host is never attacker-influenced, so this cannot be redirected to an untrusted origin.",
      "rule_id": "sast.rules.python.lang.security.audit.dynamic-urllib-use-detected"
    },
    "08675bcb8e6608d2": {
      "classification": "false_positive",
      "line": 430,
      "path": "conformance/schema.ts",
      "rationale": "The matched pattern is one of this file's three module-level constant regexes (CASE_ID_PATTERN, IDENTIFIER_PATTERN, ERROR_CODE_PATTERN), each a linear-time character-class repetition with no nested or overlapping quantifiers -- the rule's generic backtracking heuristic cannot see that '-' / '.' / '_' separators are disjoint from the surrounding [a-z0-9] class, so there is no ambiguous split point for the engine to backtrack over. Inputs are also schema-validated fixture/case identifiers, not attacker-controlled request data.",
      "rule_id": "sast.rules.javascript.lang.security.audit.detect-redos"
    },
    "55e7b6ff6cd88ba2": {
      "classification": "false_positive",
      "line": 319,
      "path": "scripts/browser-harness.mjs",
      "rationale": "The template string builds a `<SECRET_N>` / `<TYPE_N>` redaction placeholder label for test-fixture comparison or CLI output text; it is never assigned to `innerHTML`, rendered into a DOM, or otherwise interpreted as markup, so the XSS this rule guards against cannot occur here.",
      "rule_id": "sast.rules.javascript.lang.security.html-in-template-string"
    },
    "c340bf1dcd2c1d76": {
      "classification": "false_positive",
      "line": 534,
      "path": "scripts/check-rust-workspace.py",
      "rationale": "The URL is built from a fixed, hardcoded registry host (pypi.org, crates.io, or registry.npmjs.org) with only a package/crate name path-appended; the host is never attacker-influenced, so this cannot be redirected to an untrusted origin.",
      "rule_id": "sast.rules.python.lang.security.audit.dynamic-urllib-use-detected"
    },
    "1e1300c67c36372d": {
      "classification": "false_positive",
      "line": 22,
      "path": "packages/javascript/src/formatters.ts",
      "rationale": "The template string builds a `<SECRET_N>` / `<TYPE_N>` redaction placeholder label for test-fixture comparison or CLI output text; it is never assigned to `innerHTML`, rendered into a DOM, or otherwise interpreted as markup, so the XSS this rule guards against cannot occur here.",
      "rule_id": "sast.rules.javascript.lang.security.html-in-template-string"
    },
    "22f2bb62f9acadd4": {
      "classification": "false_positive",
      "line": 285,
      "path": "conformance/schema.ts",
      "rationale": "The matched pattern is one of this file's three module-level constant regexes (CASE_ID_PATTERN, IDENTIFIER_PATTERN, ERROR_CODE_PATTERN), each a linear-time character-class repetition with no nested or overlapping quantifiers -- the rule's generic backtracking heuristic cannot see that '-' / '.' / '_' separators are disjoint from the surrounding [a-z0-9] class, so there is no ambiguous split point for the engine to backtrack over. Inputs are also schema-validated fixture/case identifiers, not attacker-controlled request data.",
      "rule_id": "sast.rules.javascript.lang.security.audit.detect-redos"
    },
    "24e8db64636972d7": {
      "classification": "false_positive",
      "line": 354,
      "path": "conformance/schema.ts",
      "rationale": "The matched pattern is one of this file's three module-level constant regexes (CASE_ID_PATTERN, IDENTIFIER_PATTERN, ERROR_CODE_PATTERN), each a linear-time character-class repetition with no nested or overlapping quantifiers -- the rule's generic backtracking heuristic cannot see that '-' / '.' / '_' separators are disjoint from the surrounding [a-z0-9] class, so there is no ambiguous split point for the engine to backtrack over. Inputs are also schema-validated fixture/case identifiers, not attacker-controlled request data.",
      "rule_id": "sast.rules.javascript.lang.security.audit.detect-redos"
    },
    "b7303fc73d176e6c": {
      "classification": "false_positive",
      "line": 312,
      "path": "scripts/run-sast.py",
      "rationale": "Every argument list passed to this subprocess call is built entirely from this script's own internal logic (interpreter paths, pinned tool names, static flags) as a list -- never `shell=True` and never externally- or network-controlled data -- so there is no command-injection surface for this generic audit rule to flag.",
      "rule_id": "sast.rules.python.lang.security.audit.dangerous-subprocess-use-audit"
    },
    "31fc2ae115215156": {
      "classification": "false_positive",
      "line": 586,
      "path": "conformance/schema.ts",
      "rationale": "The matched pattern is one of this file's three module-level constant regexes (CASE_ID_PATTERN, IDENTIFIER_PATTERN, ERROR_CODE_PATTERN), each a linear-time character-class repetition with no nested or overlapping quantifiers -- the rule's generic backtracking heuristic cannot see that '-' / '.' / '_' separators are disjoint from the surrounding [a-z0-9] class, so there is no ambiguous split point for the engine to backtrack over. Inputs are also schema-validated fixture/case identifiers, not attacker-controlled request data.",
      "rule_id": "sast.rules.javascript.lang.security.audit.detect-redos"
    },
    "26cc88d0b2f418ad": {
      "classification": "false_positive",
      "line": 290,
      "path": "scripts/qualify-cli-binary.mjs",
      "rationale": "The template string builds a `<SECRET_N>` / `<TYPE_N>` redaction placeholder label for test-fixture comparison or CLI output text; it is never assigned to `innerHTML`, rendered into a DOM, or otherwise interpreted as markup, so the XSS this rule guards against cannot occur here.",
      "rule_id": "sast.rules.javascript.lang.security.html-in-template-string"
    },
    "49ee517623249dd4": {
      "classification": "false_positive",
      "line": 553,
      "path": "conformance/schema.ts",
      "rationale": "The matched pattern is one of this file's three module-level constant regexes (CASE_ID_PATTERN, IDENTIFIER_PATTERN, ERROR_CODE_PATTERN), each a linear-time character-class repetition with no nested or overlapping quantifiers -- the rule's generic backtracking heuristic cannot see that '-' / '.' / '_' separators are disjoint from the surrounding [a-z0-9] class, so there is no ambiguous split point for the engine to backtrack over. Inputs are also schema-validated fixture/case identifiers, not attacker-controlled request data.",
      "rule_id": "sast.rules.javascript.lang.security.audit.detect-redos"
    },
    "df47445dce8a325c": {
      "classification": "false_positive",
      "line": 387,
      "path": "scripts/run-sast.py",
      "rationale": "Every argument list passed to this subprocess call is built entirely from this script's own internal logic (interpreter paths, pinned tool names, static flags) as a list -- never `shell=True` and never externally- or network-controlled data -- so there is no command-injection surface for this generic audit rule to flag.",
      "rule_id": "sast.rules.python.lang.security.audit.dangerous-subprocess-use-audit"
    },
    "638af74061133716": {
      "classification": "false_positive",
      "line": 303,
      "path": "conformance/schema.ts",
      "rationale": "The matched pattern is one of this file's three module-level constant regexes (CASE_ID_PATTERN, IDENTIFIER_PATTERN, ERROR_CODE_PATTERN), each a linear-time character-class repetition with no nested or overlapping quantifiers -- the rule's generic backtracking heuristic cannot see that '-' / '.' / '_' separators are disjoint from the surrounding [a-z0-9] class, so there is no ambiguous split point for the engine to backtrack over. Inputs are also schema-validated fixture/case identifiers, not attacker-controlled request data.",
      "rule_id": "sast.rules.javascript.lang.security.audit.detect-redos"
    },
    "7a42fd3bd154ca27": {
      "classification": "false_positive",
      "line": 878,
      "path": "conformance/schema.ts",
      "rationale": "The matched pattern is one of this file's three module-level constant regexes (CASE_ID_PATTERN, IDENTIFIER_PATTERN, ERROR_CODE_PATTERN), each a linear-time character-class repetition with no nested or overlapping quantifiers -- the rule's generic backtracking heuristic cannot see that '-' / '.' / '_' separators are disjoint from the surrounding [a-z0-9] class, so there is no ambiguous split point for the engine to backtrack over. Inputs are also schema-validated fixture/case identifiers, not attacker-controlled request data.",
      "rule_id": "sast.rules.javascript.lang.security.audit.detect-redos"
    },
    "856c95923ce13904": {
      "classification": "false_positive",
      "line": 305,
      "path": "conformance/schema.ts",
      "rationale": "The matched pattern is one of this file's three module-level constant regexes (CASE_ID_PATTERN, IDENTIFIER_PATTERN, ERROR_CODE_PATTERN), each a linear-time character-class repetition with no nested or overlapping quantifiers -- the rule's generic backtracking heuristic cannot see that '-' / '.' / '_' separators are disjoint from the surrounding [a-z0-9] class, so there is no ambiguous split point for the engine to backtrack over. Inputs are also schema-validated fixture/case identifiers, not attacker-controlled request data.",
      "rule_id": "sast.rules.javascript.lang.security.audit.detect-redos"
    },
    "8a43d95927506619": {
      "classification": "false_positive",
      "line": 877,
      "path": "conformance/schema.ts",
      "rationale": "The matched pattern is one of this file's three module-level constant regexes (CASE_ID_PATTERN, IDENTIFIER_PATTERN, ERROR_CODE_PATTERN), each a linear-time character-class repetition with no nested or overlapping quantifiers -- the rule's generic backtracking heuristic cannot see that '-' / '.' / '_' separators are disjoint from the surrounding [a-z0-9] class, so there is no ambiguous split point for the engine to backtrack over. Inputs are also schema-validated fixture/case identifiers, not attacker-controlled request data.",
      "rule_id": "sast.rules.javascript.lang.security.audit.detect-redos"
    },
    "5790c1e4d7eb2c4e": {
      "classification": "false_positive",
      "line": 187,
      "path": "scripts/browser-package-harness.mjs",
      "rationale": "The template string builds a `<SECRET_N>` / `<TYPE_N>` redaction placeholder label for test-fixture comparison or CLI output text; it is never assigned to `innerHTML`, rendered into a DOM, or otherwise interpreted as markup, so the XSS this rule guards against cannot occur here.",
      "rule_id": "sast.rules.javascript.lang.security.html-in-template-string"
    },
    "aec2b8ae9020368d": {
      "classification": "false_positive",
      "line": 394,
      "path": "conformance/schema.ts",
      "rationale": "The matched pattern is one of this file's three module-level constant regexes (CASE_ID_PATTERN, IDENTIFIER_PATTERN, ERROR_CODE_PATTERN), each a linear-time character-class repetition with no nested or overlapping quantifiers -- the rule's generic backtracking heuristic cannot see that '-' / '.' / '_' separators are disjoint from the surrounding [a-z0-9] class, so there is no ambiguous split point for the engine to backtrack over. Inputs are also schema-validated fixture/case identifiers, not attacker-controlled request data.",
      "rule_id": "sast.rules.javascript.lang.security.audit.detect-redos"
    },
    "b0d617d9f421f686": {
      "classification": "false_positive",
      "line": 113,
      "path": "scripts/verify-release-governance.py",
      "rationale": "The URL is built from a fixed, hardcoded registry host (pypi.org, crates.io, or registry.npmjs.org) with only a package/crate name path-appended; the host is never attacker-influenced, so this cannot be redirected to an untrusted origin.",
      "rule_id": "sast.rules.python.lang.security.audit.dynamic-urllib-use-detected"
    },
    "b48cb559dc871d68": {
      "classification": "false_positive",
      "line": 353,
      "path": "conformance/schema.ts",
      "rationale": "The matched pattern is one of this file's three module-level constant regexes (CASE_ID_PATTERN, IDENTIFIER_PATTERN, ERROR_CODE_PATTERN), each a linear-time character-class repetition with no nested or overlapping quantifiers -- the rule's generic backtracking heuristic cannot see that '-' / '.' / '_' separators are disjoint from the surrounding [a-z0-9] class, so there is no ambiguous split point for the engine to backtrack over. Inputs are also schema-validated fixture/case identifiers, not attacker-controlled request data.",
      "rule_id": "sast.rules.javascript.lang.security.audit.detect-redos"
    },
    "b544574e5c73c44d": {
      "classification": "false_positive",
      "line": 85,
      "path": "scripts/install-opengrep.py",
      "rationale": "The URL is built from a fixed, hardcoded registry host (pypi.org, crates.io, or registry.npmjs.org) with only a package/crate name path-appended; the host is never attacker-influenced, so this cannot be redirected to an untrusted origin.",
      "rule_id": "sast.rules.python.lang.security.audit.dynamic-urllib-use-detected"
    },
    "b9a4504f44a95b0b": {
      "classification": "false_positive",
      "line": 304,
      "path": "conformance/schema.ts",
      "rationale": "The matched pattern is one of this file's three module-level constant regexes (CASE_ID_PATTERN, IDENTIFIER_PATTERN, ERROR_CODE_PATTERN), each a linear-time character-class repetition with no nested or overlapping quantifiers -- the rule's generic backtracking heuristic cannot see that '-' / '.' / '_' separators are disjoint from the surrounding [a-z0-9] class, so there is no ambiguous split point for the engine to backtrack over. Inputs are also schema-validated fixture/case identifiers, not attacker-controlled request data.",
      "rule_id": "sast.rules.javascript.lang.security.audit.detect-redos"
    },
    "ba28dfbc29b2211c": {
      "classification": "false_positive",
      "line": 525,
      "path": "conformance/schema.ts",
      "rationale": "The matched pattern is one of this file's three module-level constant regexes (CASE_ID_PATTERN, IDENTIFIER_PATTERN, ERROR_CODE_PATTERN), each a linear-time character-class repetition with no nested or overlapping quantifiers -- the rule's generic backtracking heuristic cannot see that '-' / '.' / '_' separators are disjoint from the surrounding [a-z0-9] class, so there is no ambiguous split point for the engine to backtrack over. Inputs are also schema-validated fixture/case identifiers, not attacker-controlled request data.",
      "rule_id": "sast.rules.javascript.lang.security.audit.detect-redos"
    },
    "c033a11f58a39bcb": {
      "classification": "false_positive",
      "line": 277,
      "path": "conformance/schema.ts",
      "rationale": "The matched pattern is one of this file's three module-level constant regexes (CASE_ID_PATTERN, IDENTIFIER_PATTERN, ERROR_CODE_PATTERN), each a linear-time character-class repetition with no nested or overlapping quantifiers -- the rule's generic backtracking heuristic cannot see that '-' / '.' / '_' separators are disjoint from the surrounding [a-z0-9] class, so there is no ambiguous split point for the engine to backtrack over. Inputs are also schema-validated fixture/case identifiers, not attacker-controlled request data.",
      "rule_id": "sast.rules.javascript.lang.security.audit.detect-redos"
    },
    "c91a0ea10933026c": {
      "classification": "false_positive",
      "line": 90,
      "path": "scripts/install-opengrep.py",
      "rationale": "Every argument list passed to this subprocess call is built entirely from this script's own internal logic (interpreter paths, pinned tool names, static flags) as a list -- never `shell=True` and never externally- or network-controlled data -- so there is no command-injection surface for this generic audit rule to flag.",
      "rule_id": "sast.rules.python.lang.security.audit.dangerous-subprocess-use-audit"
    },
    "e7acd9e8d80c2913": {
      "classification": "false_positive",
      "line": 330,
      "path": "scripts/qualify-python-wheel.py",
      "rationale": "Every argument list passed to this subprocess call is built entirely from this script's own internal logic (interpreter paths, pinned tool names, static flags) as a list -- never `shell=True` and never externally- or network-controlled data -- so there is no command-injection surface for this generic audit rule to flag.",
      "rule_id": "sast.rules.python.lang.security.audit.dangerous-subprocess-use-audit"
    },
    "d6f9dcd628672c9b": {
      "classification": "false_positive",
      "line": 50,
      "path": "docs/audits/evidence/beta2-final-review/reproduce.py",
      "rationale": "This audit probe executes a repository-owned release-reporting shell snippet with a constant offline npm stub and a fixed `bash -c` argv list. No untrusted input reaches the command string, the probe publishes nothing, and it is stored under audit evidence rather than product runtime code, so the generic subprocess audit rule's command-injection concern does not apply.",
      "rule_id": "sast.rules.python.lang.security.audit.dangerous-subprocess-use-audit"
    },
    "7bb18f634eda7dd7": {
      "classification": "false_positive",
      "line": 149,
      "path": "scripts/qualify-node-addon.mjs",
      "rationale": "The template string builds a `<SECRET_N>` / `<TYPE_N>` redaction placeholder label for test-fixture comparison or CLI output text; it is never assigned to `innerHTML`, rendered into a DOM, or otherwise interpreted as markup, so the XSS this rule guards against cannot occur here.",
      "rule_id": "sast.rules.javascript.lang.security.html-in-template-string"
    },
    "f8dbf3e31005bcd2": {
      "classification": "false_positive",
      "line": 393,
      "path": "conformance/schema.ts",
      "rationale": "The matched pattern is one of this file's three module-level constant regexes (CASE_ID_PATTERN, IDENTIFIER_PATTERN, ERROR_CODE_PATTERN), each a linear-time character-class repetition with no nested or overlapping quantifiers -- the rule's generic backtracking heuristic cannot see that '-' / '.' / '_' separators are disjoint from the surrounding [a-z0-9] class, so there is no ambiguous split point for the engine to backtrack over. Inputs are also schema-validated fixture/case identifiers, not attacker-controlled request data.",
      "rule_id": "sast.rules.javascript.lang.security.audit.detect-redos"
    },
    "fc19e3365b5260b7": {
      "classification": "false_positive",
      "line": 70,
      "path": "crates/secret-scan-cli/src/main.rs",
      "rationale": "The flagged call is `env::args_os().skip(1)`: argv[0] is explicitly discarded before the remaining arguments are used as scan-target paths, so the exact property this audit rule warns about (relying on argv[0] for a security decision) does not apply.",
      "rule_id": "sast.rules.rust.lang.security.args-os"
    },
    "f1955d528b4083fa": {
      "classification": "false_positive",
      "line": 113,
      "path": "crates/secret-scan-core/examples/assessment_adapter.rs",
      "rationale": "The assessment adapter reads `env::args().skip(1)`, explicitly discarding argv[0] before parsing its own fixed command options. The arguments select local assessment modes and output paths only; they are not used for authentication, authorization, or shell execution, so the argv[0] spoofing concern this generic rule targets does not apply.",
      "rule_id": "sast.rules.rust.lang.security.args"
    },
    "29ebc0560937d8b7": {
      "classification": "false_positive",
      "line": 712,
      "path": "crates/secret-scan-core/examples/assessment_adapter.rs",
      "rationale": "This call also uses `env::args().skip(1)` only to reconstruct the adapter invocation in assessment provenance. The resulting string is written to JSON/Markdown reports as inert reproducibility metadata and is never executed, parsed as a shell command, or used for a security decision.",
      "rule_id": "sast.rules.rust.lang.security.args"
    },
    "6482d71ba63ff531": {
      "classification": "false_positive",
      "path": "scripts/verify-recovery-artifact.py",
      "rule_id": "sast.rules.python.lang.security.audit.dynamic-urllib-use-detected",
      "rationale": "Recovery tooling reads only the fixed https://pypi.org registry authority; the project/version path is derived from the original approved source after its run and inventory identity checks. It sends no credentials, has a bounded timeout, and verifies exact filenames and SHA-256 hashes before accepting an existing publication. This is intentional release-time registry verification, not a runtime core network operation or an attacker-selected URL authority.",
      "line": 47
    },
    "e5330565a5235757": {
      "classification": "false_positive",
      "line": 124,
      "path": "scripts/verify-recovery-artifact.py",
      "rationale": "Recovery tooling reads only the fixed https://pypi.org registry authority; the project/version path is derived from the original approved source after its run and inventory identity checks. It sends no credentials, has a bounded timeout, and verifies exact filenames and SHA-256 hashes before accepting an existing publication. This is intentional release-time registry verification, not a runtime core network operation or an attacker-selected URL authority.",
      "rule_id": "sast.rules.python.lang.security.audit.dynamic-urllib-use-detected"
    },
    "a6ead658861ef47d": {
      "classification": "false_positive",
      "line": 274,
      "path": "assessment/schema.ts",
      "rationale": "The matched pattern is one of this file's two module-level constant regexes (CASE_ID_PATTERN, IDENTIFIER_PATTERN), each a linear-time character-class repetition with no nested or overlapping quantifiers -- the rule's generic backtracking heuristic cannot see that '-' / '.' / '_' separators are disjoint from the surrounding [a-z0-9] class, so there is no ambiguous split point for the engine to backtrack over. Inputs are also schema-validated fixture/profile/result identifiers, not attacker-controlled request data.",
      "rule_id": "sast.rules.javascript.lang.security.audit.detect-redos"
    },
    "4202a5067b1e6dbc": {
      "classification": "false_positive",
      "line": 308,
      "path": "assessment/schema.ts",
      "rationale": "The matched pattern is one of this file's two module-level constant regexes (CASE_ID_PATTERN, IDENTIFIER_PATTERN), each a linear-time character-class repetition with no nested or overlapping quantifiers -- the rule's generic backtracking heuristic cannot see that '-' / '.' / '_' separators are disjoint from the surrounding [a-z0-9] class, so there is no ambiguous split point for the engine to backtrack over. Inputs are also schema-validated fixture/profile/result identifiers, not attacker-controlled request data.",
      "rule_id": "sast.rules.javascript.lang.security.audit.detect-redos"
    },
    "bd5a5d2d6e39d697": {
      "classification": "false_positive",
      "line": 309,
      "path": "assessment/schema.ts",
      "rationale": "The matched pattern is one of this file's two module-level constant regexes (CASE_ID_PATTERN, IDENTIFIER_PATTERN), each a linear-time character-class repetition with no nested or overlapping quantifiers -- the rule's generic backtracking heuristic cannot see that '-' / '.' / '_' separators are disjoint from the surrounding [a-z0-9] class, so there is no ambiguous split point for the engine to backtrack over. Inputs are also schema-validated fixture/profile/result identifiers, not attacker-controlled request data.",
      "rule_id": "sast.rules.javascript.lang.security.audit.detect-redos"
    },
    "6803ede8aa0a2a1a": {
      "classification": "false_positive",
      "line": 349,
      "path": "assessment/schema.ts",
      "rationale": "The matched pattern is one of this file's two module-level constant regexes (CASE_ID_PATTERN, IDENTIFIER_PATTERN), each a linear-time character-class repetition with no nested or overlapping quantifiers -- the rule's generic backtracking heuristic cannot see that '-' / '.' / '_' separators are disjoint from the surrounding [a-z0-9] class, so there is no ambiguous split point for the engine to backtrack over. Inputs are also schema-validated fixture/profile/result identifiers, not attacker-controlled request data.",
      "rule_id": "sast.rules.javascript.lang.security.audit.detect-redos"
    },
    "923cc290babf7e68": {
      "classification": "false_positive",
      "line": 365,
      "path": "assessment/schema.ts",
      "rationale": "The matched pattern is one of this file's two module-level constant regexes (CASE_ID_PATTERN, IDENTIFIER_PATTERN), each a linear-time character-class repetition with no nested or overlapping quantifiers -- the rule's generic backtracking heuristic cannot see that '-' / '.' / '_' separators are disjoint from the surrounding [a-z0-9] class, so there is no ambiguous split point for the engine to backtrack over. Inputs are also schema-validated fixture/profile/result identifiers, not attacker-controlled request data.",
      "rule_id": "sast.rules.javascript.lang.security.audit.detect-redos"
    },
    "179616cea1155f98": {
      "classification": "false_positive",
      "line": 411,
      "path": "assessment/schema.ts",
      "rationale": "The matched pattern is one of this file's two module-level constant regexes (CASE_ID_PATTERN, IDENTIFIER_PATTERN), each a linear-time character-class repetition with no nested or overlapping quantifiers -- the rule's generic backtracking heuristic cannot see that '-' / '.' / '_' separators are disjoint from the surrounding [a-z0-9] class, so there is no ambiguous split point for the engine to backtrack over. Inputs are also schema-validated fixture/profile/result identifiers, not attacker-controlled request data.",
      "rule_id": "sast.rules.javascript.lang.security.audit.detect-redos"
    },
    "963460bbc975ca1b": {
      "classification": "false_positive",
      "line": 319,
      "path": "scripts/run-sast.py",
      "rationale": "Every argument list passed to this subprocess call is built as a list -- never shell=True. The executable is the pinned, digest-verified OpenGrep binary or git, and the dynamic values are repository paths, static flags, reviewed EXCLUSIONS entries, or caller-provided scan targets interpreted only by OpenGrep as paths to scan; they are not shell-interpreted command text, so this generic audit rule's command-injection concern does not apply.",
      "rule_id": "sast.rules.python.lang.security.audit.dangerous-subprocess-use-audit"
    },
    "80ba78d9aefc379d": {
      "classification": "false_positive",
      "line": 394,
      "path": "scripts/run-sast.py",
      "rationale": "Every argument list passed to this subprocess call is built as a list -- never shell=True. The executable is the pinned, digest-verified OpenGrep binary or git, and the dynamic values are repository paths, static flags, reviewed EXCLUSIONS entries, or caller-provided scan targets interpreted only by OpenGrep as paths to scan; they are not shell-interpreted command text, so this generic audit rule's command-injection concern does not apply.",
      "rule_id": "sast.rules.python.lang.security.audit.dangerous-subprocess-use-audit"
    },
    "d7701438119a990d": {
      "classification": "false_positive",
      "line": 92,
      "path": "examples/logging-redaction/python/test_logging_filter.py",
      "rationale": "The rule's `.*(api.key|secret|credential|token|password).*%s.*` heuristic matches this format string on the literal word \"token\", but the value logged is `SECRET_TOKEN_1`, this repository's synthetic, non-functional secret-shaped placeholder used identically by every example's fake-scanner fixtures (e.g. `examples/tracing-masking/python/fake_scanner.py`) -- never a real credential. This test's own point is to prove `RedactSecretFilter` redacts it before it reaches any destination: the next line asserts `handler.lines == [\"user alice presented token <SECRET_1>\"]`, i.e. no plaintext is ever emitted. The rule sees only the literal message text and cannot see that the logger call is wrapped by the filter under test.",
      "rule_id": "sast.rules.python.lang.security.audit.logging.python-logger-credential-disclosure"
    },
    "65255e62b1c093ee": {
      "classification": "false_positive",
      "path": "scripts/browser-package-harness-core.mjs",
      "line": 198,
      "rule_id": "sast.rules.javascript.lang.security.html-in-template-string",
      "rationale": "The template string builds a `<SECRET_N>` / `<TYPE_N>` redaction placeholder label for test-fixture comparison or CLI output text; it is never assigned to `innerHTML`, rendered into a DOM, or otherwise interpreted as markup, so the XSS this rule guards against cannot occur here."
    },
    "157f6252412ceb22": {
      "classification": "false_positive",
      "path": "scripts/qualify-node-addon.mjs",
      "line": 196,
      "rule_id": "sast.rules.javascript.lang.security.html-in-template-string",
      "rationale": "The template string builds a `<SECRET_N>` / `<TYPE_N>` redaction placeholder label for test-fixture comparison or CLI output text; it is never assigned to `innerHTML`, rendered into a DOM, or otherwise interpreted as markup, so the XSS this rule guards against cannot occur here."
    },
    "5365d750d14aca76": {
      "classification": "false_positive",
      "path": "scripts/qualify-node-addon.mjs",
      "line": 328,
      "rule_id": "sast.rules.javascript.lang.security.audit.unsafe-dynamic-method",
      "rationale": "The dynamic member access selects a method name from the hardcoded `DETECTOR_PROFILES` map, whose two entries (`full`/`common`) are literal object keys in this file; `detectorProfile` is validated against `Object.hasOwn(DETECTOR_PROFILES, ...)` before this call, so the invoked name is always one of the two fixed literal strings `initialize` / `initializeCommon`, never attacker- or externally-controlled input. This is a local build-qualification script, not a network-facing service."
    },
    "cb10861efa643c2d": {
      "classification": "false_positive",
      "line": 290,
      "path": "conformance/schema.ts",
      "rationale": "The matched pattern is one of this file's three module-level constant regexes (CASE_ID_PATTERN, IDENTIFIER_PATTERN, ERROR_CODE_PATTERN), each a linear-time character-class repetition with no nested or overlapping quantifiers -- the rule's generic backtracking heuristic cannot see that '-' / '.' / '_' separators are disjoint from the surrounding [a-z0-9] class, so there is no ambiguous split point for the engine to backtrack over. Inputs are also schema-validated fixture/case identifiers, not attacker-controlled request data.",
      "rule_id": "sast.rules.javascript.lang.security.audit.detect-redos"
    },
    "4257c0484813a3bf": {
      "classification": "false_positive",
      "line": 298,
      "path": "conformance/schema.ts",
      "rationale": "The matched pattern is one of this file's three module-level constant regexes (CASE_ID_PATTERN, IDENTIFIER_PATTERN, ERROR_CODE_PATTERN), each a linear-time character-class repetition with no nested or overlapping quantifiers -- the rule's generic backtracking heuristic cannot see that '-' / '.' / '_' separators are disjoint from the surrounding [a-z0-9] class, so there is no ambiguous split point for the engine to backtrack over. Inputs are also schema-validated fixture/case identifiers, not attacker-controlled request data.",
      "rule_id": "sast.rules.javascript.lang.security.audit.detect-redos"
    },
    "63273c6d95d5e58d": {
      "classification": "false_positive",
      "line": 316,
      "path": "conformance/schema.ts",
      "rationale": "The matched pattern is one of this file's three module-level constant regexes (CASE_ID_PATTERN, IDENTIFIER_PATTERN, ERROR_CODE_PATTERN), each a linear-time character-class repetition with no nested or overlapping quantifiers -- the rule's generic backtracking heuristic cannot see that '-' / '.' / '_' separators are disjoint from the surrounding [a-z0-9] class, so there is no ambiguous split point for the engine to backtrack over. Inputs are also schema-validated fixture/case identifiers, not attacker-controlled request data.",
      "rule_id": "sast.rules.javascript.lang.security.audit.detect-redos"
    },
    "6ab8e1a3d2345fe7": {
      "classification": "false_positive",
      "line": 317,
      "path": "conformance/schema.ts",
      "rationale": "The matched pattern is one of this file's three module-level constant regexes (CASE_ID_PATTERN, IDENTIFIER_PATTERN, ERROR_CODE_PATTERN), each a linear-time character-class repetition with no nested or overlapping quantifiers -- the rule's generic backtracking heuristic cannot see that '-' / '.' / '_' separators are disjoint from the surrounding [a-z0-9] class, so there is no ambiguous split point for the engine to backtrack over. Inputs are also schema-validated fixture/case identifiers, not attacker-controlled request data.",
      "rule_id": "sast.rules.javascript.lang.security.audit.detect-redos"
    },
    "aef52ae2d691abc3": {
      "classification": "false_positive",
      "line": 318,
      "path": "conformance/schema.ts",
      "rationale": "The matched pattern is one of this file's three module-level constant regexes (CASE_ID_PATTERN, IDENTIFIER_PATTERN, ERROR_CODE_PATTERN), each a linear-time character-class repetition with no nested or overlapping quantifiers -- the rule's generic backtracking heuristic cannot see that '-' / '.' / '_' separators are disjoint from the surrounding [a-z0-9] class, so there is no ambiguous split point for the engine to backtrack over. Inputs are also schema-validated fixture/case identifiers, not attacker-controlled request data.",
      "rule_id": "sast.rules.javascript.lang.security.audit.detect-redos"
    },
    "4fde4419b646f5cd": {
      "classification": "false_positive",
      "line": 366,
      "path": "conformance/schema.ts",
      "rationale": "The matched pattern is one of this file's three module-level constant regexes (CASE_ID_PATTERN, IDENTIFIER_PATTERN, ERROR_CODE_PATTERN), each a linear-time character-class repetition with no nested or overlapping quantifiers -- the rule's generic backtracking heuristic cannot see that '-' / '.' / '_' separators are disjoint from the surrounding [a-z0-9] class, so there is no ambiguous split point for the engine to backtrack over. Inputs are also schema-validated fixture/case identifiers, not attacker-controlled request data.",
      "rule_id": "sast.rules.javascript.lang.security.audit.detect-redos"
    },
    "63fbcb5daf64ffdc": {
      "classification": "false_positive",
      "line": 367,
      "path": "conformance/schema.ts",
      "rationale": "The matched pattern is one of this file's three module-level constant regexes (CASE_ID_PATTERN, IDENTIFIER_PATTERN, ERROR_CODE_PATTERN), each a linear-time character-class repetition with no nested or overlapping quantifiers -- the rule's generic backtracking heuristic cannot see that '-' / '.' / '_' separators are disjoint from the surrounding [a-z0-9] class, so there is no ambiguous split point for the engine to backtrack over. Inputs are also schema-validated fixture/case identifiers, not attacker-controlled request data.",
      "rule_id": "sast.rules.javascript.lang.security.audit.detect-redos"
    },
    "edc4aa4c0b8c6e61": {
      "classification": "false_positive",
      "line": 408,
      "path": "conformance/schema.ts",
      "rationale": "The matched pattern is one of this file's three module-level constant regexes (CASE_ID_PATTERN, IDENTIFIER_PATTERN, ERROR_CODE_PATTERN), each a linear-time character-class repetition with no nested or overlapping quantifiers -- the rule's generic backtracking heuristic cannot see that '-' / '.' / '_' separators are disjoint from the surrounding [a-z0-9] class, so there is no ambiguous split point for the engine to backtrack over. Inputs are also schema-validated fixture/case identifiers, not attacker-controlled request data.",
      "rule_id": "sast.rules.javascript.lang.security.audit.detect-redos"
    },
    "22cbf265ad31f4dc": {
      "classification": "false_positive",
      "line": 409,
      "path": "conformance/schema.ts",
      "rationale": "The matched pattern is one of this file's three module-level constant regexes (CASE_ID_PATTERN, IDENTIFIER_PATTERN, ERROR_CODE_PATTERN), each a linear-time character-class repetition with no nested or overlapping quantifiers -- the rule's generic backtracking heuristic cannot see that '-' / '.' / '_' separators are disjoint from the surrounding [a-z0-9] class, so there is no ambiguous split point for the engine to backtrack over. Inputs are also schema-validated fixture/case identifiers, not attacker-controlled request data.",
      "rule_id": "sast.rules.javascript.lang.security.audit.detect-redos"
    },
    "9fa7d6ca68162829": {
      "classification": "false_positive",
      "line": 446,
      "path": "conformance/schema.ts",
      "rationale": "The matched pattern is one of this file's three module-level constant regexes (CASE_ID_PATTERN, IDENTIFIER_PATTERN, ERROR_CODE_PATTERN), each a linear-time character-class repetition with no nested or overlapping quantifiers -- the rule's generic backtracking heuristic cannot see that '-' / '.' / '_' separators are disjoint from the surrounding [a-z0-9] class, so there is no ambiguous split point for the engine to backtrack over. Inputs are also schema-validated fixture/case identifiers, not attacker-controlled request data.",
      "rule_id": "sast.rules.javascript.lang.security.audit.detect-redos"
    },
    "9be756ae602e06d5": {
      "classification": "false_positive",
      "line": 541,
      "path": "conformance/schema.ts",
      "rationale": "The matched pattern is one of this file's three module-level constant regexes (CASE_ID_PATTERN, IDENTIFIER_PATTERN, ERROR_CODE_PATTERN), each a linear-time character-class repetition with no nested or overlapping quantifiers -- the rule's generic backtracking heuristic cannot see that '-' / '.' / '_' separators are disjoint from the surrounding [a-z0-9] class, so there is no ambiguous split point for the engine to backtrack over. Inputs are also schema-validated fixture/case identifiers, not attacker-controlled request data.",
      "rule_id": "sast.rules.javascript.lang.security.audit.detect-redos"
    },
    "da06ed3db448df82": {
      "classification": "false_positive",
      "line": 569,
      "path": "conformance/schema.ts",
      "rationale": "The matched pattern is one of this file's three module-level constant regexes (CASE_ID_PATTERN, IDENTIFIER_PATTERN, ERROR_CODE_PATTERN), each a linear-time character-class repetition with no nested or overlapping quantifiers -- the rule's generic backtracking heuristic cannot see that '-' / '.' / '_' separators are disjoint from the surrounding [a-z0-9] class, so there is no ambiguous split point for the engine to backtrack over. Inputs are also schema-validated fixture/case identifiers, not attacker-controlled request data.",
      "rule_id": "sast.rules.javascript.lang.security.audit.detect-redos"
    },
    "d2105a3ab40ab571": {
      "classification": "false_positive",
      "line": 602,
      "path": "conformance/schema.ts",
      "rationale": "The matched pattern is one of this file's three module-level constant regexes (CASE_ID_PATTERN, IDENTIFIER_PATTERN, ERROR_CODE_PATTERN), each a linear-time character-class repetition with no nested or overlapping quantifiers -- the rule's generic backtracking heuristic cannot see that '-' / '.' / '_' separators are disjoint from the surrounding [a-z0-9] class, so there is no ambiguous split point for the engine to backtrack over. Inputs are also schema-validated fixture/case identifiers, not attacker-controlled request data.",
      "rule_id": "sast.rules.javascript.lang.security.audit.detect-redos"
    },
    "88c088ccd4f38718": {
      "classification": "false_positive",
      "line": 893,
      "path": "conformance/schema.ts",
      "rationale": "The matched pattern is one of this file's three module-level constant regexes (CASE_ID_PATTERN, IDENTIFIER_PATTERN, ERROR_CODE_PATTERN), each a linear-time character-class repetition with no nested or overlapping quantifiers -- the rule's generic backtracking heuristic cannot see that '-' / '.' / '_' separators are disjoint from the surrounding [a-z0-9] class, so there is no ambiguous split point for the engine to backtrack over. Inputs are also schema-validated fixture/case identifiers, not attacker-controlled request data.",
      "rule_id": "sast.rules.javascript.lang.security.audit.detect-redos"
    },
    "b4df2a179690b50c": {
      "classification": "false_positive",
      "line": 894,
      "path": "conformance/schema.ts",
      "rationale": "The matched pattern is one of this file's three module-level constant regexes (CASE_ID_PATTERN, IDENTIFIER_PATTERN, ERROR_CODE_PATTERN), each a linear-time character-class repetition with no nested or overlapping quantifiers -- the rule's generic backtracking heuristic cannot see that '-' / '.' / '_' separators are disjoint from the surrounding [a-z0-9] class, so there is no ambiguous split point for the engine to backtrack over. Inputs are also schema-validated fixture/case identifiers, not attacker-controlled request data.",
      "rule_id": "sast.rules.javascript.lang.security.audit.detect-redos"
    },
    "360f48b699b6f2d8": {
      "classification": "false_positive",
      "path": "scripts/browser-package-harness-core.mjs",
      "line": 207,
      "rule_id": "sast.rules.javascript.lang.security.html-in-template-string",
      "rationale": "The template string builds a `<SECRET_N>` / `<TYPE_N>` redaction placeholder label for test-fixture comparison or CLI output text; it is never assigned to `innerHTML`, rendered into a DOM, or otherwise interpreted as markup, so the XSS this rule guards against cannot occur here."
    },
    "ddfe387dd9e6db6a": {
      "classification": "false_positive",
      "path": "scripts/clean-install-doc.mjs",
      "line": 56,
      "rule_id": "sast.rules.javascript.lang.security.audit.prototype-pollution.prototype-pollution-assignment",
      "rationale": "`scenario[role] = ...` writes to a scenario object this file creates itself, and `role` is checked against the literal allowlist `setup`/`run`/`serve`/`expect` a few lines above, so the key can never be `__proto__`, `constructor`, or `prototype`. The input is this repository's own `docs/quickstart.md`, read by a local qualification script. It is not a network-facing service."
    },
    "8b8fd1cd3925ff77": {
      "classification": "false_positive",
      "path": "scripts/qualify-clean-install.mjs",
      "line": 586,
      "rule_id": "sast.rules.javascript.lang.security.audit.unsafe-dynamic-method",
      "rationale": "The dynamic lookup selects a lane function from a literal three-entry object (`node`/`python`/`browser`); `options.lane` is validated against the fixed `LANES` list during argument parsing, before this call, so the invoked function is always one of the three lane functions defined in this file, never externally controlled. This is a local qualification script, not a network-facing service."
    },
    "1069b02b1d7cc6e1": {
      "classification": "false_positive",
      "line": 176,
      "path": "scripts/rehearsal-version.py",
      "rationale": "The URL is built from a fixed, hardcoded registry host (registry.npmjs.org, crates.io, or pypi.org) with only a package name and the throwaway version path-appended; the host is never attacker-influenced, so this cannot be redirected to an untrusted origin.",
      "rule_id": "sast.rules.python.lang.security.audit.dynamic-urllib-use-detected"
    }
  },
  "known_errors": {
    "02ef21d754dd4eb4": {
      "line": 247,
      "path": ".github/workflows/reconcile-release.yml",
      "rationale": "The `sast.rules.yaml.github-actions.security.curl-eval` rule's `metavariable-pattern` sub-check re-parses a `run:` step's shell snippet as Bash; when the snippet contains a GitHub Actions `${{ ... }}` expression, that substring is not valid Bash and the sub-parser reports a partial-parsing error. This is an upstream rule/tool limitation against GitHub Actions expression syntax, not a defect in the workflow file -- the rule's other (successfully parsed) matches in the same file are unaffected.",
      "type": "PartialParsing"
    },
    "c4397c5e3d25faa7": {
      "line": 325,
      "path": ".github/workflows/reconcile-release.yml",
      "rationale": "The `sast.rules.yaml.github-actions.security.curl-eval` rule's `metavariable-pattern` sub-check re-parses a `run:` step's shell snippet as Bash; when the snippet contains a GitHub Actions `${{ ... }}` expression, that substring is not valid Bash and the sub-parser reports a partial-parsing error. This is an upstream rule/tool limitation against GitHub Actions expression syntax, not a defect in the workflow file -- the rule's other (successfully parsed) matches in the same file are unaffected.",
      "type": "PartialParsing"
    },
    "fc5e8d05105ef94e": {
      "line": 389,
      "path": ".github/workflows/reconcile-release.yml",
      "rationale": "The `sast.rules.yaml.github-actions.security.curl-eval` rule's `metavariable-pattern` sub-check re-parses a `run:` step's shell snippet as Bash; when the snippet contains a GitHub Actions `${{ ... }}` expression, that substring is not valid Bash and the sub-parser reports a partial-parsing error. This is an upstream rule/tool limitation against GitHub Actions expression syntax, not a defect in the workflow file -- the rule's other (successfully parsed) matches in the same file are unaffected.",
      "type": "PartialParsing"
    },
    "3b6ddc3b351f01c0": {
      "line": 397,
      "path": ".github/workflows/reconcile-release.yml",
      "rationale": "The `sast.rules.yaml.github-actions.security.curl-eval` rule's `metavariable-pattern` sub-check re-parses a `run:` step's shell snippet as Bash; when the snippet contains a GitHub Actions `${{ ... }}` expression, that substring is not valid Bash and the sub-parser reports a partial-parsing error. This is an upstream rule/tool limitation against GitHub Actions expression syntax, not a defect in the workflow file -- the rule's other (successfully parsed) matches in the same file are unaffected.",
      "type": "PartialParsing"
    },
    "234c8ca03913f3ab": {
      "line": 129,
      "path": ".github/workflows/release.yml",
      "rationale": "The `sast.rules.yaml.github-actions.security.curl-eval` rule's `metavariable-pattern` sub-check re-parses a `run:` step's shell snippet as Bash; when the snippet contains a GitHub Actions `${{ ... }}` expression, that substring is not valid Bash and the sub-parser reports a partial-parsing error. This is an upstream rule/tool limitation against GitHub Actions expression syntax, not a defect in the workflow file -- the rule's other (successfully parsed) matches in the same file are unaffected.",
      "type": "PartialParsing"
    },
    "902c789568df85af": {
      "line": 144,
      "path": ".github/workflows/release.yml",
      "rationale": "The `sast.rules.yaml.github-actions.security.curl-eval` rule's `metavariable-pattern` sub-check re-parses a `run:` step's shell snippet as Bash; when the snippet contains a GitHub Actions `${{ ... }}` expression, that substring is not valid Bash and the sub-parser reports a partial-parsing error. This is an upstream rule/tool limitation against GitHub Actions expression syntax, not a defect in the workflow file -- the rule's other (successfully parsed) matches in the same file are unaffected.",
      "type": "PartialParsing"
    },
    "f249ee9caad7f1d6": {
      "line": 147,
      "path": ".github/workflows/release.yml",
      "rationale": "The `sast.rules.yaml.github-actions.security.curl-eval` rule's `metavariable-pattern` sub-check re-parses a `run:` step's shell snippet as Bash; when the snippet contains a GitHub Actions `${{ ... }}` expression, that substring is not valid Bash and the sub-parser reports a partial-parsing error. This is an upstream rule/tool limitation against GitHub Actions expression syntax, not a defect in the workflow file -- the rule's other (successfully parsed) matches in the same file are unaffected.",
      "type": "PartialParsing"
    },
    "42145b87477fd2bf": {
      "line": 158,
      "path": ".github/workflows/release.yml",
      "rationale": "The `sast.rules.yaml.github-actions.security.curl-eval` rule's `metavariable-pattern` sub-check re-parses a `run:` step's shell snippet as Bash; when the snippet contains a GitHub Actions `${{ ... }}` expression, that substring is not valid Bash and the sub-parser reports a partial-parsing error. This is an upstream rule/tool limitation against GitHub Actions expression syntax, not a defect in the workflow file -- the rule's other (successfully parsed) matches in the same file are unaffected.",
      "type": "PartialParsing"
    },
    "8f6ff9d95485530d": {
      "line": 353,
      "path": ".github/workflows/release.yml",
      "rationale": "The `sast.rules.yaml.github-actions.security.curl-eval` rule's `metavariable-pattern` sub-check re-parses a `run:` step's shell snippet as Bash; when the snippet contains a GitHub Actions `${{ ... }}` expression, that substring is not valid Bash and the sub-parser reports a partial-parsing error. This is an upstream rule/tool limitation against GitHub Actions expression syntax, not a defect in the workflow file -- the rule's other (successfully parsed) matches in the same file are unaffected.",
      "type": "PartialParsing"
    },
    "bec881aa8e314b27": {
      "line": 447,
      "path": ".github/workflows/release.yml",
      "rationale": "The `sast.rules.yaml.github-actions.security.curl-eval` rule's `metavariable-pattern` sub-check re-parses a `run:` step's shell snippet as Bash; when the snippet contains a GitHub Actions `${{ ... }}` expression, that substring is not valid Bash and the sub-parser reports a partial-parsing error. This is an upstream rule/tool limitation against GitHub Actions expression syntax, not a defect in the workflow file -- the rule's other (successfully parsed) matches in the same file are unaffected.",
      "type": "PartialParsing"
    },
    "4ce9fc66e7d9d8c1": {
      "line": 466,
      "path": ".github/workflows/release.yml",
      "rationale": "The `sast.rules.yaml.github-actions.security.curl-eval` rule's `metavariable-pattern` sub-check re-parses a `run:` step's shell snippet as Bash; when the snippet contains a GitHub Actions `${{ ... }}` expression, that substring is not valid Bash and the sub-parser reports a partial-parsing error. This is an upstream rule/tool limitation against GitHub Actions expression syntax, not a defect in the workflow file -- the rule's other (successfully parsed) matches in the same file are unaffected.",
      "type": "PartialParsing"
    },
    "d08a914edecaaf72": {
      "line": 550,
      "path": ".github/workflows/release.yml",
      "rationale": "The `sast.rules.yaml.github-actions.security.curl-eval` rule's `metavariable-pattern` sub-check re-parses a `run:` step's shell snippet as Bash; when the snippet contains a GitHub Actions `${{ ... }}` expression, that substring is not valid Bash and the sub-parser reports a partial-parsing error. This is an upstream rule/tool limitation against GitHub Actions expression syntax, not a defect in the workflow file -- the rule's other (successfully parsed) matches in the same file are unaffected.",
      "type": "PartialParsing"
    },
    "49fdd9e0c469cb2e": {
      "line": 335,
      "path": ".github/workflows/reconcile-release.yml",
      "rationale": "The `sast.rules.yaml.github-actions.security.curl-eval` rule's `metavariable-pattern` sub-check re-parses a `run:` step's shell snippet as Bash; when the snippet contains a GitHub Actions `${{ ... }}` expression, that substring is not valid Bash and the sub-parser reports a partial-parsing error. This is an upstream rule/tool limitation against GitHub Actions expression syntax, not a defect in the workflow file -- the rule's other (successfully parsed) matches in the same file are unaffected.",
      "type": "PartialParsing"
    },
    "c106a6dc1cd0bb27": {
      "line": 407,
      "path": ".github/workflows/reconcile-release.yml",
      "rationale": "The `sast.rules.yaml.github-actions.security.curl-eval` rule's `metavariable-pattern` sub-check re-parses a `run:` step's shell snippet as Bash; when the snippet contains a GitHub Actions `${{ ... }}` expression, that substring is not valid Bash and the sub-parser reports a partial-parsing error. This is an upstream rule/tool limitation against GitHub Actions expression syntax, not a defect in the workflow file -- the rule's other (successfully parsed) matches in the same file are unaffected.",
      "type": "PartialParsing"
    },
    "28621df53b1c64ca": {
      "line": 360,
      "path": ".github/workflows/release.yml",
      "rationale": "The `sast.rules.yaml.github-actions.security.curl-eval` rule's `metavariable-pattern` sub-check re-parses a `run:` step's shell snippet as Bash; when the snippet contains a GitHub Actions `${{ ... }}` expression, that substring is not valid Bash and the sub-parser reports a partial-parsing error. This is an upstream rule/tool limitation against GitHub Actions expression syntax, not a defect in the workflow file -- the rule's other (successfully parsed) matches in the same file are unaffected.",
      "type": "PartialParsing"
    },
    "fcb5c29c505a0c79": {
      "line": 461,
      "path": ".github/workflows/release.yml",
      "rationale": "The `sast.rules.yaml.github-actions.security.curl-eval` rule's `metavariable-pattern` sub-check re-parses a `run:` step's shell snippet as Bash; when the snippet contains a GitHub Actions `${{ ... }}` expression, that substring is not valid Bash and the sub-parser reports a partial-parsing error. This is an upstream rule/tool limitation against GitHub Actions expression syntax, not a defect in the workflow file -- the rule's other (successfully parsed) matches in the same file are unaffected.",
      "type": "PartialParsing"
    },
    "e3bf474b4da38858": {
      "line": 480,
      "path": ".github/workflows/release.yml",
      "rationale": "The `sast.rules.yaml.github-actions.security.curl-eval` rule's `metavariable-pattern` sub-check re-parses a `run:` step's shell snippet as Bash; when the snippet contains a GitHub Actions `${{ ... }}` expression, that substring is not valid Bash and the sub-parser reports a partial-parsing error. This is an upstream rule/tool limitation against GitHub Actions expression syntax, not a defect in the workflow file -- the rule's other (successfully parsed) matches in the same file are unaffected.",
      "type": "PartialParsing"
    },
    "91537adcbe2912b4": {
      "line": 567,
      "path": ".github/workflows/release.yml",
      "rationale": "The `sast.rules.yaml.github-actions.security.curl-eval` rule's `metavariable-pattern` sub-check re-parses a `run:` step's shell snippet as Bash; when the snippet contains a GitHub Actions `${{ ... }}` expression, that substring is not valid Bash and the sub-parser reports a partial-parsing error. This is an upstream rule/tool limitation against GitHub Actions expression syntax, not a defect in the workflow file -- the rule's other (successfully parsed) matches in the same file are unaffected.",
      "type": "PartialParsing"
    },
    "69796645ea40e01f": {
      "line": 29,
      "path": "packages/javascript/src/native.ts",
      "rationale": "OpenGrep 1.30.0's TypeScript parser does not yet support the `unique symbol` type annotation; this is a parser limitation in the pinned tool version, not a defect in this file. Tracked for re-check on the next reviewed OpenGrep version bump.",
      "type": "PartialParsing"
    },
    "3b24a098020fcea1": {
      "line": 128,
      "path": ".github/workflows/release.yml",
      "rationale": "The `sast.rules.yaml.github-actions.security.curl-eval` rule's `metavariable-pattern` sub-check re-parses a `run:` step's shell snippet as Bash; when the snippet contains a GitHub Actions `${{ ... }}` expression, that substring is not valid Bash and the sub-parser reports a partial-parsing error. This is an upstream rule/tool limitation against GitHub Actions expression syntax, not a defect in the workflow file -- the rule's other (successfully parsed) matches in the same file are unaffected.",
      "type": "PartialParsing"
    },
    "59fabb2e6c5f34c9": {
      "line": 143,
      "path": ".github/workflows/release.yml",
      "rationale": "The `sast.rules.yaml.github-actions.security.curl-eval` rule's `metavariable-pattern` sub-check re-parses a `run:` step's shell snippet as Bash; when the snippet contains a GitHub Actions `${{ ... }}` expression, that substring is not valid Bash and the sub-parser reports a partial-parsing error. This is an upstream rule/tool limitation against GitHub Actions expression syntax, not a defect in the workflow file -- the rule's other (successfully parsed) matches in the same file are unaffected.",
      "type": "PartialParsing"
    },
    "84b184145ceb31cb": {
      "line": 146,
      "path": ".github/workflows/release.yml",
      "rationale": "The `sast.rules.yaml.github-actions.security.curl-eval` rule's `metavariable-pattern` sub-check re-parses a `run:` step's shell snippet as Bash; when the snippet contains a GitHub Actions `${{ ... }}` expression, that substring is not valid Bash and the sub-parser reports a partial-parsing error. This is an upstream rule/tool limitation against GitHub Actions expression syntax, not a defect in the workflow file -- the rule's other (successfully parsed) matches in the same file are unaffected.",
      "type": "PartialParsing"
    },
    "453b2ba6ff5b6426": {
      "line": 157,
      "path": ".github/workflows/release.yml",
      "rationale": "The `sast.rules.yaml.github-actions.security.curl-eval` rule's `metavariable-pattern` sub-check re-parses a `run:` step's shell snippet as Bash; when the snippet contains a GitHub Actions `${{ ... }}` expression, that substring is not valid Bash and the sub-parser reports a partial-parsing error. This is an upstream rule/tool limitation against GitHub Actions expression syntax, not a defect in the workflow file -- the rule's other (successfully parsed) matches in the same file are unaffected.",
      "type": "PartialParsing"
    },
    "a795f6296d7eb50d": {
      "line": 359,
      "path": ".github/workflows/release.yml",
      "rationale": "The `sast.rules.yaml.github-actions.security.curl-eval` rule's `metavariable-pattern` sub-check re-parses a `run:` step's shell snippet as Bash; when the snippet contains a GitHub Actions `${{ ... }}` expression, that substring is not valid Bash and the sub-parser reports a partial-parsing error. This is an upstream rule/tool limitation against GitHub Actions expression syntax, not a defect in the workflow file -- the rule's other (successfully parsed) matches in the same file are unaffected.",
      "type": "PartialParsing"
    },
    "f7ce93fcbc9cee7e": {
      "line": 460,
      "path": ".github/workflows/release.yml",
      "rationale": "The `sast.rules.yaml.github-actions.security.curl-eval` rule's `metavariable-pattern` sub-check re-parses a `run:` step's shell snippet as Bash; when the snippet contains a GitHub Actions `${{ ... }}` expression, that substring is not valid Bash and the sub-parser reports a partial-parsing error. This is an upstream rule/tool limitation against GitHub Actions expression syntax, not a defect in the workflow file -- the rule's other (successfully parsed) matches in the same file are unaffected.",
      "type": "PartialParsing"
    },
    "a8ea689b0b95f263": {
      "line": 479,
      "path": ".github/workflows/release.yml",
      "rationale": "The `sast.rules.yaml.github-actions.security.curl-eval` rule's `metavariable-pattern` sub-check re-parses a `run:` step's shell snippet as Bash; when the snippet contains a GitHub Actions `${{ ... }}` expression, that substring is not valid Bash and the sub-parser reports a partial-parsing error. This is an upstream rule/tool limitation against GitHub Actions expression syntax, not a defect in the workflow file -- the rule's other (successfully parsed) matches in the same file are unaffected.",
      "type": "PartialParsing"
    },
    "79746a1978688f7a": {
      "line": 566,
      "path": ".github/workflows/release.yml",
      "rationale": "The `sast.rules.yaml.github-actions.security.curl-eval` rule's `metavariable-pattern` sub-check re-parses a `run:` step's shell snippet as Bash; when the snippet contains a GitHub Actions `${{ ... }}` expression, that substring is not valid Bash and the sub-parser reports a partial-parsing error. This is an upstream rule/tool limitation against GitHub Actions expression syntax, not a defect in the workflow file -- the rule's other (successfully parsed) matches in the same file are unaffected.",
      "type": "PartialParsing"
    },
    "0bbfde62b05e55e2": {
      "type": "PartialParsing",
      "path": ".github/workflows/reconcile-release.yml",
      "line": 269,
      "rationale": "The `sast.rules.yaml.github-actions.security.curl-eval` rule's `metavariable-pattern` sub-check re-parses a `run:` step's shell snippet as Bash; when the snippet contains a GitHub Actions `${{ ... }}` expression, that substring is not valid Bash and the sub-parser reports a partial-parsing error. This is an upstream rule/tool limitation against GitHub Actions expression syntax, not a defect in the workflow file -- the rule's other (successfully parsed) matches in the same file are unaffected."
    },
    "0093b1570bc4e57e": {
      "type": "PartialParsing",
      "path": ".github/workflows/reconcile-release.yml",
      "line": 357,
      "rationale": "The `sast.rules.yaml.github-actions.security.curl-eval` rule's `metavariable-pattern` sub-check re-parses a `run:` step's shell snippet as Bash; when the snippet contains a GitHub Actions `${{ ... }}` expression, that substring is not valid Bash and the sub-parser reports a partial-parsing error. This is an upstream rule/tool limitation against GitHub Actions expression syntax, not a defect in the workflow file -- the rule's other (successfully parsed) matches in the same file are unaffected."
    },
    "c9a6218a1bcd1505": {
      "type": "PartialParsing",
      "path": ".github/workflows/reconcile-release.yml",
      "line": 429,
      "rationale": "The `sast.rules.yaml.github-actions.security.curl-eval` rule's `metavariable-pattern` sub-check re-parses a `run:` step's shell snippet as Bash; when the snippet contains a GitHub Actions `${{ ... }}` expression, that substring is not valid Bash and the sub-parser reports a partial-parsing error. This is an upstream rule/tool limitation against GitHub Actions expression syntax, not a defect in the workflow file -- the rule's other (successfully parsed) matches in the same file are unaffected."
    },
    "0d7514c06bb664ee": {
      "type": "PartialParsing",
      "path": ".github/workflows/release.yml",
      "line": 135,
      "rationale": "The `sast.rules.yaml.github-actions.security.curl-eval` rule's `metavariable-pattern` sub-check re-parses a `run:` step's shell snippet as Bash; when the snippet contains a GitHub Actions `${{ ... }}` expression, that substring is not valid Bash and the sub-parser reports a partial-parsing error. This is an upstream rule/tool limitation against GitHub Actions expression syntax, not a defect in the workflow file -- the rule's other (successfully parsed) matches in the same file are unaffected."
    },
    "594d2a15bf98f00c": {
      "type": "PartialParsing",
      "path": ".github/workflows/release.yml",
      "line": 150,
      "rationale": "The `sast.rules.yaml.github-actions.security.curl-eval` rule's `metavariable-pattern` sub-check re-parses a `run:` step's shell snippet as Bash; when the snippet contains a GitHub Actions `${{ ... }}` expression, that substring is not valid Bash and the sub-parser reports a partial-parsing error. This is an upstream rule/tool limitation against GitHub Actions expression syntax, not a defect in the workflow file -- the rule's other (successfully parsed) matches in the same file are unaffected."
    },
    "ca242ad15c2b6cca": {
      "type": "PartialParsing",
      "path": ".github/workflows/release.yml",
      "line": 153,
      "rationale": "The `sast.rules.yaml.github-actions.security.curl-eval` rule's `metavariable-pattern` sub-check re-parses a `run:` step's shell snippet as Bash; when the snippet contains a GitHub Actions `${{ ... }}` expression, that substring is not valid Bash and the sub-parser reports a partial-parsing error. This is an upstream rule/tool limitation against GitHub Actions expression syntax, not a defect in the workflow file -- the rule's other (successfully parsed) matches in the same file are unaffected."
    },
    "f8a7f1e2c9b67a32": {
      "type": "PartialParsing",
      "path": ".github/workflows/release.yml",
      "line": 164,
      "rationale": "The `sast.rules.yaml.github-actions.security.curl-eval` rule's `metavariable-pattern` sub-check re-parses a `run:` step's shell snippet as Bash; when the snippet contains a GitHub Actions `${{ ... }}` expression, that substring is not valid Bash and the sub-parser reports a partial-parsing error. This is an upstream rule/tool limitation against GitHub Actions expression syntax, not a defect in the workflow file -- the rule's other (successfully parsed) matches in the same file are unaffected."
    },
    "8b7b2b9a627b4fd5": {
      "type": "PartialParsing",
      "path": ".github/workflows/release.yml",
      "line": 280,
      "rationale": "The `sast.rules.yaml.github-actions.security.curl-eval` rule's `metavariable-pattern` sub-check re-parses a `run:` step's shell snippet as Bash; when the snippet contains a GitHub Actions `${{ ... }}` expression, that substring is not valid Bash and the sub-parser reports a partial-parsing error. This is an upstream rule/tool limitation against GitHub Actions expression syntax, not a defect in the workflow file -- the rule's other (successfully parsed) matches in the same file are unaffected."
    },
    "32d063d12a37138a": {
      "type": "PartialParsing",
      "path": ".github/workflows/release.yml",
      "line": 414,
      "rationale": "The `sast.rules.yaml.github-actions.security.curl-eval` rule's `metavariable-pattern` sub-check re-parses a `run:` step's shell snippet as Bash; when the snippet contains a GitHub Actions `${{ ... }}` expression, that substring is not valid Bash and the sub-parser reports a partial-parsing error. This is an upstream rule/tool limitation against GitHub Actions expression syntax, not a defect in the workflow file -- the rule's other (successfully parsed) matches in the same file are unaffected."
    },
    "3992bfb00e259156": {
      "type": "PartialParsing",
      "path": ".github/workflows/release.yml",
      "line": 515,
      "rationale": "The `sast.rules.yaml.github-actions.security.curl-eval` rule's `metavariable-pattern` sub-check re-parses a `run:` step's shell snippet as Bash; when the snippet contains a GitHub Actions `${{ ... }}` expression, that substring is not valid Bash and the sub-parser reports a partial-parsing error. This is an upstream rule/tool limitation against GitHub Actions expression syntax, not a defect in the workflow file -- the rule's other (successfully parsed) matches in the same file are unaffected."
    },
    "9b71872e157be141": {
      "type": "PartialParsing",
      "path": ".github/workflows/release.yml",
      "line": 534,
      "rationale": "The `sast.rules.yaml.github-actions.security.curl-eval` rule's `metavariable-pattern` sub-check re-parses a `run:` step's shell snippet as Bash; when the snippet contains a GitHub Actions `${{ ... }}` expression, that substring is not valid Bash and the sub-parser reports a partial-parsing error. This is an upstream rule/tool limitation against GitHub Actions expression syntax, not a defect in the workflow file -- the rule's other (successfully parsed) matches in the same file are unaffected."
    },
    "c21dd16d0cf0372c": {
      "type": "PartialParsing",
      "path": ".github/workflows/release.yml",
      "line": 621,
      "rationale": "The `sast.rules.yaml.github-actions.security.curl-eval` rule's `metavariable-pattern` sub-check re-parses a `run:` step's shell snippet as Bash; when the snippet contains a GitHub Actions `${{ ... }}` expression, that substring is not valid Bash and the sub-parser reports a partial-parsing error. This is an upstream rule/tool limitation against GitHub Actions expression syntax, not a defect in the workflow file -- the rule's other (successfully parsed) matches in the same file are unaffected."
    },
    "fa932e2bcca56c15": {
      "type": "PartialParsing",
      "path": ".github/workflows/release.yml",
      "line": 139,
      "rationale": "The `sast.rules.yaml.github-actions.security.curl-eval` rule's `metavariable-pattern` sub-check re-parses a `run:` step's shell snippet as Bash; when the snippet contains a GitHub Actions `${{ ... }}` expression, that substring is not valid Bash and the sub-parser reports a partial-parsing error. This is an upstream rule/tool limitation against GitHub Actions expression syntax, not a defect in the workflow file -- the rule's other (successfully parsed) matches in the same file are unaffected."
    },
    "9a31f5684d3f38b6": {
      "type": "PartialParsing",
      "path": ".github/workflows/release.yml",
      "line": 154,
      "rationale": "The `sast.rules.yaml.github-actions.security.curl-eval` rule's `metavariable-pattern` sub-check re-parses a `run:` step's shell snippet as Bash; when the snippet contains a GitHub Actions `${{ ... }}` expression, that substring is not valid Bash and the sub-parser reports a partial-parsing error. This is an upstream rule/tool limitation against GitHub Actions expression syntax, not a defect in the workflow file -- the rule's other (successfully parsed) matches in the same file are unaffected."
    },
    "cba36af28bf0759b": {
      "type": "PartialParsing",
      "path": ".github/workflows/release.yml",
      "line": 168,
      "rationale": "The `sast.rules.yaml.github-actions.security.curl-eval` rule's `metavariable-pattern` sub-check re-parses a `run:` step's shell snippet as Bash; when the snippet contains a GitHub Actions `${{ ... }}` expression, that substring is not valid Bash and the sub-parser reports a partial-parsing error. This is an upstream rule/tool limitation against GitHub Actions expression syntax, not a defect in the workflow file -- the rule's other (successfully parsed) matches in the same file are unaffected."
    },
    "420ad1e50aa12cdc": {
      "type": "PartialParsing",
      "path": ".github/workflows/release.yml",
      "line": 284,
      "rationale": "The `sast.rules.yaml.github-actions.security.curl-eval` rule's `metavariable-pattern` sub-check re-parses a `run:` step's shell snippet as Bash; when the snippet contains a GitHub Actions `${{ ... }}` expression, that substring is not valid Bash and the sub-parser reports a partial-parsing error. This is an upstream rule/tool limitation against GitHub Actions expression syntax, not a defect in the workflow file -- the rule's other (successfully parsed) matches in the same file are unaffected."
    },
    "5f4b8ad0b625c719": {
      "type": "PartialParsing",
      "path": ".github/workflows/release.yml",
      "line": 418,
      "rationale": "The `sast.rules.yaml.github-actions.security.curl-eval` rule's `metavariable-pattern` sub-check re-parses a `run:` step's shell snippet as Bash; when the snippet contains a GitHub Actions `${{ ... }}` expression, that substring is not valid Bash and the sub-parser reports a partial-parsing error. This is an upstream rule/tool limitation against GitHub Actions expression syntax, not a defect in the workflow file -- the rule's other (successfully parsed) matches in the same file are unaffected."
    },
    "1c171813f4c87bda": {
      "type": "PartialParsing",
      "path": ".github/workflows/release.yml",
      "line": 519,
      "rationale": "The `sast.rules.yaml.github-actions.security.curl-eval` rule's `metavariable-pattern` sub-check re-parses a `run:` step's shell snippet as Bash; when the snippet contains a GitHub Actions `${{ ... }}` expression, that substring is not valid Bash and the sub-parser reports a partial-parsing error. This is an upstream rule/tool limitation against GitHub Actions expression syntax, not a defect in the workflow file -- the rule's other (successfully parsed) matches in the same file are unaffected."
    },
    "7aee5b288dce4158": {
      "type": "PartialParsing",
      "path": ".github/workflows/release.yml",
      "line": 538,
      "rationale": "The `sast.rules.yaml.github-actions.security.curl-eval` rule's `metavariable-pattern` sub-check re-parses a `run:` step's shell snippet as Bash; when the snippet contains a GitHub Actions `${{ ... }}` expression, that substring is not valid Bash and the sub-parser reports a partial-parsing error. This is an upstream rule/tool limitation against GitHub Actions expression syntax, not a defect in the workflow file -- the rule's other (successfully parsed) matches in the same file are unaffected."
    },
    "c6362ba455ed9972": {
      "type": "PartialParsing",
      "path": ".github/workflows/release.yml",
      "line": 625,
      "rationale": "The `sast.rules.yaml.github-actions.security.curl-eval` rule's `metavariable-pattern` sub-check re-parses a `run:` step's shell snippet as Bash; when the snippet contains a GitHub Actions `${{ ... }}` expression, that substring is not valid Bash and the sub-parser reports a partial-parsing error. This is an upstream rule/tool limitation against GitHub Actions expression syntax, not a defect in the workflow file -- the rule's other (successfully parsed) matches in the same file are unaffected."
    },
    "30f0d3a8513d43b8": {
      "type": "PartialParsing",
      "path": ".github/workflows/release.yml",
      "line": 149,
      "rationale": "The `sast.rules.yaml.github-actions.security.curl-eval` rule's `metavariable-pattern` sub-check re-parses a `run:` step's shell snippet as Bash; when the snippet contains a GitHub Actions `${{ ... }}` expression, that substring is not valid Bash and the sub-parser reports a partial-parsing error. This is an upstream rule/tool limitation against GitHub Actions expression syntax, not a defect in the workflow file -- the rule's other (successfully parsed) matches in the same file are unaffected."
    },
    "3975b594eba68682": {
      "type": "PartialParsing",
      "path": ".github/workflows/release.yml",
      "line": 167,
      "rationale": "The `sast.rules.yaml.github-actions.security.curl-eval` rule's `metavariable-pattern` sub-check re-parses a `run:` step's shell snippet as Bash; when the snippet contains a GitHub Actions `${{ ... }}` expression, that substring is not valid Bash and the sub-parser reports a partial-parsing error. This is an upstream rule/tool limitation against GitHub Actions expression syntax, not a defect in the workflow file -- the rule's other (successfully parsed) matches in the same file are unaffected."
    },
    "2596c076189d875d": {
      "type": "PartialParsing",
      "path": ".github/workflows/release.yml",
      "line": 178,
      "rationale": "The `sast.rules.yaml.github-actions.security.curl-eval` rule's `metavariable-pattern` sub-check re-parses a `run:` step's shell snippet as Bash; when the snippet contains a GitHub Actions `${{ ... }}` expression, that substring is not valid Bash and the sub-parser reports a partial-parsing error. This is an upstream rule/tool limitation against GitHub Actions expression syntax, not a defect in the workflow file -- the rule's other (successfully parsed) matches in the same file are unaffected."
    },
    "77a791797fcea1e0": {
      "type": "PartialParsing",
      "path": ".github/workflows/release.yml",
      "line": 294,
      "rationale": "The `sast.rules.yaml.github-actions.security.curl-eval` rule's `metavariable-pattern` sub-check re-parses this `run:` step's shell snippet as Bash. The step's body is a `node --input-type=module <<'NODE_VERIFY' ... NODE_VERIFY` heredoc with a quoted delimiter, so real Bash never interprets the heredoc's JavaScript body; the embedded backtick-delimited JS template literals (e.g. the `${reportedCommon}` interpolation ending at line 292) are read by the Bash sub-parser as legacy command-substitution syntax, which is not valid Bash, and it reports a partial-parsing error once it cannot recover. This is an upstream rule/tool limitation against non-Bash content inside a heredoc, not a defect in the workflow file -- the rule's other (successfully parsed) matches in the same file are unaffected."
    },
    "b23397f54c5dc099": {
      "type": "PartialParsing",
      "path": ".github/workflows/release.yml",
      "line": 427,
      "rationale": "The `sast.rules.yaml.github-actions.security.curl-eval` rule's `metavariable-pattern` sub-check re-parses a `run:` step's shell snippet as Bash; when the snippet contains a GitHub Actions `${{ ... }}` expression, that substring is not valid Bash and the sub-parser reports a partial-parsing error. This is an upstream rule/tool limitation against GitHub Actions expression syntax, not a defect in the workflow file -- the rule's other (successfully parsed) matches in the same file are unaffected."
    },
    "f27d1cf100d3fdc1": {
      "type": "PartialParsing",
      "path": ".github/workflows/release.yml",
      "line": 528,
      "rationale": "The `sast.rules.yaml.github-actions.security.curl-eval` rule's `metavariable-pattern` sub-check re-parses a `run:` step's shell snippet as Bash; when the snippet contains a GitHub Actions `${{ ... }}` expression, that substring is not valid Bash and the sub-parser reports a partial-parsing error. This is an upstream rule/tool limitation against GitHub Actions expression syntax, not a defect in the workflow file -- the rule's other (successfully parsed) matches in the same file are unaffected."
    },
    "658c8d20bfc4616a": {
      "type": "PartialParsing",
      "path": ".github/workflows/release.yml",
      "line": 547,
      "rationale": "The `sast.rules.yaml.github-actions.security.curl-eval` rule's `metavariable-pattern` sub-check re-parses a `run:` step's shell snippet as Bash; when the snippet contains a GitHub Actions `${{ ... }}` expression, that substring is not valid Bash and the sub-parser reports a partial-parsing error. This is an upstream rule/tool limitation against GitHub Actions expression syntax, not a defect in the workflow file -- the rule's other (successfully parsed) matches in the same file are unaffected."
    },
    "3ae612d1d25508e6": {
      "type": "PartialParsing",
      "path": ".github/workflows/release.yml",
      "line": 655,
      "rationale": "The `sast.rules.yaml.github-actions.security.curl-eval` rule's `metavariable-pattern` sub-check re-parses a `run:` step's shell snippet as Bash; when the snippet contains a GitHub Actions `${{ ... }}` expression, that substring is not valid Bash and the sub-parser reports a partial-parsing error. This is an upstream rule/tool limitation against GitHub Actions expression syntax, not a defect in the workflow file -- the rule's other (successfully parsed) matches in the same file are unaffected."
    },
    "e351f899a944c110": {
      "type": "PartialParsing",
      "path": ".github/workflows/release.yml",
      "line": 173,
      "rationale": "The `sast.rules.yaml.github-actions.security.curl-eval` rule's `metavariable-pattern` sub-check re-parses a `run:` step's shell snippet as Bash; when the snippet contains a GitHub Actions `${{ ... }}` expression, that substring is not valid Bash and the sub-parser reports a partial-parsing error. This is an upstream rule/tool limitation against GitHub Actions expression syntax, not a defect in the workflow file -- the rule's other (successfully parsed) matches in the same file are unaffected."
    },
    "7772d84607681f60": {
      "type": "PartialParsing",
      "path": ".github/workflows/release.yml",
      "line": 183,
      "rationale": "The `sast.rules.yaml.github-actions.security.curl-eval` rule's `metavariable-pattern` sub-check re-parses a `run:` step's shell snippet as Bash; when the snippet contains a GitHub Actions `${{ ... }}` expression, that substring is not valid Bash and the sub-parser reports a partial-parsing error. This is an upstream rule/tool limitation against GitHub Actions expression syntax, not a defect in the workflow file -- the rule's other (successfully parsed) matches in the same file are unaffected."
    },
    "b69571fadfac488a": {
      "type": "PartialParsing",
      "path": ".github/workflows/release.yml",
      "line": 187,
      "rationale": "The `sast.rules.yaml.github-actions.security.curl-eval` rule's `metavariable-pattern` sub-check re-parses a `run:` step's shell snippet as Bash; when the snippet contains a GitHub Actions `${{ ... }}` expression, that substring is not valid Bash and the sub-parser reports a partial-parsing error. This is an upstream rule/tool limitation against GitHub Actions expression syntax, not a defect in the workflow file -- the rule's other (successfully parsed) matches in the same file are unaffected."
    },
    "698a5b272ad68d26": {
      "type": "PartialParsing",
      "path": ".github/workflows/release.yml",
      "line": 198,
      "rationale": "The `sast.rules.yaml.github-actions.security.curl-eval` rule's `metavariable-pattern` sub-check re-parses a `run:` step's shell snippet as Bash; when the snippet contains a GitHub Actions `${{ ... }}` expression, that substring is not valid Bash and the sub-parser reports a partial-parsing error. This is an upstream rule/tool limitation against GitHub Actions expression syntax, not a defect in the workflow file -- the rule's other (successfully parsed) matches in the same file are unaffected."
    },
    "b09057d1710f18f3": {
      "type": "PartialParsing",
      "path": ".github/workflows/release.yml",
      "line": 229,
      "rationale": "The `sast.rules.yaml.github-actions.security.curl-eval` rule's `metavariable-pattern` sub-check re-parses a `run:` step's shell snippet as Bash; when the snippet contains a GitHub Actions `${{ ... }}` expression, that substring is not valid Bash and the sub-parser reports a partial-parsing error. This is an upstream rule/tool limitation against GitHub Actions expression syntax, not a defect in the workflow file -- the rule's other (successfully parsed) matches in the same file are unaffected."
    },
    "7efb95265efd9a11": {
      "type": "PartialParsing",
      "path": ".github/workflows/release.yml",
      "line": 358,
      "rationale": "The `sast.rules.yaml.github-actions.security.curl-eval` rule's `metavariable-pattern` sub-check re-parses this `run:` step's shell snippet as Bash. The step's body is a `node --input-type=module <<'NODE_VERIFY' ... NODE_VERIFY` heredoc with a quoted delimiter, so real Bash never interprets the heredoc's JavaScript body; the embedded backtick-delimited JS template literals (e.g. the `${reportedCommon}` interpolation at line 356) are read by the Bash sub-parser as legacy command-substitution syntax, which is not valid Bash, and it reports a partial-parsing error once it cannot recover. This is an upstream rule/tool limitation against non-Bash content inside a heredoc, not a defect in the workflow file -- the rule's other (successfully parsed) matches in the same file are unaffected."
    },
    "1cdac6691525f229": {
      "type": "PartialParsing",
      "path": ".github/workflows/release.yml",
      "line": 558,
      "rationale": "The `sast.rules.yaml.github-actions.security.curl-eval` rule's `metavariable-pattern` sub-check re-parses a `run:` step's shell snippet as Bash; when the snippet contains a GitHub Actions `${{ ... }}` expression, that substring is not valid Bash and the sub-parser reports a partial-parsing error. This is an upstream rule/tool limitation against GitHub Actions expression syntax, not a defect in the workflow file -- the rule's other (successfully parsed) matches in the same file are unaffected."
    },
    "2208906f60d7dd7c": {
      "type": "PartialParsing",
      "path": ".github/workflows/release.yml",
      "line": 596,
      "rationale": "The `sast.rules.yaml.github-actions.security.curl-eval` rule's `metavariable-pattern` sub-check re-parses a `run:` step's shell snippet as Bash; when the snippet contains a GitHub Actions `${{ ... }}` expression, that substring is not valid Bash and the sub-parser reports a partial-parsing error. This is an upstream rule/tool limitation against GitHub Actions expression syntax, not a defect in the workflow file -- the rule's other (successfully parsed) matches in the same file are unaffected."
    },
    "3091526b9727b11d": {
      "type": "PartialParsing",
      "path": ".github/workflows/release.yml",
      "line": 696,
      "rationale": "The `sast.rules.yaml.github-actions.security.curl-eval` rule's `metavariable-pattern` sub-check re-parses a `run:` step's shell snippet as Bash; when the snippet contains a GitHub Actions `${{ ... }}` expression, that substring is not valid Bash and the sub-parser reports a partial-parsing error. This is an upstream rule/tool limitation against GitHub Actions expression syntax, not a defect in the workflow file -- the rule's other (successfully parsed) matches in the same file are unaffected."
    },
    "e0c385c4958c0e14": {
      "type": "PartialParsing",
      "path": ".github/workflows/release.yml",
      "line": 713,
      "rationale": "The `sast.rules.yaml.github-actions.security.curl-eval` rule's `metavariable-pattern` sub-check re-parses a `run:` step's shell snippet as Bash; when the snippet contains a GitHub Actions `${{ ... }}` expression, that substring is not valid Bash and the sub-parser reports a partial-parsing error. This is an upstream rule/tool limitation against GitHub Actions expression syntax, not a defect in the workflow file -- the rule's other (successfully parsed) matches in the same file are unaffected."
    },
    "dca0f899713bb957": {
      "type": "PartialParsing",
      "path": ".github/workflows/release.yml",
      "line": 729,
      "rationale": "The `sast.rules.yaml.github-actions.security.curl-eval` rule's `metavariable-pattern` sub-check re-parses a `run:` step's shell snippet as Bash; when the snippet contains a GitHub Actions `${{ ... }}` expression, that substring is not valid Bash and the sub-parser reports a partial-parsing error. This is an upstream rule/tool limitation against GitHub Actions expression syntax, not a defect in the workflow file -- the rule's other (successfully parsed) matches in the same file are unaffected."
    },
    "161555a862b86f8c": {
      "type": "PartialParsing",
      "path": ".github/workflows/release.yml",
      "line": 742,
      "rationale": "The `sast.rules.yaml.github-actions.security.curl-eval` rule's `metavariable-pattern` sub-check re-parses a `run:` step's shell snippet as Bash; when the snippet contains a GitHub Actions `${{ ... }}` expression, that substring is not valid Bash and the sub-parser reports a partial-parsing error. This is an upstream rule/tool limitation against GitHub Actions expression syntax, not a defect in the workflow file -- the rule's other (successfully parsed) matches in the same file are unaffected."
    },
    "d6c1a81af50a271e": {
      "type": "PartialParsing",
      "path": ".github/workflows/release.yml",
      "line": 753,
      "rationale": "The `sast.rules.yaml.github-actions.security.curl-eval` rule's `metavariable-pattern` sub-check re-parses a `run:` step's shell snippet as Bash; when the snippet contains a GitHub Actions `${{ ... }}` expression, that substring is not valid Bash and the sub-parser reports a partial-parsing error. This is an upstream rule/tool limitation against GitHub Actions expression syntax, not a defect in the workflow file -- the rule's other (successfully parsed) matches in the same file are unaffected."
    },
    "d3c526471c58839b": {
      "type": "PartialParsing",
      "path": ".github/workflows/release.yml",
      "line": 784,
      "rationale": "The `sast.rules.yaml.github-actions.security.curl-eval` rule's `metavariable-pattern` sub-check re-parses a `run:` step's shell snippet as Bash; when the snippet contains a GitHub Actions `${{ ... }}` expression, that substring is not valid Bash and the sub-parser reports a partial-parsing error. This is an upstream rule/tool limitation against GitHub Actions expression syntax, not a defect in the workflow file -- the rule's other (successfully parsed) matches in the same file are unaffected."
    },
    "ffcac592d643727f": {
      "type": "PartialParsing",
      "path": ".github/workflows/release.yml",
      "line": 903,
      "rationale": "The `sast.rules.yaml.github-actions.security.curl-eval` rule's `metavariable-pattern` sub-check re-parses a `run:` step's shell snippet as Bash; when the snippet contains a GitHub Actions `${{ ... }}` expression, that substring is not valid Bash and the sub-parser reports a partial-parsing error. This is an upstream rule/tool limitation against GitHub Actions expression syntax, not a defect in the workflow file -- the rule's other (successfully parsed) matches in the same file are unaffected."
    },
    "0228db9c8211f514": {
      "type": "PartialParsing",
      "path": ".github/workflows/release.yml",
      "line": 925,
      "rationale": "The `sast.rules.yaml.github-actions.security.curl-eval` rule's `metavariable-pattern` sub-check re-parses a `run:` step's shell snippet as Bash; when the snippet contains a GitHub Actions `${{ ... }}` expression, that substring is not valid Bash and the sub-parser reports a partial-parsing error. This is an upstream rule/tool limitation against GitHub Actions expression syntax, not a defect in the workflow file -- the rule's other (successfully parsed) matches in the same file are unaffected."
    },
    "5f870ce4eb6cdb80": {
      "type": "PartialParsing",
      "path": ".github/workflows/release.yml",
      "line": 572,
      "rationale": "The `sast.rules.yaml.github-actions.security.curl-eval` rule's `metavariable-pattern` sub-check re-parses a `run:` step's shell snippet as Bash; when the snippet contains a GitHub Actions `${{ ... }}` expression, that substring is not valid Bash and the sub-parser reports a partial-parsing error. This is an upstream rule/tool limitation against GitHub Actions expression syntax, not a defect in the workflow file -- the rule's other (successfully parsed) matches in the same file are unaffected."
    },
    "ac2e76c14c7c70f7": {
      "type": "PartialParsing",
      "path": ".github/workflows/release.yml",
      "line": 604,
      "rationale": "The `sast.rules.yaml.github-actions.security.curl-eval` rule's `metavariable-pattern` sub-check re-parses a `run:` step's shell snippet as Bash; when the snippet contains a GitHub Actions `${{ ... }}` expression, that substring is not valid Bash and the sub-parser reports a partial-parsing error. This is an upstream rule/tool limitation against GitHub Actions expression syntax, not a defect in the workflow file -- the rule's other (successfully parsed) matches in the same file are unaffected."
    },
    "f161e03d2d082db8": {
      "type": "PartialParsing",
      "path": ".github/workflows/release.yml",
      "line": 704,
      "rationale": "The `sast.rules.yaml.github-actions.security.curl-eval` rule's `metavariable-pattern` sub-check re-parses a `run:` step's shell snippet as Bash; when the snippet contains a GitHub Actions `${{ ... }}` expression, that substring is not valid Bash and the sub-parser reports a partial-parsing error. This is an upstream rule/tool limitation against GitHub Actions expression syntax, not a defect in the workflow file -- the rule's other (successfully parsed) matches in the same file are unaffected."
    },
    "08db120b46ba2040": {
      "type": "PartialParsing",
      "path": ".github/workflows/release.yml",
      "line": 721,
      "rationale": "The `sast.rules.yaml.github-actions.security.curl-eval` rule's `metavariable-pattern` sub-check re-parses a `run:` step's shell snippet as Bash; when the snippet contains a GitHub Actions `${{ ... }}` expression, that substring is not valid Bash and the sub-parser reports a partial-parsing error. This is an upstream rule/tool limitation against GitHub Actions expression syntax, not a defect in the workflow file -- the rule's other (successfully parsed) matches in the same file are unaffected."
    },
    "8990e379ce1d46a6": {
      "type": "PartialParsing",
      "path": ".github/workflows/release.yml",
      "line": 737,
      "rationale": "The `sast.rules.yaml.github-actions.security.curl-eval` rule's `metavariable-pattern` sub-check re-parses a `run:` step's shell snippet as Bash; when the snippet contains a GitHub Actions `${{ ... }}` expression, that substring is not valid Bash and the sub-parser reports a partial-parsing error. This is an upstream rule/tool limitation against GitHub Actions expression syntax, not a defect in the workflow file -- the rule's other (successfully parsed) matches in the same file are unaffected."
    },
    "d22c691788ac6a23": {
      "type": "PartialParsing",
      "path": ".github/workflows/release.yml",
      "line": 750,
      "rationale": "The `sast.rules.yaml.github-actions.security.curl-eval` rule's `metavariable-pattern` sub-check re-parses a `run:` step's shell snippet as Bash; when the snippet contains a GitHub Actions `${{ ... }}` expression, that substring is not valid Bash and the sub-parser reports a partial-parsing error. This is an upstream rule/tool limitation against GitHub Actions expression syntax, not a defect in the workflow file -- the rule's other (successfully parsed) matches in the same file are unaffected."
    },
    "883429ce5aa78513": {
      "type": "PartialParsing",
      "path": ".github/workflows/release.yml",
      "line": 761,
      "rationale": "The `sast.rules.yaml.github-actions.security.curl-eval` rule's `metavariable-pattern` sub-check re-parses a `run:` step's shell snippet as Bash; when the snippet contains a GitHub Actions `${{ ... }}` expression, that substring is not valid Bash and the sub-parser reports a partial-parsing error. This is an upstream rule/tool limitation against GitHub Actions expression syntax, not a defect in the workflow file -- the rule's other (successfully parsed) matches in the same file are unaffected."
    },
    "a46c9cc6dea44ff4": {
      "type": "PartialParsing",
      "path": ".github/workflows/release.yml",
      "line": 792,
      "rationale": "The `sast.rules.yaml.github-actions.security.curl-eval` rule's `metavariable-pattern` sub-check re-parses a `run:` step's shell snippet as Bash; when the snippet contains a GitHub Actions `${{ ... }}` expression, that substring is not valid Bash and the sub-parser reports a partial-parsing error. This is an upstream rule/tool limitation against GitHub Actions expression syntax, not a defect in the workflow file -- the rule's other (successfully parsed) matches in the same file are unaffected."
    },
    "e876b068ba0c5989": {
      "type": "PartialParsing",
      "path": ".github/workflows/release.yml",
      "line": 911,
      "rationale": "The `sast.rules.yaml.github-actions.security.curl-eval` rule's `metavariable-pattern` sub-check re-parses a `run:` step's shell snippet as Bash; when the snippet contains a GitHub Actions `${{ ... }}` expression, that substring is not valid Bash and the sub-parser reports a partial-parsing error. This is an upstream rule/tool limitation against GitHub Actions expression syntax, not a defect in the workflow file -- the rule's other (successfully parsed) matches in the same file are unaffected."
    },
    "54b3004d7d69b2b2": {
      "type": "PartialParsing",
      "path": ".github/workflows/release.yml",
      "line": 933,
      "rationale": "The `sast.rules.yaml.github-actions.security.curl-eval` rule's `metavariable-pattern` sub-check re-parses a `run:` step's shell snippet as Bash; when the snippet contains a GitHub Actions `${{ ... }}` expression, that substring is not valid Bash and the sub-parser reports a partial-parsing error. This is an upstream rule/tool limitation against GitHub Actions expression syntax, not a defect in the workflow file -- the rule's other (successfully parsed) matches in the same file are unaffected."
    },
    "62beec2d462a3350": {
      "type": "PartialParsing",
      "path": "packages/javascript/src/native.ts",
      "line": 30,
      "rationale": "OpenGrep 1.30.0's TypeScript parser does not yet support the `unique symbol` type annotation; this is a parser limitation in the pinned tool version, not a defect in this file. Tracked for re-check on the next reviewed OpenGrep version bump."
    },
    "04a0391a149684b8": {
      "line": 573,
      "path": ".github/workflows/release.yml",
      "rationale": "The `sast.rules.yaml.github-actions.security.curl-eval` rule's `metavariable-pattern` sub-check re-parses a `run:` step's shell snippet as Bash; when the snippet contains a GitHub Actions `${{ ... }}` expression, that substring is not valid Bash and the sub-parser reports a partial-parsing error. This is an upstream rule/tool limitation against GitHub Actions expression syntax, not a defect in the workflow file -- the rule's other (successfully parsed) matches in the same file are unaffected.",
      "type": "PartialParsing"
    },
    "bd78dd7a5193293f": {
      "line": 606,
      "path": ".github/workflows/release.yml",
      "rationale": "The `sast.rules.yaml.github-actions.security.curl-eval` rule's `metavariable-pattern` sub-check re-parses a `run:` step's shell snippet as Bash; when the snippet contains a GitHub Actions `${{ ... }}` expression, that substring is not valid Bash and the sub-parser reports a partial-parsing error. This is an upstream rule/tool limitation against GitHub Actions expression syntax, not a defect in the workflow file -- the rule's other (successfully parsed) matches in the same file are unaffected.",
      "type": "PartialParsing"
    },
    "1a4478ad284ba163": {
      "line": 706,
      "path": ".github/workflows/release.yml",
      "rationale": "The `sast.rules.yaml.github-actions.security.curl-eval` rule's `metavariable-pattern` sub-check re-parses a `run:` step's shell snippet as Bash; when the snippet contains a GitHub Actions `${{ ... }}` expression, that substring is not valid Bash and the sub-parser reports a partial-parsing error. This is an upstream rule/tool limitation against GitHub Actions expression syntax, not a defect in the workflow file -- the rule's other (successfully parsed) matches in the same file are unaffected.",
      "type": "PartialParsing"
    },
    "eef2489525acba25": {
      "line": 723,
      "path": ".github/workflows/release.yml",
      "rationale": "The `sast.rules.yaml.github-actions.security.curl-eval` rule's `metavariable-pattern` sub-check re-parses a `run:` step's shell snippet as Bash; when the snippet contains a GitHub Actions `${{ ... }}` expression, that substring is not valid Bash and the sub-parser reports a partial-parsing error. This is an upstream rule/tool limitation against GitHub Actions expression syntax, not a defect in the workflow file -- the rule's other (successfully parsed) matches in the same file are unaffected.",
      "type": "PartialParsing"
    },
    "d4298706630eb0ce": {
      "line": 739,
      "path": ".github/workflows/release.yml",
      "rationale": "The `sast.rules.yaml.github-actions.security.curl-eval` rule's `metavariable-pattern` sub-check re-parses a `run:` step's shell snippet as Bash; when the snippet contains a GitHub Actions `${{ ... }}` expression, that substring is not valid Bash and the sub-parser reports a partial-parsing error. This is an upstream rule/tool limitation against GitHub Actions expression syntax, not a defect in the workflow file -- the rule's other (successfully parsed) matches in the same file are unaffected.",
      "type": "PartialParsing"
    },
    "8e2fda74f022696e": {
      "line": 752,
      "path": ".github/workflows/release.yml",
      "rationale": "The `sast.rules.yaml.github-actions.security.curl-eval` rule's `metavariable-pattern` sub-check re-parses a `run:` step's shell snippet as Bash; when the snippet contains a GitHub Actions `${{ ... }}` expression, that substring is not valid Bash and the sub-parser reports a partial-parsing error. This is an upstream rule/tool limitation against GitHub Actions expression syntax, not a defect in the workflow file -- the rule's other (successfully parsed) matches in the same file are unaffected.",
      "type": "PartialParsing"
    },
    "c085b3231250a60d": {
      "line": 763,
      "path": ".github/workflows/release.yml",
      "rationale": "The `sast.rules.yaml.github-actions.security.curl-eval` rule's `metavariable-pattern` sub-check re-parses a `run:` step's shell snippet as Bash; when the snippet contains a GitHub Actions `${{ ... }}` expression, that substring is not valid Bash and the sub-parser reports a partial-parsing error. This is an upstream rule/tool limitation against GitHub Actions expression syntax, not a defect in the workflow file -- the rule's other (successfully parsed) matches in the same file are unaffected.",
      "type": "PartialParsing"
    },
    "47c43c655a66a019": {
      "line": 794,
      "path": ".github/workflows/release.yml",
      "rationale": "The `sast.rules.yaml.github-actions.security.curl-eval` rule's `metavariable-pattern` sub-check re-parses a `run:` step's shell snippet as Bash; when the snippet contains a GitHub Actions `${{ ... }}` expression, that substring is not valid Bash and the sub-parser reports a partial-parsing error. This is an upstream rule/tool limitation against GitHub Actions expression syntax, not a defect in the workflow file -- the rule's other (successfully parsed) matches in the same file are unaffected.",
      "type": "PartialParsing"
    },
    "2dd8418731282b7f": {
      "line": 913,
      "path": ".github/workflows/release.yml",
      "rationale": "The `sast.rules.yaml.github-actions.security.curl-eval` rule's `metavariable-pattern` sub-check re-parses a `run:` step's shell snippet as Bash; when the snippet contains a GitHub Actions `${{ ... }}` expression, that substring is not valid Bash and the sub-parser reports a partial-parsing error. This is an upstream rule/tool limitation against GitHub Actions expression syntax, not a defect in the workflow file -- the rule's other (successfully parsed) matches in the same file are unaffected.",
      "type": "PartialParsing"
    },
    "c899e2f229e1893e": {
      "line": 935,
      "path": ".github/workflows/release.yml",
      "rationale": "The `sast.rules.yaml.github-actions.security.curl-eval` rule's `metavariable-pattern` sub-check re-parses a `run:` step's shell snippet as Bash; when the snippet contains a GitHub Actions `${{ ... }}` expression, that substring is not valid Bash and the sub-parser reports a partial-parsing error. This is an upstream rule/tool limitation against GitHub Actions expression syntax, not a defect in the workflow file -- the rule's other (successfully parsed) matches in the same file are unaffected.",
      "type": "PartialParsing"
    },
    "e465e4051aab0211": {
      "line": 420,
      "path": ".github/workflows/artifact-qualification.yml",
      "rationale": "The `sast.rules.yaml.github-actions.security.curl-eval` rule's `metavariable-pattern` sub-check re-parses a `run:` step's shell snippet as Bash; when the snippet contains a GitHub Actions `${{ ... }}` expression, that substring is not valid Bash and the sub-parser reports a partial-parsing error. This is an upstream rule/tool limitation against GitHub Actions expression syntax, not a defect in the workflow file -- the rule's other (successfully parsed) matches in the same file are unaffected.",
      "type": "PartialParsing"
    },
    "81f642aab5c3b056": {
      "line": 429,
      "path": ".github/workflows/artifact-qualification.yml",
      "rationale": "The `sast.rules.yaml.github-actions.security.curl-eval` rule's `metavariable-pattern` sub-check re-parses a `run:` step's shell snippet as Bash; when the snippet contains a GitHub Actions `${{ ... }}` expression, that substring is not valid Bash and the sub-parser reports a partial-parsing error. This is an upstream rule/tool limitation against GitHub Actions expression syntax, not a defect in the workflow file -- the rule's other (successfully parsed) matches in the same file are unaffected.",
      "type": "PartialParsing"
    },
    "d3570c2c99bce5d0": {
      "line": 719,
      "path": ".github/workflows/artifact-qualification.yml",
      "rationale": "The `sast.rules.yaml.github-actions.security.curl-eval` rule's `metavariable-pattern` sub-check re-parses a `run:` step's shell snippet as Bash; when the snippet contains a GitHub Actions `${{ ... }}` expression, that substring is not valid Bash and the sub-parser reports a partial-parsing error. This is an upstream rule/tool limitation against GitHub Actions expression syntax, not a defect in the workflow file -- the rule's other (successfully parsed) matches in the same file are unaffected.",
      "type": "PartialParsing"
    },
    "34a9363fc069d323": {
      "line": 788,
      "path": ".github/workflows/artifact-qualification.yml",
      "rationale": "The `sast.rules.yaml.github-actions.security.curl-eval` rule's `metavariable-pattern` sub-check re-parses a `run:` step's shell snippet as Bash; when the snippet contains a GitHub Actions `${{ ... }}` expression, that substring is not valid Bash and the sub-parser reports a partial-parsing error. This is an upstream rule/tool limitation against GitHub Actions expression syntax, not a defect in the workflow file -- the rule's other (successfully parsed) matches in the same file are unaffected.",
      "type": "PartialParsing"
    },
    "eb6601c625fdf185": {
      "line": 258,
      "path": ".github/workflows/artifact-qualification.yml",
      "rationale": "The `sast.rules.yaml.github-actions.security.curl-eval` rule's `metavariable-pattern` sub-check re-parses a `run:` step's shell snippet as Bash; when the snippet contains a GitHub Actions `${{ ... }}` expression, that substring is not valid Bash and the sub-parser reports a partial-parsing error. This is an upstream rule/tool limitation against GitHub Actions expression syntax, not a defect in the workflow file -- the rule's other (successfully parsed) matches in the same file are unaffected.",
      "type": "PartialParsing"
    },
    "f48d1e2de803ad4e": {
      "line": 462,
      "path": ".github/workflows/artifact-qualification.yml",
      "rationale": "The `sast.rules.yaml.github-actions.security.curl-eval` rule's `metavariable-pattern` sub-check re-parses a `run:` step's shell snippet as Bash; when the snippet contains a GitHub Actions `${{ ... }}` expression, that substring is not valid Bash and the sub-parser reports a partial-parsing error. This is an upstream rule/tool limitation against GitHub Actions expression syntax, not a defect in the workflow file -- the rule's other (successfully parsed) matches in the same file are unaffected.",
      "type": "PartialParsing"
    },
    "6ccc2f6da5c086d6": {
      "line": 530,
      "path": ".github/workflows/artifact-qualification.yml",
      "rationale": "The `sast.rules.yaml.github-actions.security.curl-eval` rule's `metavariable-pattern` sub-check re-parses a `run:` step's shell snippet as Bash; when the snippet contains a GitHub Actions `${{ ... }}` expression, that substring is not valid Bash and the sub-parser reports a partial-parsing error. This is an upstream rule/tool limitation against GitHub Actions expression syntax, not a defect in the workflow file -- the rule's other (successfully parsed) matches in the same file are unaffected.",
      "type": "PartialParsing"
    },
    "932985af0f2b05e2": {
      "line": 595,
      "path": ".github/workflows/artifact-qualification.yml",
      "rationale": "The `sast.rules.yaml.github-actions.security.curl-eval` rule's `metavariable-pattern` sub-check re-parses a `run:` step's shell snippet as Bash; when the snippet contains a GitHub Actions `${{ ... }}` expression, that substring is not valid Bash and the sub-parser reports a partial-parsing error. This is an upstream rule/tool limitation against GitHub Actions expression syntax, not a defect in the workflow file -- the rule's other (successfully parsed) matches in the same file are unaffected.",
      "type": "PartialParsing"
    },
    "070037ccd7c42c7b": {
      "line": 664,
      "path": ".github/workflows/artifact-qualification.yml",
      "rationale": "The `sast.rules.yaml.github-actions.security.curl-eval` rule's `metavariable-pattern` sub-check re-parses a `run:` step's shell snippet as Bash; when the snippet contains a GitHub Actions `${{ ... }}` expression, that substring is not valid Bash and the sub-parser reports a partial-parsing error. This is an upstream rule/tool limitation against GitHub Actions expression syntax, not a defect in the workflow file -- the rule's other (successfully parsed) matches in the same file are unaffected.",
      "type": "PartialParsing"
    },
    "2064bc051e87b97b": {
      "type": "PartialParsing",
      "path": ".github/workflows/artifact-qualification.yml",
      "line": 348,
      "rationale": "The `sast.rules.yaml.github-actions.security.curl-eval` rule's `metavariable-pattern` sub-check re-parses a `run:` step's shell snippet as Bash; when the snippet contains a GitHub Actions `${{ ... }}` expression, that substring is not valid Bash and the sub-parser reports a partial-parsing error. This is an upstream rule/tool limitation against GitHub Actions expression syntax, not a defect in the workflow file -- the rule's other (successfully parsed) matches in the same file are unaffected."
    },
    "e088db656e4f1374": {
      "type": "PartialParsing",
      "path": ".github/workflows/artifact-qualification.yml",
      "line": 357,
      "rationale": "The `sast.rules.yaml.github-actions.security.curl-eval` rule's `metavariable-pattern` sub-check re-parses a `run:` step's shell snippet as Bash; when the snippet contains a GitHub Actions `${{ ... }}` expression, that substring is not valid Bash and the sub-parser reports a partial-parsing error. This is an upstream rule/tool limitation against GitHub Actions expression syntax, not a defect in the workflow file -- the rule's other (successfully parsed) matches in the same file are unaffected."
    },
    "b34dfc21bfbcd36e": {
      "type": "PartialParsing",
      "path": ".github/workflows/artifact-qualification.yml",
      "line": 642,
      "rationale": "The `sast.rules.yaml.github-actions.security.curl-eval` rule's `metavariable-pattern` sub-check re-parses a `run:` step's shell snippet as Bash; when the snippet contains a GitHub Actions `${{ ... }}` expression, that substring is not valid Bash and the sub-parser reports a partial-parsing error. This is an upstream rule/tool limitation against GitHub Actions expression syntax, not a defect in the workflow file -- the rule's other (successfully parsed) matches in the same file are unaffected."
    },
    "f157a3a3755644c5": {
      "type": "PartialParsing",
      "path": ".github/workflows/artifact-qualification.yml",
      "line": 706,
      "rationale": "The `sast.rules.yaml.github-actions.security.curl-eval` rule's `metavariable-pattern` sub-check re-parses a `run:` step's shell snippet as Bash; when the snippet contains a GitHub Actions `${{ ... }}` expression, that substring is not valid Bash and the sub-parser reports a partial-parsing error. This is an upstream rule/tool limitation against GitHub Actions expression syntax, not a defect in the workflow file -- the rule's other (successfully parsed) matches in the same file are unaffected."
    },
    "06caeba9a237acfa": {
      "line": 447,
      "path": ".github/workflows/artifact-qualification.yml",
      "rationale": "The `sast.rules.yaml.github-actions.security.curl-eval` rule's `metavariable-pattern` sub-check re-parses a `run:` step's shell snippet as Bash; when the snippet contains a GitHub Actions `${{ ... }}` expression, that substring is not valid Bash and the sub-parser reports a partial-parsing error. This is an upstream rule/tool limitation against GitHub Actions expression syntax, not a defect in the workflow file -- the rule's other (successfully parsed) matches in the same file are unaffected.",
      "type": "PartialParsing"
    },
    "a583d805b5f12645": {
      "line": 456,
      "path": ".github/workflows/artifact-qualification.yml",
      "rationale": "The `sast.rules.yaml.github-actions.security.curl-eval` rule's `metavariable-pattern` sub-check re-parses a `run:` step's shell snippet as Bash; when the snippet contains a GitHub Actions `${{ ... }}` expression, that substring is not valid Bash and the sub-parser reports a partial-parsing error. This is an upstream rule/tool limitation against GitHub Actions expression syntax, not a defect in the workflow file -- the rule's other (successfully parsed) matches in the same file are unaffected.",
      "type": "PartialParsing"
    },
    "7332aaf14b649cab": {
      "line": 746,
      "path": ".github/workflows/artifact-qualification.yml",
      "rationale": "The `sast.rules.yaml.github-actions.security.curl-eval` rule's `metavariable-pattern` sub-check re-parses a `run:` step's shell snippet as Bash; when the snippet contains a GitHub Actions `${{ ... }}` expression, that substring is not valid Bash and the sub-parser reports a partial-parsing error. This is an upstream rule/tool limitation against GitHub Actions expression syntax, not a defect in the workflow file -- the rule's other (successfully parsed) matches in the same file are unaffected.",
      "type": "PartialParsing"
    },
    "c9d05155a444ec99": {
      "line": 815,
      "path": ".github/workflows/artifact-qualification.yml",
      "rationale": "The `sast.rules.yaml.github-actions.security.curl-eval` rule's `metavariable-pattern` sub-check re-parses a `run:` step's shell snippet as Bash; when the snippet contains a GitHub Actions `${{ ... }}` expression, that substring is not valid Bash and the sub-parser reports a partial-parsing error. This is an upstream rule/tool limitation against GitHub Actions expression syntax, not a defect in the workflow file -- the rule's other (successfully parsed) matches in the same file are unaffected.",
      "type": "PartialParsing"
    },
    "2ff5d0faa8a20f1b": {
      "line": 452,
      "path": ".github/workflows/artifact-qualification.yml",
      "rationale": "The `sast.rules.yaml.github-actions.security.curl-eval` rule's `metavariable-pattern` sub-check re-parses a `run:` step's shell snippet as Bash; when the snippet contains a GitHub Actions `${{ ... }}` expression, that substring is not valid Bash and the sub-parser reports a partial-parsing error. This is an upstream rule/tool limitation against GitHub Actions expression syntax, not a defect in the workflow file -- the rule's other (successfully parsed) matches in the same file are unaffected.",
      "type": "PartialParsing"
    },
    "dbd9550d30045776": {
      "line": 461,
      "path": ".github/workflows/artifact-qualification.yml",
      "rationale": "The `sast.rules.yaml.github-actions.security.curl-eval` rule's `metavariable-pattern` sub-check re-parses a `run:` step's shell snippet as Bash; when the snippet contains a GitHub Actions `${{ ... }}` expression, that substring is not valid Bash and the sub-parser reports a partial-parsing error. This is an upstream rule/tool limitation against GitHub Actions expression syntax, not a defect in the workflow file -- the rule's other (successfully parsed) matches in the same file are unaffected.",
      "type": "PartialParsing"
    },
    "326bc2f5b2449e85": {
      "line": 751,
      "path": ".github/workflows/artifact-qualification.yml",
      "rationale": "The `sast.rules.yaml.github-actions.security.curl-eval` rule's `metavariable-pattern` sub-check re-parses a `run:` step's shell snippet as Bash; when the snippet contains a GitHub Actions `${{ ... }}` expression, that substring is not valid Bash and the sub-parser reports a partial-parsing error. This is an upstream rule/tool limitation against GitHub Actions expression syntax, not a defect in the workflow file -- the rule's other (successfully parsed) matches in the same file are unaffected.",
      "type": "PartialParsing"
    },
    "4209c87e6eb56573": {
      "line": 820,
      "path": ".github/workflows/artifact-qualification.yml",
      "rationale": "The `sast.rules.yaml.github-actions.security.curl-eval` rule's `metavariable-pattern` sub-check re-parses a `run:` step's shell snippet as Bash; when the snippet contains a GitHub Actions `${{ ... }}` expression, that substring is not valid Bash and the sub-parser reports a partial-parsing error. This is an upstream rule/tool limitation against GitHub Actions expression syntax, not a defect in the workflow file -- the rule's other (successfully parsed) matches in the same file are unaffected.",
      "type": "PartialParsing"
    },
    "289e26a0f3b29928": {
      "line": 984,
      "path": ".github/workflows/artifact-qualification.yml",
      "rationale": "The `sast.rules.yaml.github-actions.security.curl-eval` rule's `metavariable-pattern` sub-check re-parses a `run:` step's shell snippet as Bash; when the snippet contains a GitHub Actions `${{ ... }}` expression, that substring is not valid Bash and the sub-parser reports a partial-parsing error. This is an upstream rule/tool limitation against GitHub Actions expression syntax, not a defect in the workflow file -- the rule's other (successfully parsed) matches in the same file are unaffected.",
      "type": "PartialParsing"
    },
    "13aa57cb30ff656f": {
      "line": 209,
      "path": ".github/workflows/python-wheels.yml",
      "rationale": "The `sast.rules.yaml.github-actions.security.curl-eval` rule's `metavariable-pattern` sub-check re-parses a `run:` step's shell snippet as Bash; when the snippet contains a GitHub Actions `${{ ... }}` expression, that substring is not valid Bash and the sub-parser reports a partial-parsing error. This is an upstream rule/tool limitation against GitHub Actions expression syntax, not a defect in the workflow file -- the rule's other (successfully parsed) matches in the same file are unaffected.",
      "type": "PartialParsing"
    },
    "eba3e13d96728eab": {
      "line": 219,
      "path": ".github/workflows/python-wheels.yml",
      "rationale": "The `sast.rules.yaml.github-actions.security.curl-eval` rule's `metavariable-pattern` sub-check re-parses a `run:` step's shell snippet as Bash; when the snippet contains a GitHub Actions `${{ ... }}` expression, that substring is not valid Bash and the sub-parser reports a partial-parsing error. This is an upstream rule/tool limitation against GitHub Actions expression syntax, not a defect in the workflow file -- the rule's other (successfully parsed) matches in the same file are unaffected.",
      "type": "PartialParsing"
    },
    "c594538837eaf1de": {
      "line": 271,
      "path": ".github/workflows/python-wheels.yml",
      "rationale": "The `sast.rules.yaml.github-actions.security.curl-eval` rule's `metavariable-pattern` sub-check re-parses a `run:` step's shell snippet as Bash; when the snippet contains a GitHub Actions `${{ ... }}` expression, that substring is not valid Bash and the sub-parser reports a partial-parsing error. This is an upstream rule/tool limitation against GitHub Actions expression syntax, not a defect in the workflow file -- the rule's other (successfully parsed) matches in the same file are unaffected.",
      "type": "PartialParsing"
    },
    "9aa928ce893dbca7": {
      "line": 281,
      "path": ".github/workflows/python-wheels.yml",
      "rationale": "The `sast.rules.yaml.github-actions.security.curl-eval` rule's `metavariable-pattern` sub-check re-parses a `run:` step's shell snippet as Bash; when the snippet contains a GitHub Actions `${{ ... }}` expression, that substring is not valid Bash and the sub-parser reports a partial-parsing error. This is an upstream rule/tool limitation against GitHub Actions expression syntax, not a defect in the workflow file -- the rule's other (successfully parsed) matches in the same file are unaffected.",
      "type": "PartialParsing"
    },
    "4912746791c7a2f8": {
      "line": 338,
      "path": ".github/workflows/python-wheels.yml",
      "rationale": "The `sast.rules.yaml.github-actions.security.curl-eval` rule's `metavariable-pattern` sub-check re-parses a `run:` step's shell snippet as Bash; when the snippet contains a GitHub Actions `${{ ... }}` expression, that substring is not valid Bash and the sub-parser reports a partial-parsing error. This is an upstream rule/tool limitation against GitHub Actions expression syntax, not a defect in the workflow file -- the rule's other (successfully parsed) matches in the same file are unaffected.",
      "type": "PartialParsing"
    }
  }
}
