{
  "schemaVersion": 1,
  "issue": "https://github.com/redact-secret/redact-secret/issues/367",
  "reviewedAt": "2026-09-17",
  "decision": "docs/decisions/2026-09-17-freeze-precision-contracts-for-seven-provider-families.md",
  "note": "Reviewed precision contracts for seven provider families. Each `grammar` is a reference regular expression over the credential itself; `scripts/audit-precision-contracts.py` wraps it in the shared boundary rule (no `[A-Za-z0-9_-]` byte immediately before or after) when it evaluates a fixture. The grammar is an evidence oracle for review, not a runtime detector: the Rust core stays the only authoritative implementation. `basis` records whether a property comes from provider documentation (`provider`), from agreement between the pinned tool sources (`tool-agreement`), from one tool source only (`single-tool`), or from this project's own support policy (`support-policy`). A support-policy property is a choice this project makes and can revisit; it is never presented as provider evidence.",
  "evidenceTiers": {
    "T1": "provider documentation establishes the property",
    "T2": "no provider documentation for the property; pinned tool sources corroborate it",
    "T3": "project policy, not a format claim",
    "T0": "pending: no reviewed contract; unscored"
  },
  "selectedApproach": {
    "id": "default-rule-precision",
    "summary": "Refine the default provider rules in place so they enforce the reviewed contract. No strict mode, no new public option, no new detector id, no finding-type or result-interface change.",
    "alternativesRejected": [
      "A separate strict mode or option: splits the corpus into two truths, leaves the default rule broad, and adds public surface for a precision problem that the default rule caused.",
      "Reclassifying the twins as policy cases: hides the false alarms instead of removing them; reclassification never counts as a detector improvement (issue #367)."
    ]
  },
  "boundary": {
    "alphabet": "[A-Za-z0-9_-]",
    "rule": "A candidate is rejected when the byte immediately before its first byte or immediately after its last byte is in the boundary alphabet. This is beta.4's existing `pattern::boundary_ok` rule and is unchanged by the contracts.",
    "source": "crates/secret-scan-core/src/detectors/pattern.rs"
  },
  "sources": {
    "slack-docs-tokens": {
      "url": "https://docs.slack.dev/authentication/tokens",
      "kind": "provider",
      "revision": "page shows no version or last-updated date",
      "observedAt": "2026-09-17",
      "establishes": [
        "prefixes: `xoxb-` (bot), `xoxp-` (user), `xwfp-` (workflow), `xapp-` (app-level)",
        "structure: \"The token is divided into several sections, separated by a `-` character. The final section is the secret.\"",
        "user-token secret length: 32 characters; tokens created before August 2016 may have 6- or 10-character secrets and can be rotated to 32",
        "example user token shape `xoxp-111-222-333-<32 hex characters>` (three placeholder numeric sections before the secret)"
      ],
      "doesNotEstablish": [
        "numeric section widths (the examples use `111`, `222`, `333` placeholders)",
        "bot-token secret length or alphabet",
        "any `xoxe` prefix, or legacy `xoxa`/`xoxr`/`xoxs`/`xoxo` shapes"
      ]
    },
    "slack-docs-rotation": {
      "url": "https://docs.slack.dev/authentication/using-token-rotation/",
      "kind": "provider",
      "revision": "page shows no version or last-updated date",
      "observedAt": "2026-09-17",
      "establishes": [
        "rotated access tokens carry `xoxe.xoxb-1-...` (bot) and `xoxe.xoxp-1-1234-...` (user) prefixes; the refresh token carries `xoxe-1-...`",
        "access tokens expire after 43,200 seconds"
      ],
      "doesNotEstablish": ["section widths, secret length or alphabet: every example elides the body with `...`"]
    },
    "digitalocean-release-notes": {
      "url": "https://docs.digitalocean.com/release-notes/api/",
      "kind": "provider",
      "revision": "entry dated 2022-03-29",
      "observedAt": "2026-09-17",
      "establishes": ["prefixes: `dop_v1_` (personal access tokens), `doo_v1_` (OAuth access tokens), `dor_v1_` (OAuth refresh tokens)"],
      "doesNotEstablish": ["body length or alphabet"]
    },
    "digitalocean-oauth-reference": {
      "url": "https://docs.digitalocean.com/reference/api/oauth/",
      "kind": "provider",
      "revision": "last updated 2026-09-03",
      "observedAt": "2026-09-17",
      "establishes": ["example `access_token` (`doo_v1_EXAMPLE...`) and `refresh_token` (`dor_v1_EXAMPLE...`) values whose bodies are each 64 characters long"],
      "doesNotEstablish": ["alphabet: both example bodies contain the uppercase placeholder `EXAMPLE`, and the refresh example contains a `y`, so they are illustrations of length, not of a hexadecimal alphabet"]
    },
    "cloudflare-token-formats": {
      "url": "https://developers.cloudflare.com/fundamentals/api/get-started/token-formats/",
      "kind": "provider",
      "revision": "last updated 2026-04-20",
      "observedAt": "2026-09-17",
      "establishes": [
        "\"Each credential type has a distinct prefix followed by 40 characters and a checksum.\"",
        "formats: Global API Key `cfk_[40 characters][checksum]`, User API Token `cfut_[40 characters][checksum]`, Account API Token `cfat_[40 characters][checksum]`",
        "pre-2026 formats: Global API Key 37–45 character lowercase hex; User and Account API Token 40-character alphanumeric, unprefixed; these continue to work",
        "the checksum exists so scanners can verify a token before reporting it"
      ],
      "doesNotEstablish": ["checksum width, checksum alphabet or algorithm", "the alphabet of the 40-character body in the new format"]
    },
    "cloudflare-create-token": {
      "url": "https://developers.cloudflare.com/fundamentals/api/get-started/create-token/",
      "kind": "provider",
      "revision": "last updated 2026-04-20",
      "observedAt": "2026-09-17",
      "establishes": ["\"New API tokens use the `cfut_` prefixed scannable format, which allows credential scanning tools to detect leaked tokens.\""]
    },
    "cloudflare-account-tokens": {
      "url": "https://developers.cloudflare.com/fundamentals/api/get-started/account-owned-tokens/",
      "kind": "provider",
      "revision": "last updated 2026-09-02",
      "observedAt": "2026-09-17",
      "establishes": ["new account API tokens use the `cfat_` prefixed scannable format"]
    },
    "huggingface-docs-tokens": {
      "url": "https://huggingface.co/docs/hub/security-tokens",
      "kind": "provider",
      "revision": "page shows no version or last-updated date",
      "observedAt": "2026-09-17",
      "establishes": ["user access tokens exist in fine-grained, read and write roles; the only token text shown is the placeholder `hf_...`"],
      "doesNotEstablish": ["length, alphabet, or any format difference between roles", "the organization `api_org_` token shape"]
    },
    "linear-docs-graphql": {
      "url": "https://linear.app/developers/graphql",
      "kind": "provider",
      "revision": "page shows no version or last-updated date",
      "observedAt": "2026-09-17",
      "establishes": ["personal API keys are sent as `Authorization: <API_KEY>`; OAuth access tokens as `Authorization: Bearer <ACCESS_TOKEN>`"],
      "doesNotEstablish": ["any prefix, length or alphabet"]
    },
    "linear-docs-oauth": {
      "url": "https://linear.app/developers/oauth-2-0-authentication",
      "kind": "provider",
      "revision": "states that all OAuth2 applications migrated to refresh tokens on 2026-04-01; no page date shown",
      "observedAt": "2026-09-17",
      "establishes": ["the example `access_token` is a bare 64-character lowercase-hex string with no `lin_oauth_` prefix; access tokens are valid for 24 hours"],
      "doesNotEstablish": ["that `lin_oauth_` is an issued prefix; no consulted provider page shows it"]
    },
    "linear-docs-api-webhooks": {
      "url": "https://linear.app/docs/api-and-webhooks",
      "kind": "provider",
      "revision": "page shows no version or last-updated date",
      "observedAt": "2026-09-17",
      "establishes": ["personal API keys are created under Settings > Account > Security & Access with optional permission restriction"],
      "doesNotEstablish": ["any prefix, length or alphabet"]
    },
    "docker-docs-pat": {
      "url": "https://docs.docker.com/security/access-tokens/",
      "kind": "provider",
      "revision": "page shows no version or last-updated date",
      "observedAt": "2026-09-17",
      "establishes": ["personal access tokens exist and replace passwords for CLI login"],
      "doesNotEstablish": ["any prefix, length or alphabet"]
    },
    "docker-docs-oat": {
      "url": "https://docs.docker.com/security/for-admins/access-tokens/",
      "kind": "provider",
      "revision": "page shows no version or last-updated date",
      "observedAt": "2026-09-17",
      "establishes": ["organization access tokens exist and are distinct from personal access tokens"],
      "doesNotEstablish": ["any prefix, length or alphabet"]
    },
    "docker-ai-governance-api": {
      "url": "https://docs.docker.com/reference/api/ai-governance/api.yaml",
      "kind": "provider",
      "revision": "docker/docs content/reference/api/ai-governance/api.yaml, present since 553c69e1b7 (2026-06-02)",
      "observedAt": "2026-09-23",
      "establishes": ["the `bearerAuth` credential table's Format column states `dckr_pat_*` for a Personal Access Token and `dckr_oat_*` for an Organization Access Token (issues #647, #648)"],
      "doesNotEstablish": ["body length or alphabet: `*` is a glob, not a grammar"]
    },
    "docker-hub-api-spec": {
      "url": "https://docs.docker.com/reference/api/hub/latest.yaml",
      "kind": "provider",
      "revision": "page shows no version; two occurrences re-counted 2026-09-23",
      "observedAt": "2026-09-23",
      "establishes": ["an example `createOrgAccessTokenResponse.token` value of `dckr_oat_` followed by a 27-character `[A-Za-z0-9]` body (example strength only; issue #708)"],
      "doesNotEstablish": ["that 27 is the only issued OAT width, or the body alphabet as a rule"]
    },
    "openai-help-api-key": {
      "url": "https://help.openai.com/en/articles/4936850-where-do-i-find-my-openai-api-key",
      "kind": "provider",
      "revision": "not retrievable: HTTP 403 to automated fetch on 2026-09-17",
      "observedAt": "2026-09-17",
      "establishes": [],
      "doesNotEstablish": ["nothing could be reviewed; no OpenAI page that publishes a key grammar is known to this review"]
    },
    "openai-platform-authentication": {
      "url": "https://platform.openai.com/docs/api-reference/authentication",
      "kind": "provider",
      "revision": "not retrievable: HTTP 403 to automated fetch on 2026-09-17",
      "observedAt": "2026-09-17",
      "establishes": [],
      "doesNotEstablish": ["nothing could be reviewed"]
    },
    "gitleaks-8.30.1": {
      "url": "https://github.com/gitleaks/gitleaks/blob/v8.30.1/config/gitleaks.toml",
      "kind": "tool",
      "revision": "tag v8.30.1 = commit 83d9cd684c87d95d656c1458ef04895a7f1cbd8e",
      "observedAt": "2026-09-17",
      "establishes": ["the rule patterns quoted per family below; consulted only as an external behavioral reference per AGENTS.md, no code reproduced"],
      "doesNotEstablish": ["provider issuance, liveness, or that a shape the rule accepts is the only shape a provider issues"]
    },
    "trufflehog-3.97.4": {
      "url": "https://github.com/trufflesecurity/trufflehog/tree/v3.97.4/pkg/detectors",
      "kind": "tool",
      "revision": "tag v3.97.4 = commit 363923b901c911a9164f50b6c423f47c15372b1c",
      "observedAt": "2026-09-17",
      "establishes": ["the detector patterns quoted per family below; consulted only as an external behavioral reference per AGENTS.md, no code reproduced"],
      "doesNotEstablish": ["provider issuance, liveness, or that a shape the pattern accepts is the only shape a provider issues"]
    },
    "trufflehog-3.97.5": {
      "url": "https://github.com/trufflesecurity/trufflehog/blob/v3.97.5/pkg/detectors/cloudflareapitoken/v2/cloudflareapitoken.go",
      "kind": "tool",
      "revision": "tag v3.97.5 = commit f714bf454f350590f4a24c3ddb1aef02c35bf5b6, the latest tagged release as of observedAt",
      "observedAt": "2026-09-20",
      "establishes": ["issue #486: the cloudflareapitoken v2 detector's `keyPat` is unchanged from v3.97.4 — `\\b(cf[ua]t_[a-zA-Z0-9]{40}[a-f0-9]{8})\\b` — the `cf[ua]t_` alternation still excludes `k`, so no `cfk_` rule exists in the latest tagged release either"],
      "doesNotEstablish": ["provider issuance, liveness, or that a shape the pattern accepts is the only shape a provider issues"]
    },
    "github-secret-scanning-patterns": {
      "url": "https://docs.github.com/en/code-security/secret-scanning/introduction/supported-secret-scanning-patterns",
      "kind": "corroboration",
      "revision": "page shows no version; observed 2026-09-17",
      "observedAt": "2026-09-17",
      "establishes": ["which token types each provider registers with GitHub's partner program (existence of a type, not its grammar): OpenAI API Key; DigitalOcean OAuth Token, Personal Access Token, Refresh Token, System Token; Docker Organization Access Token, Personal Access Token; Slack API Token; Hugging Face User Access Token, Organization API Token; Cloudflare Account API Token, Global User API Key, User API Token; Linear API Key, OAuth Access Token; Google OAuth Client ID (also lists `google_oauth_client_secret`), Google OAuth Access Token, Google OAuth Refresh Token (reobserved 2026-09-20 for issue #487); Vercel API Key, Vercel App Refresh Token (no push protection), Vercel App User Access Token, Vercel Integration Access Token, Vercel Personal Access Token, Vercel Support Access Token (reobserved 2026-09-20 for issue #516)"],
      "doesNotEstablish": ["any prefix, length or alphabet"]
    },
    "benchmark-common-formats": {
      "url": "redact-secret-benchmarks fixtures/generated/common-formats.mjs",
      "kind": "corroboration",
      "revision": "2f3d1c051e3a9f4c776835c939cd0b3c5d560533 with uncommitted modifications; corpus SHA-256 19ca47f5dbf21acc94c6712a51d6586c9fd1653b3da132172622c0e7c4b08fb8",
      "observedAt": "2026-09-17",
      "establishes": ["the synthetic construction of every positive and negative twin frozen in beta4-twin-baseline.json; the benchmark's own tier assignment per family"],
      "doesNotEstablish": ["anything about provider formats: it is downstream of the same tool sources"]
    },
    "google-cloud-api-key-docs": {
      "url": "https://cloud.google.com/docs/authentication/api-keys",
      "kind": "provider",
      "revision": "page shows no version; observed 2026-09-20",
      "observedAt": "2026-09-20",
      "establishes": ["a single example API key value, `AIzaSyDaGmWKa4JsXZ-HjGw7ISLn_3namBGewQe`, beginning with the `AIza` prefix and totaling 39 characters (35 after the prefix) — an example, not a stated grammar"],
      "doesNotEstablish": ["a stated prefix, length, or alphabet rule in prose; the 35-byte length is counted from the one example, not asserted by the page"]
    },
    "google-oauth2-docs": {
      "url": "https://developers.google.com/identity/protocols/oauth2",
      "kind": "provider",
      "revision": "page shows no version; observed 2026-09-20",
      "observedAt": "2026-09-20",
      "establishes": ["token size limits only: authorization codes ≤256 bytes, access tokens ≤2048 bytes, refresh tokens ≤512 bytes"],
      "doesNotEstablish": ["the `GOCSPX-`, `1//`, or `ya29.` prefix; any body length or alphabet; no example value of any of the three credentials appears on the page, and a size ceiling alone does not distinguish a real credential from an arbitrary string of the same size"]
    },
    "flare-redact-1.6.1": {
      "url": "https://github.com/flare-collection/flare-redact/blob/v1.6.1/spec/detectors.json",
      "kind": "tool",
      "revision": "tag v1.6.1 = commit c652ea7946028b70527069d7c282752b8a0ccee3",
      "observedAt": "2026-09-20",
      "establishes": ["the detector patterns quoted per family below; consulted only as an external behavioral reference per AGENTS.md, no code reproduced"],
      "doesNotEstablish": ["provider issuance, liveness, or that a shape the rule accepts is the only shape a provider issues"]
    },
    "notion-changelog": {
      "url": "https://developers.notion.com/page/changelog",
      "kind": "provider",
      "revision": "entry dated 2024-09-11 (effective 2024-09-25); page carries no per-entry version",
      "observedAt": "2026-09-20",
      "establishes": [
        "\"Starting September 25, 2024, newly generated Public API tokens will automatically use the `ntn_` prefix instead of the `secret_` prefix.\"",
        "\"Existing tokens with the `secret_` prefix remain functional with no required updates.\"",
        "\"We strongly advise against using regular expressions (regex) to identify or validate Notion Public API tokens... Instead, treat the token as an opaque string and use it as provided.\""
      ],
      "doesNotEstablish": [
        "the suffix length or alphabet of either prefix's body"
      ]
    },
    "notion-docs-create-token": {
      "url": "https://developers.notion.com/reference/create-a-token",
      "kind": "provider",
      "revision": "page shows no version or last-updated date",
      "observedAt": "2026-09-20",
      "establishes": [
        "how an integration token is generated and retrieved through the Notion UI"
      ],
      "doesNotEstablish": [
        "any token prefix, length, or alphabet; no example token value is shown"
      ]
    },
    "notion-docs-authorization": {
      "url": "https://developers.notion.com/guides/get-started/authorization",
      "kind": "provider",
      "revision": "page shows no version or last-updated date",
      "observedAt": "2026-09-20",
      "establishes": [
        "one example OAuth refresh token value, `nrt_4991090011501Ejc6Xn4sHguI7jZIN449mKe9PRhpMfNK`, confirming the `nrt_` prefix and showing a 13-digit run before its body"
      ],
      "doesNotEstablish": [
        "any `secret_`/`ntn_` integration-token detail; neither prefix appears on this page"
      ]
    },
    "vercel-changelog-token-formats": {
      "url": "https://vercel.com/changelog/new-token-formats-and-secret-scanning",
      "kind": "provider",
      "revision": "published 2026-02-09; page carries no separate version",
      "observedAt": "2026-09-20",
      "establishes": [
        "\"Each credential type now includes a prefix: `vcp` for Vercel personal access tokens, `vci` for Vercel integration tokens, `vca` for Vercel app access tokens, `vcr` for Vercel app refresh tokens, `vck` for Vercel API keys.\"",
        "detection for these prefixes \"is powered by GitHub secret scanning\""
      ],
      "doesNotEstablish": [
        "any body length or alphabet for any of the five prefixes"
      ]
    },
    "vercel-docs-access-tokens": {
      "url": "https://vercel.com/docs/accounts/access-tokens",
      "kind": "provider",
      "revision": "last_updated 2026-09-08",
      "observedAt": "2026-09-20",
      "establishes": [
        "\"Personal access tokens begin with the prefix `vcp_`\" (literal prose rule for the prefix)",
        "a masked example in a curl snippet, `vcp_xxxxxxxxxxxxxxxxxxxxxxxx` (24 literal `x` filler characters)"
      ],
      "doesNotEstablish": [
        "a real body length or alphabet: the masked example is a redaction placeholder in a code snippet, not a generated value, the same category this file already declines to treat as establishing a length (compare `families.slack-token`'s `xoxp-111-222-333-...` numeric placeholders)"
      ]
    },
    "vercel-docs-sign-in-tokens": {
      "url": "https://vercel.com/docs/sign-in-with-vercel/tokens",
      "kind": "provider",
      "revision": "last_updated 2026-03-30",
      "observedAt": "2026-09-20",
      "establishes": [
        "an \"Access Token example\" (`vca_` prefix) and a \"Refresh Token example\" (`vcr_` prefix), both carrying the identical 56-byte mixed-case alphanumeric body `BQuu9ChDu3n6Pfh6YQnCshpoYkWDSFKogLqmBtQ0tC8NAA5rXt340sjz` (no `-`/`_` in this instance)"
      ],
      "doesNotEstablish": [
        "a stated length or alphabet rule in prose; the two examples reuse the identical body string, so this is one documented instance, not two independent ones, for either prefix"
      ]
    },
    "vercel-docs-ai-gateway-api-keys": {
      "url": "https://vercel.com/docs/ai-gateway/authentication-and-byok/api-keys",
      "kind": "provider",
      "revision": "last_updated 2026-09-08",
      "observedAt": "2026-09-20",
      "establishes": [
        "confirms the `vck_` prefix via a compromised-secret report request body: `{ \"secret\": { \"api_key\": \"vck_...\" } }`"
      ],
      "doesNotEstablish": [
        "any body length or alphabet"
      ]
    },
    "vercel-docs-api-integrations": {
      "url": "https://vercel.com/docs/integrations/create-integration/vercel-api-integrations",
      "kind": "provider",
      "revision": "last_updated 2026-09-16 (five weeks after the 2026-02-09 prefix changelog)",
      "observedAt": "2026-09-20",
      "establishes": [
        "the OAuth `code`-exchange endpoint (`POST /v2/oauth/access_token`) example shows a request `client_secret=EOBPvZuBYAtb3SbYo8H1iWFP` (24 bytes, alphanumeric, no prefix) and a JSON response `\"access_token\": \"xEbuzM1ZAJ46afITQlYqH605\"` (24 bytes, alphanumeric, no prefix) — a real, currently-documented, unprefixed credential surface distinct from the five `vc*_`-prefixed classes, updated after the prefix rollout and still unprefixed"
      ],
      "doesNotEstablish": [
        "any prefix or shape for the dashboard-issued `vci_` \"Integration Access Token\" the changelog and GitHub's partner list separately name"
      ]
    },
    "vercel-rest-api-reference": {
      "url": "https://vercel.com/docs/rest-api",
      "kind": "provider",
      "revision": "last_updated 2026-09-21",
      "observedAt": "2026-09-20",
      "establishes": [
        "the page is purely an endpoint index (method/path/description tables per resource group, including an `authentication` section listing only endpoint rows)"
      ],
      "doesNotEstablish": [
        "any token prefix, length, or alphabet; no prose on the page discusses credential format at all"
      ]
    }
  },
  "families": [
    {
      "detector": "openai-token",
      "findingType": "openai_api_key",
      "implementation": "crates/secret-scan-core/src/detectors/openai.rs",
      "beta4Rule": "`sk-` then either `proj-`/`svcacct-` plus >=20 bytes of `[A-Za-z0-9_-]`, or (when the next four bytes are not `ant-`) >=20 bytes of `[A-Za-z0-9_-]`. No marker, no upper bound, no per-segment length.",
      "toolRules": {
        "gitleaks-8.30.1": "openai-api-key: `\\b(sk-(?:proj|svcacct|admin)-(?:[A-Za-z0-9_-]{74}|[A-Za-z0-9_-]{58})T3BlbkFJ(?:[A-Za-z0-9_-]{74}|[A-Za-z0-9_-]{58})\\b|sk-[a-zA-Z0-9]{20}T3BlbkFJ[a-zA-Z0-9]{20})` followed by a delimiter or end; keyword `t3blbkfj`",
        "trufflehog-3.97.4": "openai: `\\b(sk-(?:(?:proj|svcacct|service)-[A-Za-z0-9_-]+|[a-zA-Z0-9]+)T3BlbkFJ[A-Za-z0-9_-]+)\\b`; source comment: the marker is the base64 encoding of `OpenAI`; matches legacy, project and service-account keys (`svcacct-` or `service-`); does not match admin keys"
      },
      "variants": [
        {
          "id": "legacy",
          "support": "supported",
          "tier": "T2",
          "grammar": "sk-[A-Za-z0-9]{20}T3BlbkFJ[A-Za-z0-9]{20}",
          "segments": [
            {"name": "prefix", "literal": "sk-", "basis": "tool-agreement"},
            {"name": "left", "alphabet": "[A-Za-z0-9]", "length": 20, "basis": "single-tool (gitleaks fixes 20; trufflehog accepts any positive length); adopted as support-policy"},
            {"name": "marker", "literal": "T3BlbkFJ", "basis": "tool-agreement"},
            {"name": "right", "alphabet": "[A-Za-z0-9]", "length": 20, "basis": "single-tool (gitleaks fixes 20; trufflehog accepts any positive length); adopted as support-policy"}
          ],
          "sources": ["gitleaks-8.30.1", "trufflehog-3.97.4"],
          "evidenceGaps": ["no provider documentation was retrievable (openai-help-api-key, openai-platform-authentication); tier T2"]
        },
        {
          "id": "proj",
          "support": "supported",
          "tier": "T2",
          "grammar": "sk-proj-(?:[A-Za-z0-9_-]{74}|[A-Za-z0-9_-]{58})T3BlbkFJ(?:[A-Za-z0-9_-]{74}|[A-Za-z0-9_-]{58})",
          "segments": [
            {"name": "prefix", "literal": "sk-proj-", "basis": "tool-agreement"},
            {"name": "left", "alphabet": "[A-Za-z0-9_-]", "length": [58, 74], "basis": "single-tool (gitleaks names exactly these two widths; trufflehog accepts any positive length); adopted as support-policy"},
            {"name": "marker", "literal": "T3BlbkFJ", "basis": "tool-agreement"},
            {"name": "right", "alphabet": "[A-Za-z0-9_-]", "length": [58, 74], "basis": "single-tool; adopted as support-policy"}
          ],
          "sources": ["gitleaks-8.30.1", "trufflehog-3.97.4"]
        },
        {
          "id": "svcacct",
          "support": "supported",
          "tier": "T2",
          "grammar": "sk-svcacct-(?:[A-Za-z0-9_-]{74}|[A-Za-z0-9_-]{58})T3BlbkFJ(?:[A-Za-z0-9_-]{74}|[A-Za-z0-9_-]{58})",
          "segments": [
            {"name": "prefix", "literal": "sk-svcacct-", "basis": "tool-agreement"},
            {"name": "left", "alphabet": "[A-Za-z0-9_-]", "length": [58, 74], "basis": "single-tool; adopted as support-policy"},
            {"name": "marker", "literal": "T3BlbkFJ", "basis": "tool-agreement"},
            {"name": "right", "alphabet": "[A-Za-z0-9_-]", "length": [58, 74], "basis": "single-tool; adopted as support-policy"}
          ],
          "sources": ["gitleaks-8.30.1", "trufflehog-3.97.4"]
        }
      ],
      "pending": [
        {"id": "admin", "prefix": "sk-admin-", "tier": "T0", "reason": "gitleaks registers it with the proj/svcacct widths; trufflehog explicitly excludes admin keys; no provider documentation. Unresolved tool disagreement; not supported by the default rule until a second independent source establishes the shape. Under beta.4 this prefix fell through to the bare branch and matched as a legacy key; under the contract it is a recorded false negative."},
        {"id": "service", "prefix": "sk-service-", "tier": "T0", "reason": "trufflehog alone registers `service-` as a service-account spelling; gitleaks does not; no provider documentation. Not supported until independently established."}
      ],
      "excluded": [
        {"id": "anthropic-namespace", "prefix": "sk-ant-", "basis": "ownership", "reason": "Owned by `anthropic-token`; the legacy grammar's `[A-Za-z0-9]` left segment cannot contain the `-` of `ant-`, so the exclusion is structural under the contract and the explicit `ant-` check stays as a documented guard."},
        {"id": "marker-less-body", "basis": "support-policy", "reason": "Any `sk-`-prefixed value without the `T3BlbkFJ` marker is intentionally unsupported. Both tool sources require the marker; the benchmark twin `openai-token-legacy-*-twin` mutates it. This is the property that removes the legacy false alarm, and it also stops matching every existing synthetic positive in this repository that has no marker (listed in corpus-audit.json)."}
      ],
      "conflicts": [
        {"property": "segment lengths", "positions": {"gitleaks-8.30.1": "exactly 74 or 58 per segment (proj/svcacct/admin); exactly 20/20 (legacy)", "trufflehog-3.97.4": "one or more bytes per segment"}, "resolution": "Adopt gitleaks's enumerated widths as an explicit support-policy choice, because it is the only source that states a width and the measured twin differs only in width (73 vs 74). This is not converted into a provider claim: a provider-issued key with another width is a recorded intentionally-unsupported form, and the ADR flags it for revisiting if provider documentation appears. The marker requirement, on which both tools agree, is the evidence-backed part of the rule."},
        {"property": "prefix set", "positions": {"gitleaks-8.30.1": "proj, svcacct, admin", "trufflehog-3.97.4": "proj, svcacct, service; explicitly not admin"}, "resolution": "Support the intersection (proj, svcacct) plus legacy; record admin and service as pending."}
      ],
      "mutations": [
        {"mutationKind": "boundary", "mutation": "internal marker: T3BlbkFK vs contracted T3BlbkFJ", "fixtureIds": ["openai-token-legacy-plain-twin", "openai-token-legacy-unicode-crlf-twin"], "distinguishingProperty": "marker literal", "basis": "tool-agreement", "disposition": "retained", "reason": "Both tool sources require the literal marker; silence follows from construction."},
        {"mutationKind": "length", "mutation": "length: 73-character left segment vs contracted 74", "fixtureIds": ["openai-token-proj-plain-twin", "openai-token-proj-unicode-crlf-twin", "openai-token-svcacct-plain-twin", "openai-token-svcacct-unicode-crlf-twin"], "distinguishingProperty": "left segment width in {58, 74}", "basis": "single-tool; adopted as support-policy", "disposition": "retained", "reason": "Retained as a must-not-flag/T2 negative under the frozen contract. The width itself rests on one tool source, so the contract records that a real key of width 73 would be an intentionally unsupported form; the twin's classification is unchanged."}
      ]
    },
    {
      "detector": "digitalocean-token",
      "findingType": "digitalocean_token",
      "implementation": "crates/secret-scan-core/src/detectors/additional_providers.rs (DIGITALOCEAN)",
      "beta4Rule": "`dop_v1_`, `doo_v1_` or `dor_v1_` plus >=20 bytes of `[A-Za-z0-9_-]`.",
      "toolRules": {
        "gitleaks-8.30.1": "digitalocean-pat `\\b(dop_v1_[a-f0-9]{64})`, digitalocean-access-token `\\b(doo_v1_[a-f0-9]{64})`, digitalocean-refresh-token `(?i)\\b(dor_v1_[a-f0-9]{64})` (the refresh rule alone is case-insensitive), each followed by a delimiter or end",
        "trufflehog-3.97.4": "digitaloceanv2: `\\b((?:dop|doo|dor)_v1_[a-f0-9]{64})\\b`"
      },
      "variants": [
        {"id": "dop", "support": "supported", "tier": "T1", "grammar": "dop_v1_[0-9a-f]{64}", "segments": [{"name": "prefix", "literal": "dop_v1_", "basis": "provider"}, {"name": "body", "alphabet": "[0-9a-f]", "length": 64, "basis": "length: provider examples (digitalocean-oauth-reference show 64-character bodies for the sibling variants) and tool-agreement; alphabet: tool-agreement (lowercase hex)"}], "sources": ["digitalocean-release-notes", "digitalocean-oauth-reference", "gitleaks-8.30.1", "trufflehog-3.97.4"]},
        {"id": "doo", "support": "supported", "tier": "T1", "grammar": "doo_v1_[0-9a-f]{64}", "segments": [{"name": "prefix", "literal": "doo_v1_", "basis": "provider"}, {"name": "body", "alphabet": "[0-9a-f]", "length": 64, "basis": "length: provider example and tool-agreement; alphabet: tool-agreement"}], "sources": ["digitalocean-release-notes", "digitalocean-oauth-reference", "gitleaks-8.30.1", "trufflehog-3.97.4"]},
        {"id": "dor", "support": "supported", "tier": "T1", "grammar": "dor_v1_[0-9a-f]{64}", "segments": [{"name": "prefix", "literal": "dor_v1_", "basis": "provider"}, {"name": "body", "alphabet": "[0-9a-f]", "length": 64, "basis": "length: provider example and tool-agreement; alphabet: tool-agreement"}], "sources": ["digitalocean-release-notes", "digitalocean-oauth-reference", "gitleaks-8.30.1", "trufflehog-3.97.4"]}
      ],
      "pending": [
        {"id": "system-token", "tier": "T0", "reason": "GitHub's partner list names a `DigitalOcean System Token`; no consulted source shows its shape."},
        {"id": "future-version", "prefix": "dop_v2_ and siblings", "tier": "T0", "reason": "Already recorded in the module documentation as an intentional false negative until the shape is confirmed."}
      ],
      "excluded": [
        {"id": "uppercase-hex-or-mixed-case", "basis": "support-policy", "reason": "gitleaks's refresh-token rule is case-insensitive, its other two rules and trufflehog are not, and the provider examples are placeholders. The contract adopts lowercase hex; an uppercase or mixed-case body (including an uppercase prefix, which the existing `digitalocean-boundary-case-sensitivity` fixture already rejects) is intentionally unsupported."}
      ],
      "conflicts": [
        {"property": "body alphabet", "positions": {"digitalocean-oauth-reference": "example bodies contain non-hex placeholder characters", "gitleaks-8.30.1": "lowercase hex (dor rule case-insensitive)", "trufflehog-3.97.4": "lowercase hex"}, "resolution": "The provider examples establish length (64) but not alphabet, because they are visibly placeholders. Alphabet comes from tool agreement (lowercase hex). The single case-insensitive gitleaks rule is treated as a tool artifact, not adopted."}
      ],
      "mutations": [
        {"mutationKind": "length", "mutation": "length: 63 vs contracted 64", "fixtureIds": ["digitalocean-token-dop-plain-twin", "digitalocean-token-dop-unicode-crlf-twin", "digitalocean-token-doo-plain-twin", "digitalocean-token-doo-unicode-crlf-twin", "digitalocean-token-dor-plain-twin", "digitalocean-token-dor-unicode-crlf-twin"], "distinguishingProperty": "body length exactly 64", "basis": "provider examples plus tool-agreement", "disposition": "retained", "reason": "Every consulted source that states a length states 64; silence follows from construction."}
      ]
    },
    {
      "detector": "docker-token",
      "findingType": "docker_token",
      "implementation": "crates/secret-scan-core/src/detectors/additional_providers.rs (DOCKER)",
      "beta4Rule": "`dckr_pat_` or `dckr_oat_` plus >=20 bytes of `[A-Za-z0-9_-]`; both segment names share one rule.",
      "toolRules": {
        "gitleaks-8.30.1": "no Docker Hub access-token rule exists in this revision",
        "trufflehog-3.97.4": "dockerhub v2: `\\b(dckr_pat_[a-zA-Z0-9_-]{27}|dckr_oat_[a-zA-Z0-9_-]{32})(?:[^a-zA-Z0-9_-]|\\z)`; source comment: the scanner checks PATs and OATs, not passwords"
      },
      "variants": [
        {"id": "pat", "support": "supported", "tier": "T2", "grammar": "dckr_pat_[A-Za-z0-9_-]{27}", "segments": [{"name": "prefix", "literal": "dckr_pat_", "basis": "single-tool; existence of the type corroborated by github-secret-scanning-patterns"}, {"name": "body", "alphabet": "[A-Za-z0-9_-]", "length": 27, "basis": "single-tool; adopted as support-policy"}], "sources": ["trufflehog-3.97.4", "github-secret-scanning-patterns", "docker-docs-pat"]},
        {"id": "oat", "support": "supported", "tier": "T2", "grammar": "dckr_oat_[A-Za-z0-9_-]{32}", "segments": [{"name": "prefix", "literal": "dckr_oat_", "basis": "single-tool; existence of the type corroborated by github-secret-scanning-patterns"}, {"name": "body", "alphabet": "[A-Za-z0-9_-]", "length": 32, "basis": "single-tool; adopted as support-policy"}], "sources": ["trufflehog-3.97.4", "github-secret-scanning-patterns", "docker-docs-oat", "docker-ai-governance-api"]},
        {"id": "oat-provider-example-width", "support": "supported", "tier": "T2", "grammar": "dckr_oat_[A-Za-z0-9_-]{27}", "segments": [{"name": "prefix", "literal": "dckr_oat_", "basis": "provider (docs.docker.com AI Governance API `bearerAuth` Format column; issue #647)"}, {"name": "body", "alphabet": "[A-Za-z0-9_-]", "length": 27, "basis": "provider example (docs.docker.com Hub API spec); alphabet kept at the OAT support-policy alphabet"}], "sources": ["docker-hub-api-spec", "docker-ai-governance-api"]}
      ],
      "pending": [],
      "excluded": [
        {"id": "unprefixed-registry-password", "basis": "evidence", "reason": "Docker Hub passwords carry no distinguishing prefix; already out of scope in the module documentation and in the trufflehog source comment."},
        {"id": "shared-minimum", "basis": "support-policy", "reason": "PAT and OAT are validated separately at their own exact lengths; the beta.4 shared 20-byte minimum is withdrawn."}
      ],
      "conflicts": [
        {"property": "exact lengths 27 and 32", "positions": {"trufflehog-3.97.4": "exact 27 (PAT) and 32 (OAT)", "gitleaks-8.30.1": "no rule", "docker-docs-pat, docker-docs-oat": "no grammar published", "docker-hub-api-spec": "one OAT example with a 27-character body"}, "resolution": "No disagreement exists, but only one source states a length. The contract adopts it as a support-policy choice and records that a provider-issued token of another length would be an intentionally unsupported form until a second source establishes the width. The measured twins (26 and 31) are retained because they differ from the only stated width, not because the width is provider-proven. Issue #566 (2026-09-21) re-checked both docs.docker.com PAT and OAT pages plus GitHub's supported-secret-scanning-patterns page and the Docker blog for a documented `dckr_pat_` body width; none publish one, so the question 'is a 32-byte `dckr_pat_` body issued' (the `detector-coverage--docker-token-shape-1-*` fixtures' shape) remains unresolved by any source and is not adopted -- the 27-byte contract is unchanged and the fixtures are dispositioned as a benchmarks-side generator artifact, not a coverage gap (see `docs/audits/evidence/566/README.md`). Issue #708 (2026-09-23): Docker's own Hub API spec shows an Organization Access Token example with a 27-character body, which contradicts the trufflehog-only 32, and every other Docker-authored artefact that shows a width shows 27. The OAT branch now accepts exactly 27 (`oat-provider-example-width`) or exactly 32 (`oat`), and every other width is still rejected. Only a fresh OAT measurement (#647) could retire either width."}
      ],
      "mutations": [
        {"mutationKind": "length", "mutation": "length: 26 vs contracted 27", "fixtureIds": ["docker-token-pat-plain-twin", "docker-token-pat-unicode-crlf-twin"], "distinguishingProperty": "PAT body length exactly 27", "basis": "single-tool; adopted as support-policy", "disposition": "retained", "reason": "Retained as must-not-flag/T2; the tier already records that the property is tool-corroborated, not provider-documented."},
        {"mutationKind": "length", "mutation": "length: 31 vs contracted 32", "fixtureIds": ["docker-token-oat-plain-twin", "docker-token-oat-unicode-crlf-twin"], "distinguishingProperty": "OAT body length exactly 32", "basis": "single-tool; adopted as support-policy", "disposition": "retained", "reason": "As above."}
      ]
    },
    {
      "detector": "slack-token",
      "findingType": "slack_token",
      "implementation": "crates/secret-scan-core/src/detectors/additional_providers.rs (SLACK)",
      "beta4Rule": "`xoxb-`, `xoxp-`, `xapp-`, `xwfp-`, `xoxe-`, `xoxe.xoxb-` or `xoxe.xoxp-` plus >=20 bytes of `[A-Za-z0-9_-]`; no section grammar.",
      "toolRules": {
        "gitleaks-8.30.1": "slack-bot-token `xoxb-[0-9]{10,13}-[0-9]{10,13}[a-zA-Z0-9-]*` (no separator required before the tail); slack-legacy-bot-token `xoxb-[0-9]{8,14}-[a-zA-Z0-9]{18,26}`; slack-user-token `xox[pe](?:-[0-9]{10,13}){3}-[a-zA-Z0-9-]{28,34}`; slack-app-token `(?i)xapp-\\d-[A-Z0-9]+-\\d+-[a-z0-9]+`; slack-config-access-token `(?i)xoxe.xox[bp]-\\d-[A-Z0-9]{163,166}`; slack-config-refresh-token `(?i)xoxe-\\d-[A-Z0-9]{146}`; slack-legacy-workspace-token `xox[ar]-(?:\\d-)?[0-9a-zA-Z]{8,48}`; slack-legacy-token `xox[os]-\\d+-\\d+-\\d+-[a-fA-F\\d]+`",
        "trufflehog-3.97.4": "slack: `xoxb\\-[0-9]{10,13}\\-[0-9]{10,13}[a-zA-Z0-9\\-]*` and the same shape for `xoxp`, `xoxa`, `xoxr` (no separator required before the tail)"
      },
      "variants": [
        {
          "id": "bot",
          "support": "supported",
          "tier": "T1",
          "grammar": "xoxb-[0-9]{10,13}-[0-9]{10,13}-[A-Za-z0-9]{18,}",
          "segments": [
            {"name": "prefix", "literal": "xoxb-", "basis": "provider"},
            {"name": "section-1", "alphabet": "[0-9]", "length": [10, 13], "basis": "tool-agreement (width); provider establishes only that sections are `-`-separated"},
            {"name": "separator", "literal": "-", "basis": "provider"},
            {"name": "section-2", "alphabet": "[0-9]", "length": [10, 13], "basis": "tool-agreement (width)"},
            {"name": "separator", "literal": "-", "basis": "provider: \"The final section is the secret\" separated by `-`; neither tool requires this separator, which is why all three baseline scanners flag the twin"},
            {"name": "secret", "alphabet": "[A-Za-z0-9]", "length": "18 or more", "basis": "support-policy floor: the smallest bot-secret width any consulted tool accepts (gitleaks legacy-bot 18); the provider documents no bot secret length; alphabet is the provider example's alphanumeric shape, without the `-` the tool tails allow because the provider defines the secret as the final section"}
          ],
          "sources": ["slack-docs-tokens", "gitleaks-8.30.1", "trufflehog-3.97.4"]
        },
        {"id": "user", "support": "supported", "tier": "T1", "grammar": "xoxp-[0-9]+-[0-9]+-[0-9]+-[A-Za-z0-9]+", "segments": [{"name": "prefix", "literal": "xoxp-", "basis": "provider"}, {"name": "section-1", "alphabet": "[0-9]", "length": "1 or more", "basis": "provider establishes only that sections are `-`-separated; width open (issue #730), tools' 10-13 is not a negative rule"}, {"name": "separator", "literal": "-", "basis": "provider"}, {"name": "section-2", "alphabet": "[0-9]", "length": "1 or more", "basis": "width open (issue #730)"}, {"name": "separator", "literal": "-", "basis": "provider"}, {"name": "section-3", "alphabet": "[0-9]", "length": "1 or more", "basis": "provider (example `xoxp-111-222-333-...`, a three-numeric-section shape); width open (issue #730)"}, {"name": "separator", "literal": "-", "basis": "provider: \"The final section is the secret\""}, {"name": "secret", "alphabet": "[A-Za-z0-9]", "length": "1 or more", "basis": "width open (issue #730): the provider's own example secret is 32 bytes (reobserved 2026-09-20) but states no length rule, and documents pre-2016 6/10-byte secrets as still rotatable; alphabet excludes the `-` gitleaks' tail alphabet allows, the same choice the bot secret already makes"}], "sources": ["slack-docs-tokens", "gitleaks-8.30.1", "trufflehog-3.97.4"], "candidateGrammar": null, "candidateBasis": "Issue #730 adopts the section anatomy frozen by issue #726 (docs/audits/evidence/726/README.md) and emits it as its own `slack_user_token` finding type. It replaces #512's tool-derived 10-13 digit sections and 28-byte secret floor, which the provider never stated and which rejected documented pre-2016 secrets."},
        {"id": "app-level", "support": "supported", "tier": "T2", "grammar": "xapp-[0-9]+-[A-Za-z0-9]+-[0-9]+-[A-Za-z0-9]+", "segments": [{"name": "prefix", "literal": "xapp-", "basis": "provider"}, {"name": "app-version", "alphabet": "[0-9]", "length": "1 or more", "basis": "section order: provisional provider-code/tool evidence (issue #726 freeze); width open because sources disagree"}, {"name": "app-id", "alphabet": "[A-Za-z0-9]", "length": "1 or more", "basis": "section order: provisional provider-code/tool evidence (issue #726 freeze); width and alphabet open"}, {"name": "sequence", "alphabet": "[0-9]", "length": "1 or more", "basis": "section order: provisional provider-code/tool evidence (issue #726 freeze); width open"}, {"name": "secret", "alphabet": "[A-Za-z0-9]", "length": "1 or more", "basis": "section order: provisional provider-code/tool evidence (issue #726 freeze); width and alphabet open"}], "sources": ["slack-docs-tokens", "gitleaks-8.30.1"], "candidateGrammar": null, "candidateBasis": "Issue #729 adopts the section anatomy frozen by issue #726 (docs/audits/evidence/726/README.md) as the app-level contract and emits it as its own `slack_app_level_token` finding type. The beta.4 opaque `xapp-[A-Za-z0-9_-]{20,}` guard is retired; section widths stay open because tool sources disagree, and the anatomy stays empirical (T2) until the benchmarks#177 safe-observation gates pass."},
        {"id": "workflow", "support": "supported-interim", "tier": "T0", "grammar": "xwfp-[A-Za-z0-9_-]{20,}", "segments": [{"name": "prefix", "literal": "xwfp-", "basis": "provider"}, {"name": "body", "alphabet": "[A-Za-z0-9_-]", "length": "20 or more", "basis": "support-policy: beta.4 rule retained unchanged as an interim guard; issue #570 restores this open floor after issue #551 briefly narrowed it to an exact length, which stopped matching every realistic-length token in this family (`RunLength::AtLeast` against the identical alphabet the boundary itself checks let a directly-glued wider identifier through, and #551 fixed that by conflating the floor with an unreviewed maximum instead of fixing the boundary check itself); `RunLength::OpenFloor` is the actual fix, capping the run back to the floor only when a dash or underscore starts right there"}], "sources": ["slack-docs-tokens"], "candidateGrammar": null, "candidateBasis": "No tool source registers `xwfp-`; the benchmark keeps its sample pending (T0). Issue #512 reverified 2026-09-20: the live token page states only the bare `xwfp-` prefix and the 15-minute/function-step expiry, no structural detail; still zero tool corroboration; not promoted."},
        {"id": "refresh", "support": "supported", "tier": "T1", "grammar": "xoxe-[0-9]-[A-Za-z0-9_-]{20,}", "segments": [{"name": "prefix", "literal": "xoxe-", "basis": "provider (slack-docs-rotation example `xoxe-1-...`)"}, {"name": "version", "alphabet": "[0-9]", "length": 1, "basis": "provider (every rotation example shows exactly one digit, reobserved 2026-09-20); gitleaks' slack-config-refresh-token rule encodes the same single digit"}, {"name": "separator", "literal": "-", "basis": "provider"}, {"name": "body", "alphabet": "[A-Za-z0-9_-]", "length": "20 or more", "basis": "support-policy: beta.4 rule retained unchanged as an interim guard; issue #570 restores this open floor after issue #551 briefly narrowed it to an exact length, which stopped matching every realistic-length token in this family (`RunLength::AtLeast` against the identical alphabet the boundary itself checks let a directly-glued wider identifier through, and #551 fixed that by conflating the floor with an unreviewed maximum instead of fixing the boundary check itself); `RunLength::OpenFloor` is the actual fix, capping the run back to the floor only when a dash or underscore starts right there; gitleaks' own 146-character exact width is not adopted"}], "sources": ["slack-docs-rotation", "gitleaks-8.30.1"]},
        {"id": "rotating-bot", "support": "supported", "tier": "T1", "grammar": "xoxe\\.xoxb-[0-9]-[A-Za-z0-9_-]{20,}", "segments": [{"name": "prefix", "literal": "xoxe.xoxb-", "basis": "provider (slack-docs-rotation example `xoxe.xoxb-1-...`)"}, {"name": "version", "alphabet": "[0-9]", "length": 1, "basis": "provider, reobserved 2026-09-20; gitleaks' slack-config-access-token rule encodes the same single digit"}, {"name": "separator", "literal": "-", "basis": "provider"}, {"name": "body", "alphabet": "[A-Za-z0-9_-]", "length": "20 or more", "basis": "support-policy: beta.4 rule retained unchanged as an interim guard; issue #570 restores this open floor after issue #551 briefly narrowed it to an exact length, which stopped matching every realistic-length token in this family (`RunLength::AtLeast` against the identical alphabet the boundary itself checks let a directly-glued wider identifier through, and #551 fixed that by conflating the floor with an unreviewed maximum instead of fixing the boundary check itself); `RunLength::OpenFloor` is the actual fix, capping the run back to the floor only when a dash or underscore starts right there; gitleaks' own 163-166-character exact width is not adopted"}], "sources": ["slack-docs-rotation", "gitleaks-8.30.1"]},
        {"id": "rotating-user", "support": "supported", "tier": "T1", "grammar": "xoxe\\.xoxp-[0-9]-[A-Za-z0-9_-]{20,}", "segments": [{"name": "prefix", "literal": "xoxe.xoxp-", "basis": "provider (slack-docs-rotation example `xoxe.xoxp-1-1234-...`)"}, {"name": "version", "alphabet": "[0-9]", "length": 1, "basis": "provider, reobserved 2026-09-20; gitleaks' slack-config-access-token rule encodes the same single digit"}, {"name": "separator", "literal": "-", "basis": "provider"}, {"name": "body", "alphabet": "[A-Za-z0-9_-]", "length": "20 or more", "basis": "support-policy: beta.4 rule retained unchanged as an interim guard; issue #570 restores this open floor after issue #551 briefly narrowed it to an exact length, which stopped matching every realistic-length token in this family (`RunLength::AtLeast` against the identical alphabet the boundary itself checks let a directly-glued wider identifier through, and #551 fixed that by conflating the floor with an unreviewed maximum instead of fixing the boundary check itself); `RunLength::OpenFloor` is the actual fix, capping the run back to the floor only when a dash or underscore starts right there; the example's own extra `-1234-` section is a single uncorroborated instance and is not decomposed further, and gitleaks' own 163-166-character exact width is not adopted"}], "sources": ["slack-docs-rotation", "gitleaks-8.30.1"]}
      ],
      "pending": [
        {"id": "legacy-workspace-and-custom-integration", "prefix": "xoxa-, xoxr-, xoxs-, xoxo-", "tier": "T0", "reason": "The provider page lists legacy workspace apps as fully deprecated since August 2021 and legacy custom-integration tokens as no longer recommended, without prefixes; trufflehog registers `xoxa`/`xoxr`, gitleaks registers `xoxa`/`xoxr`/`xoxs`/`xoxo`. Not supported by beta.4 and not added by this contract."}
      ],
      "excluded": [
        {"id": "bot-without-secret-separator", "basis": "provider", "reason": "A value whose second numeric section runs straight into the secret has no `-` before its final section and so has no secret section under the provider's own structure. Evidence-backed exclusion; the tools' `[a-zA-Z0-9-]*` tails accept it, which is why competitor agreement is not the negative oracle here."},
        {"id": "bot-widths-applied-to-other-prefixes", "basis": "support-policy", "reason": "The bot section grammar applies to `xoxb-` only. Every other prefix keeps its own interim guard above."},
        {"id": "pre-2016-short-user-secrets", "basis": "support-policy", "reason": "Issue #512 adopted the user grammar's 28-byte secret floor; a 6- or 10-character pre-2016 secret is now an intentional false negative, the accepted cost of the same floor policy the bot contract already carries."}
      ],
      "conflicts": [
        {"property": "separator before the bot secret", "positions": {"slack-docs-tokens": "sections are `-`-separated and the final section is the secret", "gitleaks-8.30.1": "bot rule accepts `[a-zA-Z0-9-]*` directly after the second numeric section", "trufflehog-3.97.4": "same as gitleaks"}, "resolution": "Provider documentation comes first: the separator is required. Tool sources corroborate the numeric section widths only."},
        {"property": "bot secret length", "positions": {"slack-docs-tokens": "not stated for bot tokens (32 stated for user tokens)", "gitleaks-8.30.1": "legacy-bot 18–26 alphanumeric; modern bot unbounded", "trufflehog-3.97.4": "unbounded"}, "resolution": "No exact length is imposed. An 18-byte alphanumeric floor is a support-policy choice taken from the smallest width any tool accepts; it is not a provider claim and the ADR says so."},
        {"property": "tier of `user`/`refresh`/`rotating-bot`/`rotating-user` (issue #512)", "positions": {"367-audit-variants": "recorded T3 ('beta.4 rule retained unchanged as an interim guard'), each with a documented but unadopted `candidateGrammar`", "512-review": "provider structure plus tool-corroborated (`user`) or provider-example-plus-single-tool (`refresh`/`rotating-bot`/`rotating-user`) widths, reviewed against the current live pages on 2026-09-20"}, "resolution": "`user`'s three-numeric-section-plus-secret shape is provider-documented (example `xoxp-111-222-333-...`) with gitleaks corroborating every section width; re-tiered T3 to T1 and adopted, mirroring the bot contract's own bar. `refresh`/`rotating-bot`/`rotating-user` each gained a provider-documented single-digit version section (every rotation example shows exactly one digit) that gitleaks' config-access/-refresh-token rules corroborate; only that section is promoted (T1), and each family's body stays the unchanged T3 opaque interim guard because no source corroborates a body width or alphabet. `app-level` and `workflow` were reviewed and left unpromoted: `app-level`'s only candidate structure remains a single uncorroborated tool source and `workflow` has no tool source at all, unchanged since #367."}
      ],
      "mutations": [
        {"mutationKind": "boundary", "mutation": "boundary: missing dash before the secret section", "fixtureIds": ["slack-token-bot-plain-twin", "slack-token-bot-unicode-crlf-twin"], "distinguishingProperty": "`-` before the final (secret) section", "basis": "provider", "disposition": "retained", "reason": "Evidence-backed: the provider defines the secret as the final `-`-separated section."}
      ]
    },
    {
      "detector": "huggingface-token",
      "findingType": "huggingface_token",
      "implementation": "crates/secret-scan-core/src/detectors/additional_providers.rs (HUGGING_FACE)",
      "beta4Rule": "`hf_` plus >=20 bytes of `[A-Za-z0-9_-]`.",
      "toolRules": {
        "gitleaks-8.30.1": "huggingface-access-token `\\b(hf_(?i:[a-z]{34}))` followed by a delimiter or end (letters only, either case); huggingface-organization-api-token `\\b(api_org_(?i:[a-z]{34}))`",
        "trufflehog-3.97.4": "huggingface: `\\b(?:hf_|api_org_)[a-zA-Z0-9]{34}\\b`"
      },
      "variants": [
        {"id": "user", "support": "supported", "tier": "T2", "grammar": "hf_[A-Za-z0-9]{34}", "segments": [{"name": "prefix", "literal": "hf_", "basis": "provider (placeholder `hf_...`) and tool-agreement"}, {"name": "body", "alphabet": "[A-Za-z0-9]", "length": 34, "basis": "length: tool-agreement; alphabet: support-policy (the union of the two tool alphabets; see conflicts)"}], "sources": ["huggingface-docs-tokens", "gitleaks-8.30.1", "trufflehog-3.97.4", "github-secret-scanning-patterns"]},
        {"id": "organization-token", "support": "supported", "tier": "T2", "grammar": "api_org_[A-Za-z0-9]{34}", "segments": [{"name": "prefix", "literal": "api_org_", "basis": "tool-agreement; existence of the type corroborated by github-secret-scanning-patterns"}, {"name": "body", "alphabet": "[A-Za-z0-9]", "length": 34, "basis": "length: tool-agreement; alphabet: support-policy (the union of the two tool alphabets, identically to `hf_`; see conflicts)"}], "sources": ["gitleaks-8.30.1", "trufflehog-3.97.4", "github-secret-scanning-patterns"]}
      ],
      "pending": [
        {"id": "digit-bearing-body", "tier": "T0", "reason": "Accepted by the default rule as a support-policy choice (rejecting digits would follow one tool only and would create a false negative if the other tool is right). The benchmark keeps its digit-body samples pending (T0) and they stay unscored until independently reviewed; accepting them is not claimed as evidence. Applies identically to `hf_` and `api_org_`."}
      ],
      "excluded": [
        {"id": "underscore-or-dash-in-body", "basis": "evidence", "reason": "Neither tool alphabet contains `_` or `-`; the beta.4 acceptance of them was a shared-rule artifact. Evidence-backed exclusion. Applies identically to `hf_` and `api_org_`."},
        {"id": "dash-prefix", "prefix": "hf-", "basis": "evidence", "reason": "Already rejected by the existing `huggingface-negative-lookalike` fixture."}
      ],
      "conflicts": [
        {"property": "body alphabet", "positions": {"gitleaks-8.30.1": "letters only, either case", "trufflehog-3.97.4": "letters and digits", "huggingface-docs-tokens": "no grammar published"}, "resolution": "Not resolved by guesswork into the letters-only intersection. The default rule accepts the union `[A-Za-z0-9]` as a support-policy choice that favors not missing a real token, and scored evidence stays on letters-only positives; digit-body samples remain pending (T0). The exclusion of `_`/`-`, on which both tools agree, is evidence-backed. Applies identically to `hf_` and `api_org_`: gitleaks registers `api_org_` as its own letters-only `huggingface-organization-api-token` rule, and trufflehog matches `api_org_` under the same `(?:hf_|api_org_)[a-zA-Z0-9]{34}` rule it uses for `hf_`."},
        {"property": "tier of `api_org_` (issue #485)", "positions": {"367-audit-pending-entry": "recorded T0 ('Both tools register `api_org_` with a 34-character body and GitHub lists an Organization API Token, but beta.4 does not match this prefix and adding a variant is a coverage change, not a precision fix. Recorded as a known false negative for a separate issue.')", "hf_-user-variant-precedent": "T2, on a body-length basis of `tool-agreement` and a body-alphabet basis of `support-policy` -- no provider documentation of either property"}, "resolution": "The #367 pending entry deferred `api_org_` on scope (#367 was a precision fix, not a coverage change), not because its evidence was weaker than `hf_`'s. Measured against the same T2 definition ('no provider documentation for the property; pinned tool sources corroborate it'), `api_org_` has the identical basis already accepted for `hf_`'s own tier: gitleaks and trufflehog both register the 34-byte body (tool-agreement), and the two tools' letters-only-vs-union alphabet disagreement is the same disagreement already resolved for `hf_` as a support-policy union. `huggingface-docs-tokens` establishes neither `api_org_`'s nor `hf_`'s length or alphabet -- only `hf_`'s bare prefix as a placeholder -- so the absence of provider text for `api_org_`'s prefix does not distinguish it from `hf_` at the property level the tier scores. Re-tiered to T2 and adopted as a second shape over `hf_`'s identical body grammar; `hf_`'s own T2 tier and contract are unchanged by this resolution."}
      ],
      "mutations": [
        {"mutationKind": "length", "mutation": "length: 33 vs contracted 34", "fixtureIds": ["huggingface-token-user-plain-twin", "huggingface-token-user-unicode-crlf-twin"], "distinguishingProperty": "body length exactly 34", "basis": "tool-agreement", "disposition": "retained", "reason": "Both tools state 34; silence follows from construction."},
        {"mutationKind": "length", "mutation": "length: 33 vs contracted 34", "fixtureIds": ["huggingface-token-organization-plain-twin", "huggingface-token-organization-unicode-crlf-twin"], "distinguishingProperty": "body length exactly 34", "basis": "tool-agreement", "disposition": "retained", "reason": "Issue #485: the `api_org_` twin of the `hf_` mutation above. Both tools state 34 for `api_org_` as well; silence follows from construction."}
      ]
    },
    {
      "detector": "cloudflare-token",
      "findingType": "cloudflare_api_token",
      "implementation": "crates/secret-scan-core/src/detectors/cloudflare.rs (CLOUDFLARE)",
      "beta4Rule": "`cfut_` plus >=20 bytes of `[A-Za-z0-9_-]`.",
      "toolRules": {
        "gitleaks-8.30.1": "no `cfut_` rule; cloudflare-api-key is a context-gated legacy `[a-z0-9_-]{40}` rule and cloudflare-global-api-key a context-gated `[a-f0-9]{37}` rule",
        "trufflehog-3.97.4": "cloudflareapitoken v2: `\\b(cf[ua]t_[a-zA-Z0-9]{40}[a-f0-9]{8})\\b`"
      },
      "variants": [
        {"id": "user", "support": "supported", "tier": "T1", "grammar": "cfut_[A-Za-z0-9]{40}[0-9a-f]{8}", "segments": [{"name": "prefix", "literal": "cfut_", "basis": "provider"}, {"name": "body", "alphabet": "[A-Za-z0-9]", "length": 40, "basis": "length: provider; alphabet: provider's description of the pre-2026 token as 40-character alphanumeric plus single-tool"}, {"name": "checksum", "alphabet": "[0-9a-f]", "length": 8, "basis": "existence: provider; width and alphabet: single-tool; adopted as support-policy. Lexical only: no checksum algorithm is computed or claimed."}], "sources": ["cloudflare-token-formats", "cloudflare-create-token", "trufflehog-3.97.4", "github-secret-scanning-patterns"]},
        {"id": "account-token", "support": "supported", "tier": "T1", "grammar": "cfat_[A-Za-z0-9]{40}[0-9a-f]{8}", "segments": [{"name": "prefix", "literal": "cfat_", "basis": "provider"}, {"name": "body", "alphabet": "[A-Za-z0-9]", "length": 40, "basis": "length and alphabet: provider's token-formats page documents the identical `[40 characters][checksum]` format cell as `cfut_`; corroborated by trufflehog's `cf[ua]t_` alternation"}, {"name": "checksum", "alphabet": "[0-9a-f]", "length": 8, "basis": "existence: provider; width and alphabet: trufflehog's `cf[ua]t_[a-zA-Z0-9]{40}[a-f0-9]{8}` rule matches both `cfut_` and `cfat_` with the same checksum shape; adopted as support-policy identically to `cfut_`. Lexical only: no checksum algorithm is computed or claimed."}], "sources": ["cloudflare-token-formats", "cloudflare-account-tokens", "trufflehog-3.97.4", "github-secret-scanning-patterns"]}
      ],
      "pending": [
        {"id": "scannable-global-key", "prefix": "cfk_", "tier": "T1", "reason": "Provider-documented to exist with the same generic `[40 characters][checksum]` format cell as `cfut_`/`cfat_` (reverified 2026-09-20 against the live token-formats page), but its checksum width and alphabet remain uncorroborated by either tool: trufflehog's `cf[ua]t_` alternation does not include `k`, and gitleaks' `cloudflare-global-api-key` rule targets only the legacy unprefixed 37-45-character hex key, not the new scannable format. Freezing a grammar here would be a guess, not a reviewed contract; issue #481 adopted `cfat_` alone and split `cfk_` into issue #486 rather than force a disposition on unresolved evidence. Issue #486 (2026-09-20) re-checked every avenue its own text names and found none resolved: the live `token-formats` and `keys` provider pages still state only the generic `[checksum]` placeholder, with no width or alphabet, for all three prefixes; trufflehog's `cloudflareapitoken` v2 detector at its latest tagged release (v3.97.5, superseding the pinned v3.97.4) still excludes `k` from its `cf[ua]t_` alternation (`trufflehog-3.97.5`); gitleaks' `gitleaks.toml` at its latest tagged release (v8.30.1, unchanged) still has no `cfk_`-specific rule and its `cloudflare-global-api-key` rule still targets only the legacy unprefixed key; GitHub's supported-secret-scanning-patterns page lists a `Cloudflare Global User API Key` partner type (existence only, `github-secret-scanning-patterns` already records this establishes no grammar). No direct correspondence with Cloudflare's docs/security team was sought or available to this review. With none of the three resolving avenues met, issue #486 is closed as won't-fix per the same honest-alternative policy already applied to DigitalOcean's `dop_v2_` and Slack's legacy prefixes: `cfk_` stays permanently unsupported (T1: existence is provider-documented; the checksum shape stays T0 and unscored) until a future provider documentation update states the checksum shape explicitly or a tool ships a `cfk_`-specific rule.", "sources": ["cloudflare-token-formats", "trufflehog-3.97.4", "trufflehog-3.97.5", "gitleaks-8.30.1", "github-secret-scanning-patterns"]}
      ],
      "excluded": [
        {"id": "legacy-unprefixed-formats", "basis": "evidence", "reason": "The pre-2026 40-character alphanumeric token and 37–45-character hex global key are indistinguishable from ordinary opaque values without a prefix; already recorded in the module documentation as a known false negative."},
        {"id": "non-hex-checksum", "basis": "single-tool; adopted as support-policy", "reason": "The provider says a checksum follows the 40-character body but does not publish its width or alphabet; the 8-byte lowercase-hex form comes from one tool. A token whose trailing eight bytes are not lowercase hex is intentionally unsupported under this choice."}
      ],
      "conflicts": [
        {"property": "checksum width and alphabet", "positions": {"cloudflare-token-formats": "a checksum follows the body; width and alphabet unstated", "trufflehog-3.97.4": "8 lowercase-hex bytes", "gitleaks-8.30.1": "no rule"}, "resolution": "Provider evidence establishes that a checksum segment exists (so a 40-byte body alone is malformed); one tool supplies its width and alphabet. Adopted as support-policy and recorded as lexical validation only. Applies identically to `cfut_` and `cfat_`; `cfk_` is not covered by this resolution because no tool corroborates its checksum shape (see `pending`). Issue #566 (2026-09-21) re-fetched the live `token-formats` page: still last-updated 2026-04-20, still \"Each credential type has a distinct prefix followed by 40 characters and a checksum\" for every prefix including `cfut_`, with no width or alphabet published. A checksum-less `cfut_` + 40-byte body (the `detector-coverage--cloudflare-token-shape-1-*` fixtures' shape) contradicts the provider's own statement that a checksum follows the body, so it is not adopted as a positive; the fixtures are dispositioned as a benchmarks-side generator artifact, not a coverage gap (see `docs/audits/evidence/566/README.md`)."}
      ],
      "mutations": [
        {"mutationKind": "alphabet", "mutation": "alphabet: non-hexadecimal eight-character suffix", "fixtureIds": ["cloudflare-token-user-plain-twin", "cloudflare-token-user-unicode-crlf-twin"], "distinguishingProperty": "eight-byte lowercase-hex checksum", "basis": "existence: provider; width and alphabet: single-tool; adopted as support-policy", "disposition": "retained", "reason": "Retained as must-not-flag: the value has no checksum segment of any shape a consulted source describes. The tier stays T1 for the prefix and body; the suffix alphabet is recorded as tool-corroborated."},
        {"mutationKind": "alphabet", "mutation": "alphabet: non-hexadecimal eight-character suffix", "fixtureIds": ["cloudflare-boundary-account-checksum-non-hex"], "distinguishingProperty": "eight-byte lowercase-hex checksum", "basis": "existence: provider; width and alphabet: single-tool; adopted as support-policy", "disposition": "retained", "reason": "Issue #481: the `cfat_` twin of the `cfut_` non-hex-checksum mutation above. Retained as must-not-flag for the identical reason: the value has no checksum segment of any shape a consulted source describes."}
      ]
    },
    {
      "detector": "linear-token",
      "findingType": "linear_token",
      "implementation": "crates/secret-scan-core/src/detectors/additional_providers.rs (LINEAR)",
      "beta4Rule": "`lin_api_` or `lin_oauth_` plus >=20 bytes of `[A-Za-z0-9_-]`; both prefixes share one rule.",
      "toolRules": {
        "gitleaks-8.30.1": "linear-api-key `lin_api_(?i)[a-z0-9]{40}`; linear-client-secret is a context-gated `[a-f0-9]{32}` rule; no `lin_oauth_` rule",
        "trufflehog-3.97.4": "linearapi: `\\b(lin_api_[0-9A-Za-z]{40})\\b`; no `lin_oauth_` detector"
      },
      "variants": [
        {"id": "api", "support": "supported", "tier": "T2", "grammar": "lin_api_[A-Za-z0-9]{40}", "segments": [{"name": "prefix", "literal": "lin_api_", "basis": "tool-agreement; existence of the type corroborated by github-secret-scanning-patterns"}, {"name": "body", "alphabet": "[A-Za-z0-9]", "length": 40, "basis": "tool-agreement"}], "sources": ["gitleaks-8.30.1", "trufflehog-3.97.4", "github-secret-scanning-patterns", "linear-docs-graphql", "linear-docs-api-webhooks"]},
        {"id": "oauth", "support": "supported-interim", "tier": "T0", "grammar": "lin_oauth_[A-Za-z0-9_-]{20,}", "segments": [{"name": "prefix", "literal": "lin_oauth_", "basis": "support-policy: no consulted provider or tool source shows this prefix; the provider's OAuth example is a bare 64-character hex string"}, {"name": "body", "alphabet": "[A-Za-z0-9_-]", "length": "20 or more", "basis": "support-policy: beta.4 rule retained unchanged as an interim guard; issue #570 restores this open floor after issue #551 briefly narrowed it to an exact length, which stopped matching every realistic-length token in this family (`RunLength::AtLeast` against the identical alphabet the boundary itself checks let a directly-glued wider identifier through, and #551 fixed that by conflating the floor with an unreviewed maximum instead of fixing the boundary check itself); `RunLength::OpenFloor` is the actual fix, capping the run back to the floor only when a dash or underscore starts right there"}], "sources": ["linear-docs-oauth", "github-secret-scanning-patterns"], "candidateGrammar": null, "candidateBasis": "GitHub lists a Linear OAuth Access Token type but no consulted source shows its grammar. The 40-byte API-key length must not be reused for it; the benchmark keeps its sample pending (T0)."}
      ],
      "pending": [
        {"id": "unprefixed-oauth-access-token", "tier": "T0", "reason": "The provider's OAuth documentation shows a bare 64-character hex access token. Without a prefix it is indistinguishable from ordinary hex and is not a candidate for a provider rule."}
      ],
      "excluded": [
        {"id": "underscore-or-dash-in-api-body", "basis": "evidence", "reason": "Neither tool alphabet contains `_` or `-`; the beta.4 acceptance was a shared-rule artifact."}
      ],
      "conflicts": [
        {"property": "case of the API-key body", "positions": {"gitleaks-8.30.1": "case-insensitive alphanumeric", "trufflehog-3.97.4": "mixed-case alphanumeric"}, "resolution": "Both accept `[A-Za-z0-9]`; no conflict in effect."}
      ],
      "mutations": [
        {"mutationKind": "length", "mutation": "length: 39 vs contracted 40", "fixtureIds": ["linear-token-api-plain-twin", "linear-token-api-unicode-crlf-twin"], "distinguishingProperty": "API-key body length exactly 40", "basis": "tool-agreement", "disposition": "retained", "reason": "Both tools state 40; silence follows from construction."}
      ]
    },
    {
      "detector": "google-api-key",
      "findingType": "google_api_key",
      "implementation": "crates/secret-scan-core/src/detectors/additional_providers.rs (GOOGLE)",
      "beta4Rule": "Not one of issue #367's seven frozen families. The default rule has been `AIza` plus an exact 35-byte suffix from `[A-Za-z0-9_-]` since the detector's introduction, already an exact-length grammar rather than beta.4's shared 20-byte minimum. Issue #487 (this entry) evaluates Google's three OAuth credential formats for coverage and leaves the `AIza` contract unchanged.",
      "toolRules": {
        "gitleaks-8.30.1": "gcp-api-key: `\\b(AIza[\\w-]{35})(?:[\\x60'\"\\s;]|\\\\[nr]|$)` — exact 35-byte body, alphabet `[A-Za-z0-9_-]`; no rule of any kind for `GOCSPX-`, `1//`, or `ya29.`",
        "trufflehog-3.97.4": "googlegemini: `\\b(AIzaSy[A-Za-z0-9_-]{33})` — a narrower 6-byte `AIzaSy` prefix plus 33 bytes, the same 39-byte total length as gitleaks' 4-byte-prefix-plus-35 rule. googleoauth2: `\\b(ya29\\.(?i:[a-z0-9_-]{10,}))(?:[^a-z0-9_-]|\\z)` — open-ended minimum length, no ceiling. No rule for `GOCSPX-` or `1//` in any of this tag's four Google/GCP detector packages (`gcp`, `gcpapplicationdefaultcredentials`, `googlegemini`, `googleoauth2`); `gcpapplicationdefaultcredentials` only extracts an already-present `client_secret` JSON field value contextually, which is not a lexical grammar for the bare, dotenv, or log-line surfaces issue #487 reports as undetected.",
        "flare-redact-1.6.1": "google_api_key: `AIza[0-9A-Za-z_-]{35}` — agrees with gitleaks exactly. gcp_refresh_token: `1//[A-Za-z0-9_-]{20,160}` — the only rule any consulted source ships for `1//`, single-tool and open-range. No rule for `GOCSPX-` or `ya29.`."
      },
      "variants": [
        {"id": "api-key", "support": "supported", "tier": "T2", "grammar": "AIza[A-Za-z0-9_-]{35}", "segments": [{"name": "prefix", "literal": "AIza", "basis": "provider (one example value) and tool-agreement (gitleaks, trufflehog and flare-redact all match this literal prefix)"}, {"name": "body", "alphabet": "[A-Za-z0-9_-]", "length": 35, "basis": "length: the provider's one example is 39 characters total (35 after the prefix), corroborated by tool-agreement (gitleaks and flare-redact both state exactly 35; trufflehog states the same 39-byte total under a narrower 6-byte `AIzaSy` prefix split); alphabet: tool-agreement"}], "sources": ["google-cloud-api-key-docs", "gitleaks-8.30.1", "trufflehog-3.97.4", "flare-redact-1.6.1"]}
      ],
      "pending": [
        {"id": "oauth-client-secret", "prefix": "GOCSPX-", "tier": "T0", "reason": "Issue #487. No consulted source corroborates a body grammar. Google's own OAuth 2.0 protocol page (reobserved 2026-09-20) shows no example value or format detail for any OAuth credential, only unrelated size ceilings that do not by themselves distinguish a real credential from an arbitrary string of the same size. Neither pinned tool registers a `GOCSPX-` rule at all: gitleaks 8.30.1's only Google rule is `gcp-api-key` (the `AIza` shape above); trufflehog 3.97.4 ships four Google/GCP detector packages and none matches a bare `GOCSPX-` value — `gcpapplicationdefaultcredentials` only extracts a value already sitting in a `client_secret` JSON field once `{...client_secret...}` structure and the `.apps.googleusercontent.com` keyword are present, contextual extraction rather than a lexical grammar, and it cannot help the bare, dotenv, or log-line surfaces this issue reports as undetected. flare-redact 1.6.1 ships `google_api_key` and `gcp_refresh_token` but no `GOCSPX-` rule of any kind. The one signal that exists is GitHub's secret-scanning partner-pattern listing, which names a Google OAuth Client ID type that also lists `google_oauth_client_secret`, with push protection enabled — existence corroboration only, the identical reading this file already applies to Cloudflare's `cfk_` (`families.cloudflare-token.pending`). Freezing a grammar here would be a guess, not a reviewed contract, so `GOCSPX-` is not adopted. If a future gitleaks or trufflehog release ships a `GOCSPX-`-specific rule, or Google's own documentation states the body's length or alphabet, this disposition should be revisited.", "sources": ["google-oauth2-docs", "gitleaks-8.30.1", "trufflehog-3.97.4", "flare-redact-1.6.1", "github-secret-scanning-patterns"]},
        {"id": "oauth-refresh-token", "prefix": "1//", "tier": "T0", "reason": "Issue #487. The only rule any consulted source ships is flare-redact 1.6.1's `gcp_refresh_token`: `1//[A-Za-z0-9_-]{20,160}` — a bare 3-byte prefix plus a 20-to-160-byte minimum-to-maximum run of a common alphabet, single-tool and not independently corroborated by gitleaks (no rule) or trufflehog 3.97.4 (no `1//`-specific rule anywhere in its four Google/GCP detector packages). This is the same broad prefix-plus-minimum-length shape issue #367 found flags near-miss twins across seven other families; adopting a range this wide as a body grammar would reproduce that risk rather than establish a reviewed contract. Google's own OAuth 2.0 protocol page states an upper bound only (refresh tokens ≤512 bytes total) and shows no example value, so it corroborates neither this nor any narrower range. GitHub's secret-scanning partner-pattern listing names a Google OAuth Refresh Token type (push protection enabled) — existence only, the same reading as `GOCSPX-` above. Not adopted; left undetected and recorded, matching how this file already handles DigitalOcean's `dop_v2_` and Slack's legacy prefixes.", "sources": ["google-oauth2-docs", "gitleaks-8.30.1", "trufflehog-3.97.4", "flare-redact-1.6.1", "github-secret-scanning-patterns"]},
        {"id": "oauth-access-token", "prefix": "ya29.", "tier": "T0", "reason": "Issue #487. trufflehog 3.97.4's `googleoauth2` package registers exactly one rule, for this prefix: `\\b(ya29\\.(?i:[a-z0-9_-]{10,}))(?:[^a-z0-9_-]|\\z)` — an open-ended minimum length (10-byte floor, no ceiling) over a common alphabet, corroborated by no other consulted source: gitleaks has no rule, and flare-redact 1.6.1 has no `ya29.` rule (only `google_api_key` and `gcp_refresh_token`). An open-ended minimum is a weaker shape than even `1//`'s bounded range above and carries the identical near-miss risk issue #367 already documented for bare-prefix-plus-minimum-length grammars. Google's own OAuth 2.0 protocol page states an upper bound only (access tokens ≤2048 bytes total) and shows no example value. GitHub's secret-scanning partner-pattern listing names a Google OAuth Access Token type (push protection enabled) — existence only. `ya29.` tokens are also short-lived by design, which lowers the value of detecting them even if a grammar existed. Not adopted; left undetected and recorded.", "sources": ["google-oauth2-docs", "trufflehog-3.97.4", "flare-redact-1.6.1", "github-secret-scanning-patterns"]}
      ],
      "excluded": [],
      "conflicts": [
        {"property": "existence of a `GOCSPX-`/`1//`/`ya29.` body grammar in the pinned tool inventory", "positions": {"issue-487-body": "names TruffleHog's `googleoauth2` registration and flare-redact's `gcp_refresh_token` detector as tool corroboration available for the three OAuth formats generally, and ranks `GOCSPX-` as \"the strongest candidate\"", "gitleaks-8.30.1": "registers no rule for any of the three formats", "trufflehog-3.97.4": "`googleoauth2` registers a rule for `ya29.` only (open-ended minimum length); no rule for `GOCSPX-` or `1//` exists in this package or any other Google/GCP detector directory in the pinned tag", "flare-redact-1.6.1": "`gcp_refresh_token` registers a rule for `1//` only (20-160 byte range); no rule for `GOCSPX-` or `ya29.`"}, "resolution": "Verified directly against each pinned tool's source rather than taken on the issue body's summary: neither cited tool's corroboration covers `GOCSPX-` at all, and each of the other two formats is covered by exactly one tool's open-ended or wide-range rule, not independently corroborated. `GOCSPX-` therefore has less tool corroboration than `1//` or `ya29.`, the reverse of the issue body's own tentative ranking. All three stay pending (T0) rather than any one being frozen on a single uncorroborated or absent rule."}
      ],
      "mutations": []
    },
    {
      "detector": "notion-token",
      "findingType": "notion_integration_token",
      "implementation": "crates/secret-scan-core/src/detectors/notion.rs",
      "beta4Rule": "Not one of issue #367's seven frozen families and not present in beta.4. Issue #300 (2026-09-16) introduced the detector with both shapes already frozen at introduction (docs/decisions/2026-09-17-freeze-precision-contracts-for-seven-provider-families.md, Notion row). Issue #514 (this entry) records the formal precision-contract evidence for that grammar; the grammar itself is unchanged.",
      "toolRules": {
        "gitleaks-8.30.1": "notion-api-token: `\\b(ntn_[0-9]{11}[A-Za-z0-9]{32}[A-Za-z0-9]{3})(?:[\\x60'\"\\s;]|\\\\[nr]|$)` \u2014 `ntn_` only; no rule for `secret_`.",
        "trufflehog-3.97.4": "notion: `\\b(secret_[A-Za-z0-9]{43})\\b` \u2014 `secret_` only; no rule for `ntn_`.",
        "flare-redact-1.6.1": "notion_token: `(?:ntn_[A-Za-z0-9]{40,50}|secret_[A-Za-z0-9]{43})` \u2014 covers both prefixes; the `ntn_` branch is an open 40-50 byte range over the full alnum alphabet with no digit-run distinction, looser than gitleaks' exact 11-digit-then-35-alnum split."
      },
      "variants": [
        {
          "id": "legacy",
          "support": "supported",
          "tier": "T2",
          "grammar": "secret_[A-Za-z0-9]{43}",
          "segments": [
            {
              "name": "prefix",
              "literal": "secret_",
              "basis": "tool-agreement (trufflehog and flare-redact both use this literal); the provider changelog also names it as the pre-2024-09-25 prefix that remains functional"
            },
            {
              "name": "body",
              "alphabet": "[A-Za-z0-9]",
              "length": 43,
              "basis": "tool-agreement: trufflehog and flare-redact both fix this at exactly 43"
            }
          ],
          "sources": [
            "notion-changelog",
            "trufflehog-3.97.4",
            "flare-redact-1.6.1"
          ]
        },
        {
          "id": "current",
          "support": "supported",
          "tier": "T2",
          "grammar": "ntn_[0-9]{11}[A-Za-z0-9]{35}",
          "segments": [
            {
              "name": "prefix",
              "literal": "ntn_",
              "basis": "provider: \"Starting September 25, 2024, newly generated Public API tokens will automatically use the `ntn_` prefix instead of the `secret_` prefix.\""
            },
            {
              "name": "digit-run",
              "alphabet": "[0-9]",
              "length": 11,
              "basis": "single-tool (gitleaks); flare-redact's `ntn_` branch does not distinguish a digit-only segment and trufflehog has no `ntn_` rule at all"
            },
            {
              "name": "body",
              "alphabet": "[A-Za-z0-9]",
              "length": 35,
              "basis": "single-tool (gitleaks) for the exact 35-byte split after the digit run; corroborated only loosely by flare-redact's 40-50 total-suffix-byte range (46 falls inside it), which does not itself require a digit run"
            }
          ],
          "sources": [
            "notion-changelog",
            "gitleaks-8.30.1",
            "flare-redact-1.6.1"
          ]
        }
      ],
      "excluded": [
        {
          "id": "oauth-refresh-token-nrt",
          "basis": "provider",
          "reason": "The authorization guide's only token example (`nrt_4991090011501Ejc6Xn4sHguI7jZIN449mKe9PRhpMfNK`) is an OAuth refresh token, not a Public API integration token; its 13-digit run does not match the current integration-token shape's 11, and neither tool source registers an `nrt_` rule. Deliberately unsupported per the 2026-09-16 grammar freeze."
        },
        {
          "id": "oauth-access-token",
          "basis": "provider",
          "reason": "Notion's public-integration OAuth access tokens have no stable documented or community-converged prefix; the docs show only opaque/temporary example values."
        },
        {
          "id": "page-database-block-ids",
          "basis": "provider",
          "reason": "UUID-shaped page/database/block identifiers and share-URL identifiers are public, not secrets, and carry neither the `secret_` nor `ntn_` prefix, so this detector never matches them regardless of length."
        }
      ],
      "conflicts": [
        {
          "property": "ntn_ suffix structure and total length",
          "positions": {
            "gitleaks-8.30.1": "exact 11-digit run then exact 35-byte alnum run (46 total suffix bytes)",
            "flare-redact-1.6.1": "no digit-run distinction; any 40-50 byte alnum run",
            "trufflehog-3.97.4": "no rule for `ntn_` at all"
          },
          "resolution": "gitleaks' structurally-specific rule is adopted because it is the only source that encodes the digit-run boundary the frozen grammar (2026-09-16 decision) relies on; flare-redact's broader range is treated as directionally corroborating only (46 falls inside 40-50), not as an independent confirmation of the exact split."
        },
        {
          "property": "tool coverage of the two prefixes",
          "positions": {
            "trufflehog-3.97.4": "`secret_` only",
            "gitleaks-8.30.1": "`ntn_` only",
            "flare-redact-1.6.1": "both `secret_` and `ntn_`"
          },
          "resolution": "No single external tool corroborates both shapes with the same rule; flare-redact is the only source that registers both prefixes at all, and its `ntn_` branch is looser than gitleaks'. Each shape's own exact-length contract is unaffected by the other tools' silence on it."
        }
      ]
    },
    {
      "detector": "vercel-token",
      "findingType": "vercel_token",
      "implementation": "crates/secret-scan-core/src/detectors/additional_providers.rs (VERCEL)",
      "beta4Rule": "Not one of issue #367's seven frozen families. Unchanged since introduction: five `PrefixShape::at_least(_, 20, is_alnum_dash, ...)` shapes, one per prefix (`vcp_`, `vci_`, `vca_`, `vcr_`, `vck_`). Issue #516 (this entry) records the formal precision-contract evidence for that grammar; the grammar itself is unchanged.",
      "toolRules": {
        "gitleaks-8.30.1": "no Vercel rule of any kind; the full 3209-line pinned config was searched directly for `vercel` with zero matches.",
        "trufflehog-3.97.4": "vercel: `PrefixRegex([\"vercel\"]) + \\b([a-zA-Z0-9]{24})\\b` — keyword-gated (the literal word `vercel` must appear nearby), matching a bare, exactly-24-character alphanumeric body with no fixed prefix. Registers no rule for any of the five `vc*_`-prefixed shapes. Fetched directly and diffed byte-for-byte against the latest tag `v3.97.5`: identical, unchanged.",
        "flare-redact-1.6.1": "no Vercel-related detector entries of any kind."
      },
      "variants": [],
      "pending": [
        {"id": "personal-access-token", "prefix": "vcp_", "tier": "T0", "reason": "Issue #516. `vercel-docs-access-tokens` states the literal prefix rule in prose (\"Personal access tokens begin with the prefix `vcp_`\"), corroborated by `vercel-changelog-token-formats`. No source establishes a body length or alphabet: the only body evidence is a masked `xxxxxxxxxxxxxxxxxxxxxxxx` redaction placeholder in a curl snippet, not a generated example, and no pinned tool (gitleaks, trufflehog, flare-redact) registers any rule for this prefix. Stays on the existing `at_least(20, is_alnum_dash)` floor, which the placeholder's 24-character length already satisfies. Unblocking condition: a shipped gitleaks or TruffleHog rule for `vcp_`, or a Vercel documentation page stating one body length/alphabet rule in prose.", "sources": ["vercel-changelog-token-formats", "vercel-docs-access-tokens", "vercel-rest-api-reference", "gitleaks-8.30.1", "trufflehog-3.97.4", "flare-redact-1.6.1"]},
        {"id": "integration-access-token", "prefix": "vci_", "tier": "T0", "reason": "Issue #516. `vercel-changelog-token-formats` and GitHub's partner-pattern listing (`github-secret-scanning-patterns`, \"Vercel Integration Access Token\") corroborate the prefix's existence and literal spelling. No source found by this audit shows an example value or a stated body length/alphabet for this specific prefix — the one integration-token example found (`vercel-docs-api-integrations`'s bare 24-character `access_token`) belongs to a different, unprefixed OAuth `code`-exchange credential (see `excluded.bare-oauth-access-token` below), not this one. No pinned tool registers any rule for `vci_`. Unblocking condition: as `personal-access-token` above, scoped to `vci_`.", "sources": ["vercel-changelog-token-formats", "github-secret-scanning-patterns", "vercel-docs-api-integrations", "gitleaks-8.30.1", "trufflehog-3.97.4", "flare-redact-1.6.1"]},
        {"id": "app-access-token", "prefix": "vca_", "tier": "T0", "reason": "Issue #516. `vercel-changelog-token-formats` and `github-secret-scanning-patterns` (\"Vercel App User Access Token\") corroborate the prefix. `vercel-docs-sign-in-tokens` shows one real example body (56 bytes, mixed-case alphanumeric, no `-`/`_`) but states no length or alphabet rule in prose, and no pinned tool registers a rule for `vca_` — a single documented instance does not promote a shape past T0 in this file's framework on its own (compare `families.google-api-key.variants[0]`'s `AIza` body, which additionally has two-tool corroboration this instance lacks entirely). Unblocking condition: as `personal-access-token` above, scoped to `vca_`.", "sources": ["vercel-changelog-token-formats", "github-secret-scanning-patterns", "vercel-docs-sign-in-tokens", "gitleaks-8.30.1", "trufflehog-3.97.4", "flare-redact-1.6.1"]},
        {"id": "app-refresh-token", "prefix": "vcr_", "tier": "T0", "reason": "Issue #516. `vercel-changelog-token-formats` and `github-secret-scanning-patterns` (\"Vercel App Refresh Token\", the one Vercel row without push protection) corroborate the prefix. `vercel-docs-sign-in-tokens`'s \"Refresh Token example\" reuses the identical 56-byte body shown for `vca_` above, so it is not independent evidence for this prefix's own body shape; no pinned tool registers a rule for `vcr_`. Unblocking condition: as `personal-access-token` above, scoped to `vcr_`.", "sources": ["vercel-changelog-token-formats", "github-secret-scanning-patterns", "vercel-docs-sign-in-tokens", "gitleaks-8.30.1", "trufflehog-3.97.4", "flare-redact-1.6.1"]},
        {"id": "api-key", "prefix": "vck_", "tier": "T0", "reason": "Issue #516. `vercel-changelog-token-formats` and `github-secret-scanning-patterns` corroborate the prefix; `vercel-docs-ai-gateway-api-keys` independently confirms it via a compromised-secret-report example (`\"api_key\": \"vck_...\"`). No source found by this audit gives a body length or alphabet, and no pinned tool registers a rule for `vck_`. Unblocking condition: as `personal-access-token` above, scoped to `vck_`.", "sources": ["vercel-changelog-token-formats", "github-secret-scanning-patterns", "vercel-docs-ai-gateway-api-keys", "gitleaks-8.30.1", "trufflehog-3.97.4", "flare-redact-1.6.1"]}
      ],
      "excluded": [
        {"id": "support-access-token", "basis": "existence-only", "reason": "GitHub's secret-scanning partner-pattern listing names a sixth Vercel type, \"Vercel Support Access Token\", with push protection enabled, alongside the five that map onto `vercel-changelog-token-formats`'s five prefixes. No Vercel documentation, changelog, or pinned tool consulted by this audit names a prefix or any shape for it. Not scored `pending` because there is no candidate grammar of any kind to revisit: this is opaque, existence-only corroboration, the same reading this file already gives Cloudflare's `cfk_` checksum shape (`families.cloudflare-token.pending[0]`) and Google's three OAuth formats (`families.google-api-key.pending`). Per issue #501's own instruction, an opaque credential with no lexical evidence is not forced into a detector shape."},
        {"id": "bare-oauth-access-token", "basis": "provider", "reason": "`vercel-docs-api-integrations` (updated 2026-09-16, five weeks after the prefix changelog) still returns a bare, unprefixed, exactly-24-character alphanumeric `access_token` (and accepts a `client_secret` of the identical shape) from the OAuth `code`-exchange endpoint (`POST /v2/oauth/access_token`). This is the real credential TruffleHog's pinned `vercel` detector matches (`PrefixRegex([\"vercel\"]) + [a-zA-Z0-9]{24}`) — not a stale rule for a superseded format, but tool-agreement with a currently-documented, still-unprefixed Vercel credential surface distinct from the five `vc*_`-prefixed classes above. Excluded permanently, not pending: without a fixed prefix this value is indistinguishable from an arbitrary 24-character identifier, the same reasoning already applied to Linear's `unprefixed-oauth-access-token` (`families.linear-token.excluded`) and to Google's unprefixed OAuth candidates. It stays covered only incidentally by `generic-token`'s contextual rule when the keyword `vercel` or a high-signal key name triggers it — the same keyword gate TruffleHog's own rule requires."}
      ],
      "conflicts": [
        {"property": "what the pinned TruffleHog `vercel` detector's bare 24-character rule corresponds to", "positions": {"issue-516-framing": "reads the rule as conflicting with the corpus's five documented prefixes, implying it is stale or wrong", "vercel-docs-api-integrations": "shows the OAuth `code`-exchange endpoint still issuing bare, unprefixed 24-character `access_token`/`client_secret` values as of 2026-09-16, five weeks after the prefix changelog shipped", "trufflehog-3.97.4": "the rule is unchanged at the latest tag `v3.97.5` and registers no rule for any `vc*_`-prefixed shape"}, "resolution": "Not a conflict between one right and one wrong source: two different Vercel credential surfaces exist side by side. TruffleHog's rule accurately covers the older, unprefixed OAuth-integration surface and says nothing about the five newer prefixed classes, which stay pending on their own lack of tool or documented-length evidence, unaffected by TruffleHog's rule one way or the other. The unprefixed surface itself is excluded regardless, since it carries no fixed prefix to build a provider rule on."}
      ],
      "mutations": []
    }
  ]
}
