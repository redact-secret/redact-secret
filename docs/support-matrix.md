# Support matrix

Generated by `scripts/generate-support-matrix-docs.py` from the pinned `benchmarks/support-matrix.json` (issue #510, A9; part of #500). Do not edit by hand --
regenerate with:

```sh
python3 -B scripts/generate-support-matrix-docs.py
```

`npm run ci` fails if this file, or the README support-status section, is out of date with `benchmarks/support-matrix.json`. That file is itself a pinned copy of `redact-secret-benchmarks`'s generated evidence ([source revision](https://github.com/redact-secret/redact-secret-benchmarks/commit/28fc818966d9acf7ad3f77ce38577d6b5c14ea0b)); see the module docstring for how to refresh it.

42 providers, 93 credential families.

For the detailed measurement protocol behind these statuses -- evidence tiers, and the twin, benign, metamorphic, mutation, and differential criteria a family must clear -- see `redact-secret-benchmarks`'s [support-status specification](https://github.com/redact-secret/redact-secret-benchmarks/blob/28fc818966d9acf7ad3f77ce38577d6b5c14ea0b/docs/support-status.md). You do not need to read it to use this table.

## What each status means

### `stable` (51)

Officially supported. Provider-documented (T1) contract, negative twins, benign controls, metamorphic and mutation evidence, with no unresolved critical disagreement. You can rely on this family's detection and its precision behavior. Stable does not mean every historical or future variant of this credential is detected -- see each family's supported contexts and known limitations below.

### `provisional` (21)

Useful today, but the evidence behind it is incomplete -- typically a T2, tool-corroborated contract rather than a provider-documented (T1) one. Provisional is not "almost stable": it is a distinct, load-bearing evidence state that can persist indefinitely if a provider never publishes a documented format.

### `pending` (2)

No stable positive contract exists yet (T0): no fixture for this family has cleared review. Do not rely on this family's detection or its absence.

### `unsupported` (19)

This project explicitly does not detect this credential family. The reason is stated per family below -- a related family is covered instead, the shape has no reviewed contract, and so on -- rather than the family being silently absent from this document.

## Families

### Stable

| Provider | Family | Evidence tier | Detector(s) | Supported contexts & known limitations |
| --- | --- | --- | --- | --- |
| (format) | JSON Web Token | T1 | jwt | three base64url segments; the reviewed control is additionally signature-verified offline |
| (format) | PEM-encoded private key | T1 | private-key | BEGIN/END PRIVATE KEY labels and base64 body; the reviewed control is additionally parsed as PKCS#8 offline |
| anthropic | Secret API key | T1 | anthropic-token | the provider identifies sk-ant-api03- as the Claude API key prefix; the 93-character body, alphabet and AA suffix remain tool-corroborated |
| aws | IAM user access key | T1 | aws-access-key | AKIA/ASIA/ABIA/ACCA prefixes, and AIDA as the IAM-user unique-ID prefix rather than an access key (re-checked 2026-09-20, #36); 16-character base32 body and 40-character secret are tool-corroborated |
| azure-devops | Personal access token | T1 | azure-devops-personal-access-token | tokens are 84 characters long with a fixed AZDO signature at positions 76-80; Microsoft Purview separately supplies the alphanumeric detail used by the pattern |
| cloudflare | Scannable API token | T1 | cloudflare-token | cfut_ prefix documented as the scannable token format; 40+8 body structure is tool-corroborated |
| confluent | Cloud API secret (cflt-prefixed) | T1 | confluent-cloud-api-secret | the page states "API secrets created after July 30, 2025 have a cflt prefix followed by 60 characters consisting of A-Z, a-z, 0-9, + or /" and that "the final 6 characters contain a Base64-encoded CRC32 checksum of the prior 54 characters"; the checksum is documented but not part of this contract's lexical pattern (it shares the body's alphabet, per the cloudflare-token precedent) |
| datadog | API key | T1 | datadog-api-key | Datadog's own API reference spec (docs.datadoghq.com /resources/json/full_spec_v1.json, v1 ApiKey schema, generated from DataDog/documentation hugo/data/api/v1/full_spec.yaml) fixes the API key value at exactly 32 characters (minLength 32, maxLength 32) and names the DD-API-KEY header and DD_API_KEY environment variable. The constraint is stated on the v1 key model; the v2 schema states no length. The lowercase-hex alphabet is not provider-stated: the spec's example fits it, and it is corroborated by Datadog-owned code and tools only |
| datadog | Application key (ddapp_-prefixed) | T1 | datadog-application-key | Application keys column: "Identifiable prefix … ddapp_ (new)" establishes ddapp_ as the identifying prefix of current-format application keys. Body length (34 characters) and alphabet are not stated on this page; both are corroborated only by Datadog-owned code and a third-party partner doc, never by a provider-domain page |
| digitalocean | OAuth token | T1 | digitalocean-token | dop_v1_/doo_v1_/dor_v1_ prefixes; 64-hex body is tool-corroborated |
| digitalocean | Personal access token | T1 | digitalocean-token | dop_v1_/doo_v1_/dor_v1_ prefixes; 64-hex body is tool-corroborated |
| digitalocean | Refresh token | T1 | digitalocean-token | dop_v1_/doo_v1_/dor_v1_ prefixes; 64-hex body is tool-corroborated |
| docker | OAuth access token | T1 | docker-token | Docker's own API reference (docs.docker.com AI Governance API spec, bearerAuth credential table) states the Personal Access Token format as dckr_pat_* and the Organization Access Token format as dckr_oat_*, establishing both prefixes (the * is a glob, not a grammar). Body lengths and alphabet are not provider-stated, except that Docker's Hub API spec (createOrgAccessTokenResponse.token) shows an example OAT with a 27-character alphanumeric body, on example strength only. The 27-character dckr_pat_ body, the 32-character dckr_oat_ width and the [A-Za-z0-9_-] alphabet remain tool-corroborated |
| docker | Personal access token | T1 | docker-token | Docker's own API reference (docs.docker.com AI Governance API spec, bearerAuth credential table) states the Personal Access Token format as dckr_pat_* and the Organization Access Token format as dckr_oat_*, establishing both prefixes (the * is a glob, not a grammar). Body lengths and alphabet are not provider-stated, except that Docker's Hub API spec (createOrgAccessTokenResponse.token) shows an example OAT with a 27-character alphanumeric body, on example strength only. The 27-character dckr_pat_ body, the 32-character dckr_oat_ width and the [A-Za-z0-9_-] alphabet remain tool-corroborated |
| github | GitHub App server-to-server token | T1 | github-token | ghp_/gho_/ghu_/ghs_/ghr_ prefixes and underscore separator; 36-character body is tool-corroborated (checksum in the last six characters per github.blog/2021-04-05) |
| github | GitHub App user-to-server token | T1 | github-token | ghp_/gho_/ghu_/ghs_/ghr_ prefixes and underscore separator; 36-character body is tool-corroborated (checksum in the last six characters per github.blog/2021-04-05) |
| github | Classic personal access token | T1 | github-token | ghp_/gho_/ghu_/ghs_/ghr_ prefixes and underscore separator; 36-character body is tool-corroborated (checksum in the last six characters per github.blog/2021-04-05) |
| github | OAuth access token | T1 | github-token | ghp_/gho_/ghu_/ghs_/ghr_ prefixes and underscore separator; 36-character body is tool-corroborated (checksum in the last six characters per github.blog/2021-04-05) |
| github | OAuth refresh token | T1 | github-token | ghp_/gho_/ghu_/ghs_/ghr_ prefixes and underscore separator; 36-character body is tool-corroborated (checksum in the last six characters per github.blog/2021-04-05) |
| gitlab | Legacy personal access token | T1 | gitlab-token | glpat- prefix; 20-character legacy body is tool-corroborated, routable tokens are not covered |
| google | Generic API key | T1 | google-api-key | the provider page identifies the exposed key_string as an API key and shows an AIza-prefixed example; the 35-character suffix and alphabet remain gitleaks-corroborated |
| grafana | Cloud access policy token | T1 | grafana-cloud-access-policy-token | Grafana states that Cloud access policy tokens start with glc_; body length and alphabet remain tool-corroborated |
| grafana | Service account token | T1 | grafana-service-account-token | Grafana states that service account tokens carry a glsa prefix; the delimiter, segment lengths, alphabet and checksum placement remain tool-corroborated |
| hashicorp-terraform | Organization token | T1 | terraform-cloud-token | three independent HCP Terraform API-reference pages (user-tokens, organization-tokens, team-tokens) each publish a full example token; every one of the three is exactly 14 bytes before the literal .atlasv1. marker and exactly 67 bytes after it, and the mirrored Terraform Enterprise organization-tokens page publishes the identical shape and example, confirming Cloud and Enterprise are one grammar rather than two. Neither page documents a checksum or any marker beyond the literal .atlasv1. tag |
| hashicorp-terraform | Team token | T1 | terraform-cloud-token | three independent HCP Terraform API-reference pages (user-tokens, organization-tokens, team-tokens) each publish a full example token; every one of the three is exactly 14 bytes before the literal .atlasv1. marker and exactly 67 bytes after it, and the mirrored Terraform Enterprise organization-tokens page publishes the identical shape and example, confirming Cloud and Enterprise are one grammar rather than two. Neither page documents a checksum or any marker beyond the literal .atlasv1. tag |
| hashicorp-terraform | User token | T1 | terraform-cloud-token | three independent HCP Terraform API-reference pages (user-tokens, organization-tokens, team-tokens) each publish a full example token; every one of the three is exactly 14 bytes before the literal .atlasv1. marker and exactly 67 bytes after it, and the mirrored Terraform Enterprise organization-tokens page publishes the identical shape and example, confirming Cloud and Enterprise are one grammar rather than two. Neither page documents a checksum or any marker beyond the literal .atlasv1. tag |
| hashicorp-vault | Batch token | T1 | vault-token | prefixes and minimum length; the provider states the structure is opaque, so the 90–120-character rule the pinned tools use is corroboration, not contract |
| hashicorp-vault | Recovery token | T1 | vault-token | prefixes and minimum length; the provider states the structure is opaque, so the 90–120-character rule the pinned tools use is corroboration, not contract |
| hashicorp-vault | Service token | T1 | vault-token | prefixes and minimum length; the provider states the structure is opaque, so the 90–120-character rule the pinned tools use is corroboration, not contract |
| heroku | OAuth access token (HRKU-prefixed) | T1 | heroku-api-key | the page states "Heroku OAuth access tokens are 65 characters long and prefixed with HRKU-" and shows a 65-character worked example beginning HRKU-AA (not reproduced here, since secret scanners classify it as a live token); the literal AA after the dash and the [A-Za-z0-9_-] body alphabet are tool-corroborated and consistent with that example, not stated in prose |
| huggingface | API token | T1 | huggingface-token | Hugging Face's SDK reference on huggingface.co types the user access token as hf_${string}, establishing the hf_ prefix; the provider's Hub OpenAPI spec (/.well-known/openapi.json, POST /api/credentials/revoke) gives a raw access-token example of hf_ followed by 34 placeholder characters, which establishes the 34-character body length on example strength only. The [A-Za-z0-9] body alphabet is not provider-stated and remains a support-policy union of the tool alphabets (gitleaks letters only, TruffleHog alphanumeric); the api_org_ variant is not covered by this provider source |
| linear | Personal API key | T1 | linear-token | the provider documents lin_api_ for API keys and lin_oauth_ for OAuth access tokens; the 40-character body and alphabet remain tool-corroborated |
| microsoft-entra | Application client secret | T1 | microsoft-entra-client-secret | Microsoft Graph PowerShell's Add-MgApplicationPassword reference (learn.microsoft.com) prints two example SecretText values, each 3 characters (equal to the printed 3-character Hint), the digit 8, the literal Q~, and 34 characters, 40 in total. The Q~ marker at offset 4 and the 8-variant's 40-character width come from these examples, not from a stated grammar. The 7Q~ 37-character previous format, the digit-to-length coupling and the body alphabet are corroborated only by Microsoft's security-utilities code (SEC101/156) and third-party tools |
| netlify | Personal access token | T1 | netlify-token | Netlify's staff-authored announcement states "All Netlify authentication tokens will start with a nf prefix followed by a single identifying character" — nfp for personal access tokens (nfc/nfo/nfu/nfb for CLI, OAuth, app and build tokens) — and that token storage capacity must grow "to 40 characters"; the "_" delimiter and the [A-Za-z0-9_] body alphabet are tool-corroborated, not stated on the page |
| new-relic | License key | T1 | new-relic-license-key | New Relic's own documentation (docs.newrelic.com, IBM MQ host-integration page) states the ingest license key as 40 characters with the literal suffix NRAL, establishing the suffix and total length; its eBPF install pages show elided examples ending FFFFNRAL. The body alphabet, the FFFF segment and the EU eu01xx prefix are not provider-stated: they rest on New Relic-authored code (newrelic-cli IsValidLicenseKeyFormat, docs-website check-for-keys.sh) and trufflehog 3.97.4 |
| new-relic | User API key | T1 | new-relic-user-api-key | New Relic says most user keys begin with NRAK-; body length and alphabet remain tool-corroborated |
| notion | Legacy integration token | T1 | notion-token | the provider documents secret_ as the still-working legacy Public API token prefix and ntn_ as the prefix for newly generated tokens; the 43-character body and alphabet remain tool-corroborated |
| npm | Granular access token | T1 | npm-token | npm_ prefix, underscore delimiter and six-character Base62 CRC32 checksum; 36-character body is tool-corroborated |
| pulumi | Organization access token | T1 | pulumi-access-token | Pulumi's own Cloud REST API reference states, of the token-creation response: "The response includes the token ID and the tokenValue (prefixed with 'pul-')." Its personal-access-tokens sibling page carries the identical sentence. Neither page states a length or alphabet for the value that follows the prefix |
| pulumi | Personal access token | T1 | pulumi-access-token | Pulumi's own Cloud REST API reference states, of the token-creation response: "The response includes the token ID and the tokenValue (prefixed with 'pul-')." Its personal-access-tokens sibling page carries the identical sentence. Neither page states a length or alphabet for the value that follows the prefix |
| pulumi | Team access token | T1 | pulumi-access-token | Pulumi's own Cloud REST API reference states, of the token-creation response: "The response includes the token ID and the tokenValue (prefixed with 'pul-')." Its personal-access-tokens sibling page carries the identical sentence. Neither page states a length or alphabet for the value that follows the prefix |
| pypi | API token | T1 | pypi-token | docs.pypi.org's own detection-format page publishes this contract's exact regex, pypi-[A-Za-z0-9-_]{85,}: the pypi- prefix, a "-" separator, a base64 PyMacaroon serialization, an 85-character floor and no ceiling |
| sendgrid | API key | T1 | sendgrid-token | the total key is documented as always 69 characters and never shorter; the SG. prefix, the dot-separated id/secret segments, the 22/43 character split between them and the alphabet are tool-corroborated, not this citation |
| shopify | Custom app access token | T1 | shopify-token | shpat_ and shppa_ prefixes; 32-hex body is tool-corroborated |
| shopify | Public app access token | T1 | shopify-token | shpat_ and shppa_ prefixes; 32-hex body is tool-corroborated |
| slack | Bot token | T1 | slack-token | prefixes and section structure with the secret last; numeric section widths and 24-character secret are tool-corroborated |
| stripe | Live restricted key | T1 | stripe-token | sk_live_/sk_test_/rk_live_/rk_test_ secret and restricted keys, pk_ publishable keys documented as safe to expose, sk_org_ organization keys; 32-character body is tool-corroborated |
| stripe | Test restricted key | T1 | stripe-token | sk_live_/sk_test_/rk_live_/rk_test_ secret and restricted keys, pk_ publishable keys documented as safe to expose, sk_org_ organization keys; 32-character body is tool-corroborated |
| stripe | Live secret key | T1 | stripe-token | sk_live_/sk_test_/rk_live_/rk_test_ secret and restricted keys, pk_ publishable keys documented as safe to expose, sk_org_ organization keys; 32-character body is tool-corroborated |
| stripe | Test secret key | T1 | stripe-token | sk_live_/sk_test_/rk_live_/rk_test_ secret and restricted keys, pk_ publishable keys documented as safe to expose, sk_org_ organization keys; 32-character body is tool-corroborated |
| supabase | Personal access token | T1 | supabase-management-token | documents the sbp_ prefix by example (sbp_fc...) and the classic-vs-scoped distinction; states no exact body grammar |

### Provisional

| Provider | Family | Evidence tier | Detector(s) | Reason |
| --- | --- | --- | --- | --- |
| (format) | Bearer credential | T3 | bearer-token | positiveContract: no T1 provider-documented contract — T2 (tool-corroborated) and T3 (project policy) have no provider-documented ground truth to certify against, and T0 has no positive at all; only a T1 contract, backed by providerSource, counts. \| minimumTwinPairs: 3 < 5 — Five discriminated positive/negative twin pairs is the minimum sample that the detector reliably tells the credential apart from a near-identical non-secret. |
| (format) | Connection-string password | T3 | connection-string | positiveContract: no T1 provider-documented contract — T2 (tool-corroborated) and T3 (project policy) have no provider-documented ground truth to certify against, and T0 has no positive at all; only a T1 contract, backed by providerSource, counts. |
| (format) | OTP seed | T3 | otpauth-uri | positiveContract: no T1 provider-documented contract — T2 (tool-corroborated) and T3 (project policy) have no provider-documented ground truth to certify against, and T0 has no positive at all; only a T1 contract, backed by providerSource, counts. |
| (format) | Unclassified assignment literal | T3 | generic-token | positiveContract: no T1 provider-documented contract — T2 (tool-corroborated) and T3 (project policy) have no provider-documented ground truth to certify against, and T0 has no positive at all; only a T1 contract, backed by providerSource, counts. |
| atlassian | API token | T2 | atlassian-api-token | positiveContract: no T1 provider-documented contract — T2 (tool-corroborated) and T3 (project policy) have no provider-documented ground truth to certify against, and T0 has no positive at all; only a T1 contract, backed by providerSource, counts. \| minimumTwinPairs: 3 < 5 — Five discriminated positive/negative twin pairs is the minimum sample that the detector reliably tells the credential apart from a near-identical non-secret. |
| confluent | Cloud API secret (unprefixed, pre-2025-07-30) | T2 | confluent-cloud-api-secret-legacy | positiveContract: no T1 provider-documented contract — T2 (tool-corroborated) and T3 (project policy) have no provider-documented ground truth to certify against, and T0 has no positive at all; only a T1 contract, backed by providerSource, counts. \| minimumTwinPairs: 3 < 5 — Five discriminated positive/negative twin pairs is the minimum sample that the detector reliably tells the credential apart from a near-identical non-secret. \| differential.unresolvedContractDisagreements: 6 > 0 — An unresolved disagreement with a peer scanner over what the provider contract requires means the contract itself may be wrong; stable requires it settled. |
| databricks | Personal access token | T2 | databricks-personal-access-token | positiveContract: no T1 provider-documented contract — T2 (tool-corroborated) and T3 (project policy) have no provider-documented ground truth to certify against, and T0 has no positive at all; only a T1 contract, backed by providerSource, counts. \| differential.unresolvedContractDisagreements: 6 > 0 — An unresolved disagreement with a peer scanner over what the provider contract requires means the contract itself may be wrong; stable requires it settled. |
| datadog | Application key (legacy 40-hex) | T2 | datadog-application-key-legacy | positiveContract: no T1 provider-documented contract — T2 (tool-corroborated) and T3 (project policy) have no provider-documented ground truth to certify against, and T0 has no positive at all; only a T1 contract, backed by providerSource, counts. \| minimumTwinPairs: 3 < 5 — Five discriminated positive/negative twin pairs is the minimum sample that the detector reliably tells the credential apart from a near-identical non-secret. |
| discord | Bot token | T2 | discord-bot-token | positiveContract: no T1 provider-documented contract — T2 (tool-corroborated) and T3 (project policy) have no provider-documented ground truth to certify against, and T0 has no positive at all; only a T1 contract, backed by providerSource, counts. \| minimumTwinPairs: 0 < 5 — Five discriminated positive/negative twin pairs is the minimum sample that the detector reliably tells the credential apart from a near-identical non-secret. \| differential.unresolvedContractDisagreements: 12 > 0 — An unresolved disagreement with a peer scanner over what the provider contract requires means the contract itself may be wrong; stable requires it settled. |
| firebase | Legacy FCM server key | T2 | firebase-server-key | positiveContract: no T1 provider-documented contract — T2 (tool-corroborated) and T3 (project policy) have no provider-documented ground truth to certify against, and T0 has no positive at all; only a T1 contract, backed by providerSource, counts. \| minimumTwinPairs: 3 < 5 — Five discriminated positive/negative twin pairs is the minimum sample that the detector reliably tells the credential apart from a near-identical non-secret. |
| heroku | Legacy API key (bare UUID) | T2 | heroku-api-key-legacy | positiveContract: no T1 provider-documented contract — T2 (tool-corroborated) and T3 (project policy) have no provider-documented ground truth to certify against, and T0 has no positive at all; only a T1 contract, backed by providerSource, counts. \| minimumTwinPairs: 3 < 5 — Five discriminated positive/negative twin pairs is the minimum sample that the detector reliably tells the credential apart from a near-identical non-secret. |
| mailchimp | Marketing API key | T2 | mailchimp-api-key | positiveContract: no T1 provider-documented contract — T2 (tool-corroborated) and T3 (project policy) have no provider-documented ground truth to certify against, and T0 has no positive at all; only a T1 contract, backed by providerSource, counts. |
| mailgun | Private API key and HTTP webhook signing key | T2 | mailgun-api-key | positiveContract: no T1 provider-documented contract — T2 (tool-corroborated) and T3 (project policy) have no provider-documented ground truth to certify against, and T0 has no positive at all; only a T1 contract, backed by providerSource, counts. |
| okta | Management API token (SSWS) | T2 | okta-api-token | positiveContract: no T1 provider-documented contract — T2 (tool-corroborated) and T3 (project policy) have no provider-documented ground truth to certify against, and T0 has no positive at all; only a T1 contract, backed by providerSource, counts. \| differential.unresolvedContractDisagreements: 9 > 0 — An unresolved disagreement with a peer scanner over what the provider contract requires means the contract itself may be wrong; stable requires it settled. |
| openai | Secret API key | T2 | openai-token | positiveContract: no T1 provider-documented contract — T2 (tool-corroborated) and T3 (project policy) have no provider-documented ground truth to certify against, and T0 has no positive at all; only a T1 contract, backed by providerSource, counts. |
| postman | API key | T2 | postman-api-key | positiveContract: no T1 provider-documented contract — T2 (tool-corroborated) and T3 (project policy) have no provider-documented ground truth to certify against, and T0 has no positive at all; only a T1 contract, backed by providerSource, counts. |
| sentry | Organization auth token | T2 | sentry-org-auth-token | positiveContract: no T1 provider-documented contract — T2 (tool-corroborated) and T3 (project policy) have no provider-documented ground truth to certify against, and T0 has no positive at all; only a T1 contract, backed by providerSource, counts. \| minimumTwinPairs: 0 < 5 — Five discriminated positive/negative twin pairs is the minimum sample that the detector reliably tells the credential apart from a near-identical non-secret. |
| sentry | User auth token | T2 | sentry-user-auth-token | positiveContract: no T1 provider-documented contract — T2 (tool-corroborated) and T3 (project policy) have no provider-documented ground truth to certify against, and T0 has no positive at all; only a T1 contract, backed by providerSource, counts. \| minimumTwinPairs: 0 < 5 — Five discriminated positive/negative twin pairs is the minimum sample that the detector reliably tells the credential apart from a near-identical non-secret. |
| telegram | Bot token | T2 | telegram-bot-token | positiveContract: no T1 provider-documented contract — T2 (tool-corroborated) and T3 (project policy) have no provider-documented ground truth to certify against, and T0 has no positive at all; only a T1 contract, backed by providerSource, counts. \| minimumTwinPairs: 0 < 5 — Five discriminated positive/negative twin pairs is the minimum sample that the detector reliably tells the credential apart from a near-identical non-secret. |
| twilio | API key secret | T2 | twilio-api-key-secret | positiveContract: no T1 provider-documented contract — T2 (tool-corroborated) and T3 (project policy) have no provider-documented ground truth to certify against, and T0 has no positive at all; only a T1 contract, backed by providerSource, counts. \| minimumTwinPairs: 0 < 5 — Five discriminated positive/negative twin pairs is the minimum sample that the detector reliably tells the credential apart from a near-identical non-secret. |
| twilio | Account auth token | T2 | twilio-auth-token | positiveContract: no T1 provider-documented contract — T2 (tool-corroborated) and T3 (project policy) have no provider-documented ground truth to certify against, and T0 has no positive at all; only a T1 contract, backed by providerSource, counts. \| minimumTwinPairs: 0 < 5 — Five discriminated positive/negative twin pairs is the minimum sample that the detector reliably tells the credential apart from a near-identical non-secret. |

### Pending

| Provider | Family | Evidence tier | Detector(s) | Reason |
| --- | --- | --- | --- | --- |
| supabase | Project secret key | T0 | supabase-token | positiveContractTier T0 — Tier T0 means no positive fixture has cleared review yet, so nothing about detection accuracy can be claimed for this family; pending is the only honest label. |
| vercel | Access token | T0 | vercel-token | positiveContractTier T0 — Tier T0 means no positive fixture has cleared review yet, so nothing about detection accuracy can be claimed for this family; pending is the only honest label. |

### Unsupported

| Provider | Family | Evidence tier | Detector(s) | Reason |
| --- | --- | --- | --- | --- |
| atlassian | Access token | — | — | atlassian-api-token's contract review states the pinned TruffleHog detector targets the distinct ATCT access-token family, not the ATAT-prefixed API-token shape. |
| aws | Context-specific credential | — | — | Documented in the same IAM unique-identifier prefix table as AKIA/ASIA; not matched by the AKIA-only pattern. Provider source: https://docs.aws.amazon.com/IAM/latest/UserGuide/reference_identifiers.html#identifiers-prefixes. |
| aws | IAM user secret access key | — | — | The aws-access-key contract's companion field states a separate 40-character secret access key is required alongside the access key ID; this project has no dedicated detector for the secret half, which carries no stable prefix to anchor on. |
| aws | STS service bearer token | — | — | Documented in the same IAM unique-identifier prefix table as AKIA/ASIA; not matched by the AKIA-only pattern. Provider source: https://docs.aws.amazon.com/IAM/latest/UserGuide/reference_identifiers.html#identifiers-prefixes. |
| aws | STS temporary access key | — | — | aws-access-key's pattern is anchored to AKIA only (benchmarks/lib/assessment.ts contract), so ASIA values are not matched. classifyFixture already special-cases a bare AWS positive as policy, not must-redact, because "some legacy ASIA values also use digits outside the base32 alphabet". Provider source: https://docs.aws.amazon.com/IAM/latest/UserGuide/reference_identifiers.html#identifiers-prefixes. |
| github | Fine-grained personal access token | — | — | Named explicitly in issue #502 as a family the github-token detector does not cover: its pattern only matches the ghp_/gho_/ghu_/ghs_/ghr_ underscore-plus-36-character-body shape, which github_pat_ does not fit. Provider source: https://docs.github.com/en/authentication/keeping-your-account-and-data-secure/about-authentication-to-github#githubs-token-formats. |
| gitlab | Routable personal access token | — | — | gitlab-token's contract review states explicitly that routable tokens are not covered by the 20-character-body pattern. Provider source: https://docs.gitlab.com/security/tokens/. |
| google | OAuth2 credential | — | — | google-api-key's contract review notes the pinned TruffleHog detectors separately cover Gemini and OAuth2 credentials, not this generic AIza API-key shape. |
| linear | OAuth access token | — | — | classifyFixture explicitly routes lin_oauth_ values to the "needs a separate format contract; related detector support is not evidence of parity" guard. Provider source: benchmarks/lib/assessment.ts (classifyFixture variant guard: "Variant support must not be inferred from a related family name."). |
| mailgun | Legacy 32-8-8 hex signing key | — | — | Known unsupported legacy variant per redact-secret#314 (product PR #680); not fixtured because the provider no longer documents it. Provider source: https://github.com/gitleaks/gitleaks/blob/v8.30.1/config/gitleaks.toml. |
| mailgun | Public validation key (pubkey-) | — | — | Documented public per redact-secret#314's scope; gitleaks 8.30.1's mailgun-pub-key rule still flags it, which the mailgun-api-key public-id control records. Provider source: https://github.com/gitleaks/gitleaks/blob/v8.30.1/config/gitleaks.toml. |
| netlify | CLI, OAuth, app and build tokens (nfc_/nfo_/nfu_/nfb_) | — | — | Out of redact-secret#311's explicit scope; each is a distinct credential, so none is a control or positive for netlify:personal-access-token. Provider source: https://answers.netlify.com/t/change-to-the-netlify-authentication-token-format/106146. |
| notion | Integration token | — | — | notion-token's contract review states this current ntn_-prefixed format has no contract here; the pinned gitleaks rule targets it instead of the legacy secret_ shape. |
| npm | Legacy token | — | — | npm-token's own provider source is the changelog announcing the npm_ prefix scheme as a replacement, implying the prior UUID-format token predates it; that legacy shape is not matched by the npm_-prefixed pattern. Provider source: https://github.blog/changelog/2021-09-23-npm-has-a-new-access-token-format/. |
| slack | App-level token | — | — | Documented alongside xoxb- in the same provider prefix table; not matched by the bot-token-only pattern. Provider source: https://docs.slack.dev/authentication/tokens. |
| slack | User token | — | — | slack-token's contract lists xoxp- among the provider-documented prefixes but its pattern only matches xoxb-. Provider source: https://docs.slack.dev/authentication/tokens. |
| slack | Workflow webhook token | — | — | classifyFixture explicitly routes xwfp- values to the "needs a separate format contract" guard. Provider source: benchmarks/lib/assessment.ts (classifyFixture variant guard: "Variant support must not be inferred from a related family name."). |
| stripe | Organization API key | — | — | classifyFixture explicitly routes sk_org_ values to "this variant needs a separate format contract; related detector support is not evidence of parity", so it is not claimed by stripe-token's live/test secret-key pattern. Provider source: benchmarks/lib/assessment.ts (classifyFixture variant guard: "Variant support must not be inferred from a related family name."). |
| stripe | Webhook signing secret | — | — | Same classifyFixture guard as the organization API key covers whsec_ values; stripe-token's pattern does not match them. Provider source: benchmarks/lib/assessment.ts (classifyFixture variant guard: "Variant support must not be inferred from a related family name."). |
