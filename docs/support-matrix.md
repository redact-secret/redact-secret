# Support matrix

Generated by `scripts/generate-support-matrix-docs.py` from the pinned `benchmarks/support-matrix.json` (issue #510, A9; part of #500). Do not edit by hand --
regenerate with:

```sh
python3 -B scripts/generate-support-matrix-docs.py
```

`npm run ci` fails if this file, or the README support-status section, is out of date with `benchmarks/support-matrix.json`. That file is itself a pinned copy of `redact-secret-benchmarks`'s generated evidence ([source revision](https://github.com/redact-secret/redact-secret-benchmarks/commit/cfaeac4d83a4c98cffd77328d3416eb920a7d25b)); see the module docstring for how to refresh it.

53 providers, 108 credential families.

Support status, evidence provenance, and qualification are separate dimensions. In particular, a T2 family may be stable through the empirical profile without being described as provider-documented or rewritten as T1. User-facing labels combine the dimensions without conflating them: `Stable · Provider documented`, `Stable · Empirically qualified`, and `Provisional · Tool corroborated`.

For the detailed measurement protocol behind these statuses -- evidence tiers, and the twin, benign, metamorphic, mutation, and differential criteria a family must clear -- see `redact-secret-benchmarks`'s [support-status specification](https://github.com/redact-secret/redact-secret-benchmarks/blob/cfaeac4d83a4c98cffd77328d3416eb920a7d25b/docs/specs/support-status.md). You do not need to read it to use this table.

## What each status means

### `stable` (83)

Officially supported. The family qualified in one of two ways: its format is documented by the provider (`documented`), or several independent sources corroborate it or it was checked against keys the provider actually issued, with stricter test and behavior checks (`empirical`). An empirically qualified family is never described as provider-documented. You can rely on this family's detection and its precision behavior. Stable does not mean every historical or future variant of this credential is detected -- see each family's supported contexts and known limitations below.

### `provisional` (7)

Useful today, but the evidence behind it is incomplete -- usually the format is confirmed by other scanners or by public examples rather than by the provider's own documentation. Provisional is not "almost stable": it can stay this way indefinitely if a provider never publishes its format. The Reason column says what is still missing.

### `pending` (1)

No reviewed detection contract exists yet: nothing about this family's detection has been verified. Do not rely on this family's detection or its absence.

### `unsupported` (17)

This project explicitly does not detect this credential family. The reason is stated per family below -- a related family is covered instead, the shape has no reviewed contract, and so on -- rather than the family being silently absent from this document.

## Evidence and qualification counts

Stable qualification profiles:

| Qualification profile | Stable families |
| --- | --- |
| Documented | 57 |
| Empirical | 26 |

Evidence tiers across all families:

| Evidence tier | Families |
| --- | --- |
| T1 | 57 |
| T2 | 29 |
| T3 | 4 |
| T0 | 1 |
| No reviewed tier | 17 |

## Families

### Stable

| Provider | Family | Support | Evidence tier | Evidence basis | Qualification profile | Detector(s) | Supported contexts & known limitations |
| --- | --- | --- | --- | --- | --- | --- | --- |
| (format) | JSON Web Token | Stable · Provider documented | T1 | Provider documentation | Documented | jwt | three base64url segments; the reviewed control is additionally signature-verified offline |
| (format) | PEM-encoded private key | Stable · Provider documented | T1 | Provider documentation | Documented | private-key | BEGIN/END PRIVATE KEY labels and base64 body; the reviewed control is additionally parsed as PKCS#8 offline |
| anthropic | Secret API key | Stable · Provider documented | T1 | Provider documentation | Documented | anthropic-token | the provider identifies sk-ant-api03- as the Claude API key prefix; the 93-character body, alphabet and AA suffix remain tool-corroborated |
| atlassian | API token | Stable · Empirically qualified | T2 | Independent tool/community corroboration | Empirical | atlassian-api-token | supported contexts: CLI argument or curl -u email:token Basic auth, bare ATAT... token anywhere on a line, quoted or unquoted, client-library config, debug log line, pasted ticket text, env/YAML/JSON/compose assignment (JIRA_API_TOKEN=..., settings.json, Pipelines variable); uncertainty: Atlassian publishes no token grammar. Its docs call the length 'varied', and its staff said in 2022 that tokens are opaque, then in 2025 that the ATAT prefix can be relied on. The 12-character ATATT3xFfGF0 header, the 192-character length and the =<8 uppercase hex> CRC32 suffix are empirical: one maintainer-issued key, a five-key report and scanner samples. The maintainer could not confirm the CRC32 on an issued key. |
| aws | IAM user access key | Stable · Provider documented | T1 | Provider documentation | Documented | aws-access-key | AKIA/ASIA/ABIA/ACCA prefixes, and AIDA as the IAM-user unique-ID prefix rather than an access key (re-checked 2026-09-20, #36); 16-character base32 body and 40-character secret are tool-corroborated |
| azure-devops | Personal access token | Stable · Provider documented | T1 | Provider documentation | Documented | azure-devops-personal-access-token | tokens are 84 characters long with a fixed AZDO signature at positions 76-80; Microsoft Purview separately supplies the alphanumeric detail used by the pattern |
| cloudflare | Scannable API token | Stable · Provider documented | T1 | Provider documentation | Documented | cloudflare-token | cfut_ prefix documented as the scannable token format; 40+8 body structure is tool-corroborated |
| confluent | Cloud API secret (cflt-prefixed) | Stable · Provider documented | T1 | Provider documentation | Documented | confluent-cloud-api-secret | the page states "API secrets created after July 30, 2025 have a cflt prefix followed by 60 characters consisting of A-Z, a-z, 0-9, + or /" and that "the final 6 characters contain a Base64-encoded CRC32 checksum of the prior 54 characters"; its own secret-detection snippet fixes the algorithm (CRC32 over the 54 characters after cflt, little-endian bytes, standard Base64, first 6 characters), so the checksum is recomputable and `validate` enforces it (#209, research #234) |
| confluent | Cloud API secret (unprefixed, pre-2025-07-30) | Stable · Empirically qualified | T2 | Independent tool/community corroboration | Empirical | confluent-cloud-api-secret-legacy | supported contexts: CONFLUENT_CLOUD_API_SECRET=<secret> in env, shell export, CI/k8s YAML, confluent api-key store <key-id> <secret>, confluent_cloud_api_secret = "<secret>" (tfvars), confluentApiSecret JS constant, curl -u <key-id>:<secret> https://api.confluent.cloud/..., librdkafka config line carrying a *.confluent.cloud bootstrap host and sasl.password, same line as a case-insensitive confluent keyword, schema.registry.url=https://<key-id>:<secret>@psrc-....confluent.cloud; uncertainty: Confluent documents no grammar for the unprefixed pre-2025-07-30 secret: the 64-character [A-Za-z0-9+/] shape rests on provider examples, the provider's Terraform test data and two peer rules, while one provider Basic-auth example is shorter (about 60) and no new legacy secret can be issued to re-check. A bare 64-byte base64-alphabet run has no marker of its own (a 64-hex digest fits it too), so the value is only claimed beside a confluent keyword. |
| databricks | Personal access token | Stable · Empirically qualified | T2 | Independent tool/community corroboration | Empirical | databricks-personal-access-token | supported contexts: bare value (the dapi prefix is the only marker; no keyword or workspace host required), double-quoted assignment value, value on the line after an unrelated comment line (CRLF); uncertainty: No Databricks page states the token grammar; dapi + 32 lowercase hex rests on tools, and the optional -<digit> suffix has no provider source at all (its meaning, digit count and even existence differ across tools). Microsoft Purview gives only a 32-character length, admits A-F, and its own example contradicts its pattern; no issued token has been measured (redact-secret#697/#698 open). Since 2026-09-24 the suffix is outside the claim (docs/decisions/2026-09-24-stop-asserting-provider-undecided-format-properties.md): no fixture asserts a suffixed span or silence on a suffix, so the product's handling of it is unmeasured. |
| datadog | API key | Stable · Provider documented | T1 | Provider documentation | Documented | datadog-api-key | Datadog's own API reference spec (docs.datadoghq.com /resources/json/full_spec_v1.json, v1 ApiKey schema, generated from DataDog/documentation hugo/data/api/v1/full_spec.yaml) fixes the API key value at exactly 32 characters (minLength 32, maxLength 32) and names the DD-API-KEY header and DD_API_KEY environment variable. The constraint is stated on the v1 key model; the v2 schema states no length. The lowercase-hex alphabet is not provider-stated: the spec's example fits it, and it is corroborated by Datadog-owned code and tools only |
| datadog | Application key (ddapp_-prefixed) | Stable · Provider documented | T1 | Provider documentation | Documented | datadog-application-key | Application keys column: "Identifiable prefix … ddapp_ (new)" establishes ddapp_ as the identifying prefix of current-format application keys. Body length (34 characters) and alphabet are not stated on this page; both are corroborated only by Datadog-owned code and a third-party partner doc, never by a provider-domain page |
| datadog | Application key (legacy 40-hex) | Stable · Empirically qualified | T2 | Independent tool/community corroboration | Empirical | datadog-application-key-legacy | supported contexts: DD_APPLICATION_KEY=<40-hex> in env (no final newline, single quotes, BOM), Markdown label 'Legacy Datadog application key: `<40-hex>`', dd_application_key key in JSON / YAML / TOML / Python assignment, same line as a DD_APPLICATION_KEY / datadog marker; uncertainty: Datadog's own domain documents no grammar for the pre-ddapp_ application key; the 40 lowercase-hex shape rests on Datadog-owned validators (Agent, CloudFormation, ARM/PowerShell), the v1 OpenAPI hash field's 40-character bounds, and peer rules. The Agent scrubber alone accepts either hex case, and Datadog calls application keys legacy features after Q3 2026. A bare 40-hex run has no marker (it fits a SHA-1 digest), so the value is only claimed beside a DD_APPLICATION_KEY / datadog marker. |
| digitalocean | OAuth token | Stable · Provider documented | T1 | Provider documentation | Documented | digitalocean-token | dop_v1_/doo_v1_/dor_v1_ prefixes; 64-hex body is tool-corroborated |
| digitalocean | Personal access token | Stable · Provider documented | T1 | Provider documentation | Documented | digitalocean-token | dop_v1_/doo_v1_/dor_v1_ prefixes; 64-hex body is tool-corroborated |
| digitalocean | Refresh token | Stable · Provider documented | T1 | Provider documentation | Documented | digitalocean-token | dop_v1_/doo_v1_/dor_v1_ prefixes; 64-hex body is tool-corroborated |
| discord | Bot token | Stable · Empirically qualified | T2 | Independent tool/community corroboration | Empirical | discord-bot-token | supported contexts: Authorization: Bot <token> header, bare three-segment token anywhere on a line, quoted or unquoted, env/JSON/YAML/TOML/compose/Actions assignment and client.run("...") argument, startup log line, Markdown text; uncertainty: Discord documents no grammar. Its docs have one example header, in the legacy 24/6/27 shape. The current 24/6/38 and 26/6/38 widths rest on dated empirical reports (2022-2024) and third-party regexes, never on either pinned scanner. Segment 1 width follows from base64 of an 18- or 19-digit ID, which is arithmetic, not a stated rule. Discord gives no stability guarantee, and a maintainer-issued token has not been measured here. |
| docker | OAuth access token | Stable · Provider documented | T1 | Provider documentation | Documented | docker-token | Docker's own API reference (docs.docker.com AI Governance API spec, bearerAuth credential table) states the Personal Access Token format as dckr_pat_* and the Organization Access Token format as dckr_oat_*, establishing both prefixes (the * is a glob, not a grammar). Body lengths and alphabet are not provider-stated, except that Docker's Hub API spec (createOrgAccessTokenResponse.token) shows an example OAT with a 27-character alphanumeric body, on example strength only. The 27-character dckr_pat_ body, the 32-character dckr_oat_ width and the [A-Za-z0-9_-] alphabet remain tool-corroborated |
| docker | Personal access token | Stable · Provider documented | T1 | Provider documentation | Documented | docker-token | Docker's own API reference (docs.docker.com AI Governance API spec, bearerAuth credential table) states the Personal Access Token format as dckr_pat_* and the Organization Access Token format as dckr_oat_*, establishing both prefixes (the * is a glob, not a grammar). Body lengths and alphabet are not provider-stated, except that Docker's Hub API spec (createOrgAccessTokenResponse.token) shows an example OAT with a 27-character alphanumeric body, on example strength only. The 27-character dckr_pat_ body, the 32-character dckr_oat_ width and the [A-Za-z0-9_-] alphabet remain tool-corroborated |
| firebase | Legacy FCM server key | Stable · Empirically qualified | T2 | Independent tool/community corroboration | Empirical | firebase-server-key | supported contexts: Authorization: key=<value> HTTP header (raw request, curl), bare AAAA...:... key anywhere on a line, quoted or unquoted, env/YAML/JSON/compose/Actions assignment and source-code constant, request log line, handoff note text; uncertainty: Google never documented this shape, and its only published legacy server-key example is the older AIza form. Every tool regex copies one 2020 write-up (Abss), so tool agreement is not independent. A key in Firebase's own repo, public-code measurements (58 of 66 real keys have a 140-char body) and the head-to-project-number decoding back the shape empirically. Late-2016 keys have 162-183-char bodies. The legacy FCM API was shut down in June 2024, so no new key can be issued to check. |
| fireworks-ai | API key | Stable · Provider documented | T1 | Provider documentation | Documented | fireworks-ai-api-key | the provider documents only the fw_ prefix (examples are truncated); no body length or alphabet is stated |
| github | GitHub App server-to-server token | Stable · Provider documented | T1 | Provider documentation | Documented | github-token | ghp_/gho_/ghu_/ghs_/ghr_ prefixes and underscore separator; 36-character body is tool-corroborated (checksum in the last six characters per github.blog/2021-04-05) |
| github | GitHub App user-to-server token | Stable · Provider documented | T1 | Provider documentation | Documented | github-token | ghp_/gho_/ghu_/ghs_/ghr_ prefixes and underscore separator; 36-character body is tool-corroborated (checksum in the last six characters per github.blog/2021-04-05) |
| github | Classic personal access token | Stable · Provider documented | T1 | Provider documentation | Documented | github-token | ghp_/gho_/ghu_/ghs_/ghr_ prefixes and underscore separator; 36-character body is tool-corroborated (checksum in the last six characters per github.blog/2021-04-05) |
| github | OAuth access token | Stable · Provider documented | T1 | Provider documentation | Documented | github-token | ghp_/gho_/ghu_/ghs_/ghr_ prefixes and underscore separator; 36-character body is tool-corroborated (checksum in the last six characters per github.blog/2021-04-05) |
| github | OAuth refresh token | Stable · Provider documented | T1 | Provider documentation | Documented | github-token | ghp_/gho_/ghu_/ghs_/ghr_ prefixes and underscore separator; 36-character body is tool-corroborated (checksum in the last six characters per github.blog/2021-04-05) |
| gitlab | Legacy personal access token | Stable · Provider documented | T1 | Provider documentation | Documented | gitlab-token | glpat- prefix; 20-character legacy body is tool-corroborated, routable tokens are not covered |
| gitlab | Runner authentication token | Stable · Empirically qualified | T2 | Independent tool/community corroboration | Empirical | gitlab-runner-authentication-token | supported contexts: bare value (glrt- plus a 20-character friendly-token body or the routable grammar), double-quoted assignment value, value on the line after an unrelated comment line (CRLF); uncertainty: GitLab documents only the glrt- prefix; the 20-character friendly-token body and the routable <base64url>.<version>.<length><CRC32> grammar come from GitLab's own server code (gitlab.com, provider code) and gitleaks. glrtr- (registration-created), instance-prefixed and unversioned routable forms are unverified and outside the claim. No provider-issued token has been captured through #205 intake. |
| google | Generic API key | Stable · Provider documented | T1 | Provider documentation | Documented | google-api-key | the provider page identifies the exposed key_string as an API key and shows an AIza-prefixed example; the 35-character suffix and alphabet remain gitleaks-corroborated |
| grafana | Cloud access policy token | Stable · Provider documented | T1 | Provider documentation | Documented | grafana-cloud-access-policy-token | Grafana states that Cloud access policy tokens start with glc_; body length and alphabet remain tool-corroborated |
| grafana | Service account token | Stable · Provider documented | T1 | Provider documentation | Documented | grafana-service-account-token | Grafana states that service account tokens carry a glsa prefix; the delimiter, segment lengths, alphabet and checksum placement remain tool-corroborated |
| groq | API key | Stable · Empirically qualified | T2 | Independent tool/community corroboration | Empirical | groq-api-key | supported contexts: bare value (the gsk_ prefix plus the exact 52-character body is the only marker), double-quoted assignment value, value on the line after an unrelated comment line (CRLF); uncertainty: Groq pages show the gsk_ prefix only in placeholders and state no length or alphabet; the 52-character alphanumeric body rests on tool rules and a four-sample maintainer observation (#218), not on a provider grammar. No provider-issued key has been captured through #205 intake. |
| hashicorp-terraform | Organization token | Stable · Provider documented | T1 | Provider documentation | Documented | terraform-cloud-token | three independent HCP Terraform API-reference pages (user-tokens, organization-tokens, team-tokens) each publish a full example token; every one of the three is exactly 14 bytes before the literal .atlasv1. marker and exactly 67 bytes after it, and the mirrored Terraform Enterprise organization-tokens page publishes the identical shape and example, confirming Cloud and Enterprise are one grammar rather than two. Neither page documents a checksum or any marker beyond the literal .atlasv1. tag |
| hashicorp-terraform | Team token | Stable · Provider documented | T1 | Provider documentation | Documented | terraform-cloud-token | three independent HCP Terraform API-reference pages (user-tokens, organization-tokens, team-tokens) each publish a full example token; every one of the three is exactly 14 bytes before the literal .atlasv1. marker and exactly 67 bytes after it, and the mirrored Terraform Enterprise organization-tokens page publishes the identical shape and example, confirming Cloud and Enterprise are one grammar rather than two. Neither page documents a checksum or any marker beyond the literal .atlasv1. tag |
| hashicorp-terraform | User token | Stable · Provider documented | T1 | Provider documentation | Documented | terraform-cloud-token | three independent HCP Terraform API-reference pages (user-tokens, organization-tokens, team-tokens) each publish a full example token; every one of the three is exactly 14 bytes before the literal .atlasv1. marker and exactly 67 bytes after it, and the mirrored Terraform Enterprise organization-tokens page publishes the identical shape and example, confirming Cloud and Enterprise are one grammar rather than two. Neither page documents a checksum or any marker beyond the literal .atlasv1. tag |
| hashicorp-vault | Batch token | Stable · Provider documented | T1 | Provider documentation | Documented | vault-token | prefixes and minimum length; the provider states the structure is opaque, so the 90–120-character rule the pinned tools use is corroboration, not contract |
| hashicorp-vault | Recovery token | Stable · Provider documented | T1 | Provider documentation | Documented | vault-token | prefixes and minimum length; the provider states the structure is opaque, so the 90–120-character rule the pinned tools use is corroboration, not contract |
| hashicorp-vault | Service token | Stable · Provider documented | T1 | Provider documentation | Documented | vault-token | prefixes and minimum length; the provider states the structure is opaque, so the 90–120-character rule the pinned tools use is corroboration, not contract |
| heroku | Legacy API key (bare UUID) | Stable · Empirically qualified | T2 | Independent tool/community corroboration | Empirical | heroku-api-key-legacy | supported contexts: Authorization: Bearer <uuid> on a line naming api.heroku.com, HEROKU_API_KEY=<uuid> in env, shell export, compose/YAML env, Terraform provider "heroku" { api_key = "<uuid>" } on one line, git remote https://:<uuid>@git.heroku.com/<app>.git, same line as a case-insensitive heroku keyword, single-line .netrc: machine api.heroku.com login <email> password <uuid>; uncertainty: The bare lowercase 8-4-4-4-12 UUID shape rests on one provider changelog example plus peer rules; Heroku states no grammar in prose, whether it ever issued uppercase hex is unknown, and no legacy token can be newly issued, so issuance cannot be re-observed. A UUID carries no marker of its own and is structurally identical to Heroku app/release/request ids and to current UUID-shaped secrets (OAuth client secret, refresh token), so the value is only claimed beside a heroku keyword. |
| heroku | OAuth access token (HRKU-prefixed) | Stable · Provider documented | T1 | Provider documentation | Documented | heroku-api-key | the page states "Heroku OAuth access tokens are 65 characters long and prefixed with HRKU-" and shows a 65-character worked example beginning HRKU-AA (not reproduced here, since secret scanners classify it as a live token); its own response examples, changelog-items/2842 and changelog-items/3175 ("increasing from 41 characters to 65 characters") document the earlier 41-character HRKU-<uuid> generation. The literal AA after the dash is example- and tool-observed and the [A-Za-z0-9_-] body alphabet is tool-corroborated; neither is stated in prose |
| huggingface | API token | Stable · Provider documented | T1 | Provider documentation | Documented | huggingface-token | Hugging Face's SDK reference on huggingface.co types the user access token as hf_${string}, establishing the hf_ prefix; the provider's Hub OpenAPI spec (/.well-known/openapi.json, POST /api/credentials/revoke) gives a raw access-token example of hf_ followed by 34 placeholder characters, which establishes the 34-character body length on example strength only. The [A-Za-z0-9] body alphabet is not provider-stated and remains a support-policy union of the tool alphabets (gitleaks letters only, TruffleHog alphanumeric); the api_org_ variant is not covered by this provider source |
| langfuse | Secret API key | Stable · Empirically qualified | T2 | Independent tool/community corroboration | Empirical | langfuse-secret-key | supported contexts: bare value (sk-lf- plus a lowercase UUIDv4), double-quoted assignment value, value on the line after an unrelated comment line (CRLF); uncertainty: Langfuse documents the sk-lf-/pk-lf- prefixes, roles and contexts; the lowercase UUIDv4 body is provider code (its key generator, unchanged 2023-06 to 2026-09), not documentation. Self-hosted operator-defined values and a possible sk-lf-gw- gateway shape are outside the claim. No provider-issued key has been captured through #205 intake. |
| langsmith | API key (personal access token or service key) | Stable · Empirically qualified | T2 | Independent tool/community corroboration | Empirical | langsmith-api-key | supported contexts: bare value (the lsv2_pt_/lsv2_sk_ prefix plus the 32_10 hex layout), double-quoted assignment value, value on the line after an unrelated comment line (CRLF); uncertainty: LangSmith documents the two key roles (personal access token, service key), their contexts and one-time display, but no prefix, length or alphabet; docs render only masked lsv2_pt_/lsv2_sk_ forms. The lsv2_(pt\|sk)_ + 32 hex + _ + 10 hex layout is tool-corroborated, and the provider SDK redactor accepts a wider first segment; widths and the lowercase alphabet stay provisional. No provider-issued key has been captured through #205 intake. |
| linear | Personal API key | Stable · Provider documented | T1 | Provider documentation | Documented | linear-token | the provider documents lin_api_ for API keys and lin_oauth_ for OAuth access tokens; the 40-character body and alphabet remain tool-corroborated |
| mailchimp | Marketing API key | Stable · Empirically qualified | T2 | Independent tool/community corroboration | Empirical | mailchimp-api-key | supported contexts: same-line mailchimp keyword assignment (e.g. MAILCHIMP_API_KEY=<value>), bare, double-quoted, or after a CRLF comment line, suffix -us + one or two digits (no other data-center literal is claimed); uncertainty: Mailchimp states the shape only by examples: its fundamentals page shows a 31-hex-character body with -us6, and its own WordPress plugin shows 32 hex with -us19, matching every tool and 111 of 115 public-code candidates (redact-secret#699); the contract follows the 32-byte example. The fundamentals page says the data center is always appended as key-dc. Hex case and the data-center alphabet (only us<N> is ever shown) are not stated by the provider. The only staff statement (2009) advises against regex validation because keys may change. Non-us data-center literals and g-z body letters are outside the claim since 2026-09-24 (docs/decisions/2026-09-24-stop-asserting-provider-undecided-format-properties.md): no scored fixture asserts them either way, so the product's handling of them is unmeasured. |
| mailgun | Private API key and HTTP webhook signing key | Stable · Empirically qualified | T2 | Independent tool/community corroboration | Empirical | mailgun-api-key | supported contexts: private API key and HTTP webhook signing key roles, both key- + 32, same-line mailgun keyword assignment (MAILGUN_API_KEY= / MAILGUN_WEBHOOK_SIGNING_KEY=), bare, double-quoted, or after a CRLF comment line; uncertainty: No Mailgun page states any key grammar; key- + 32 is shown only as a placeholder for the HTTP webhook signing key, and 2018-2025 sources say the current private API key is a prefix-less 32-8-8 hex triplet, making key- possibly the older shape (redact-secret#701). Mailgun's own PHP SDK test uses a key- signing key with g-z letters, so the body is [a-z0-9], not hex-only; case sensitivity is unsettled, Mailgun documents pubkey- as a front-end public key (its Python SDK still scrubs it from logs as a conservative choice), and no issued key has been measured. Case is outside the claim since 2026-09-24 (docs/decisions/2026-09-24-stop-asserting-provider-undecided-format-properties.md): no scored fixture asserts an uppercase body either way, so the product's handling of it is unmeasured. |
| microsoft-entra | Application client secret | Stable · Provider documented | T1 | Provider documentation | Documented | microsoft-entra-client-secret | Microsoft Graph PowerShell's Add-MgApplicationPassword reference (learn.microsoft.com) prints two example SecretText values, each 3 characters (equal to the printed 3-character Hint), the digit 8, the literal Q~, and 34 characters, 40 in total. The Q~ marker at offset 4 and the 8-variant's 40-character width come from these examples, not from a stated grammar. The 7Q~ 37-character previous format, the digit-to-length coupling and the body alphabet are corroborated only by Microsoft's security-utilities code (SEC101/156) and third-party tools |
| neon | API key | Stable · Empirically qualified | T2 | Independent tool/community corroboration | Empirical | neon-api-key | supported contexts: bare value (napi_ plus at least 64 alphanumerics), double-quoted assignment value, value on the line after an unrelated comment line (CRLF); uncertainty: Neon documents the napi_ prefix (changelog 2025-01-31) and calls a key a randomly generated 64-bit token, which fits no string length. The 64-character alphanumeric body floor comes from betterleaks (exactly 64) and mask-go (64 as a floor). Legacy unprefixed keys stay valid and are outside the claim. No provider-issued key has been captured through #205 intake. |
| netlify | Personal access token | Stable · Provider documented | T1 | Provider documentation | Documented | netlify-token | Netlify's staff-authored announcement states "All Netlify authentication tokens will start with a nf prefix followed by a single identifying character" — nfp for personal access tokens (nfc/nfo/nfu/nfb for CLI, OAuth, app and build tokens) — and that token storage capacity must grow "to 40 characters"; the "_" delimiter and the [A-Za-z0-9_] body alphabet are tool-corroborated, not stated on the page |
| new-relic | License key | Stable · Provider documented | T1 | Provider documentation | Documented | new-relic-license-key | New Relic's own documentation (docs.newrelic.com, IBM MQ host-integration page) states the ingest license key as 40 characters with the literal suffix NRAL, establishing the suffix and total length; its eBPF install pages show elided examples ending FFFFNRAL. The body alphabet, the FFFF segment and the EU eu01xx prefix are not provider-stated: they rest on New Relic-authored code (newrelic-cli IsValidLicenseKeyFormat, docs-website check-for-keys.sh) and trufflehog 3.97.4 |
| new-relic | User API key | Stable · Provider documented | T1 | Provider documentation | Documented | new-relic-user-api-key | New Relic says most user keys begin with NRAK-; body length and alphabet remain tool-corroborated |
| notion | Legacy integration token | Stable · Provider documented | T1 | Provider documentation | Documented | notion-token | the provider documents secret_ as the still-working legacy Public API token prefix and ntn_ as the prefix for newly generated tokens; the 43-character body and alphabet remain tool-corroborated |
| npm | Granular access token | Stable · Provider documented | T1 | Provider documentation | Documented | npm-token | npm_ prefix, underscore delimiter and six-character Base62 CRC32 checksum; 36-character body is tool-corroborated |
| openai | Secret API key | Stable · Empirically qualified | T2 | Independent tool/community corroboration | Empirical | openai-token | supported contexts: bare sk-... key anywhere on a line, line with non-ASCII text and CRLF endings, quoted string literal; uncertainty: OpenAI documents no key grammar. Its own code (openai/codex) fixes only the prefixes, the T3BlbkFJ watermark and a 51-character minimum, and one staff forum post confirms sk-proj-. The 74/74 segment widths rest on scanner rules and revoked samples that gitleaks adopted. Earlier project-key generations (20/20, 58/58), sk-None- user keys and sk-service- keys all appear in public code, and the service-account length dispute is still open. No freshly issued key has been measured. Service-account widths other than 74/74 are outside the claim since 2026-09-24 (docs/decisions/2026-09-24-stop-asserting-provider-undecided-format-properties.md): no scored fixture asserts them either way, so the product's handling of them is unmeasured. |
| openrouter | Inference API key | Stable · Provider documented | T1 | Provider documentation | Documented | openrouter-api-key | OpenRouter's own secret-formats guardrail describes its API key as sk-or-v1- followed by 64 lowercase hexadecimal characters. |
| perplexity | API key | Stable · Empirically qualified | T2 | Independent tool/community corroboration | Empirical | perplexity-api-key | supported contexts: bare value (the pplx- prefix plus the exact 48-character body), double-quoted assignment value, value on the line after an unrelated comment line (CRLF); uncertainty: Perplexity documents the pplx- prefix; the exact 48-character alphanumeric body is tool-corroborated (gitleaks, osv-scalibr), and flare-redact admits 40-60. Analytics-key and MCP OAuth token shapes are undocumented and outside the claim. No provider-issued key has been captured through #205 intake. |
| pinecone | API key (pcsk_) | Stable · Empirically qualified | T2 | Independent tool/community corroboration | Empirical | pinecone-api-key | supported contexts: bare value (pcsk_ plus a 5-6 character label and a 63-character secret), double-quoted assignment value, value on the line after an unrelated comment line (CRLF); uncertainty: The pcsk_<label>_<secret> layout is provider code (Pinecone CLI and SDK tests); the 5-6 character label and exact 63-character secret are tool-corroborated. Pinecone's Admin API docs describe an unobserved pckey_ prefix that contradicts every pcsk_ observation, and the legacy bare-UUID key is a separate context-gated family. No provider-issued key has been captured through #205 intake. |
| postman | API key | Stable · Empirically qualified | T2 | Independent tool/community corroboration | Empirical | postman-api-key | supported contexts: bare value (the PMAK- prefix is the only marker; no keyword required), double-quoted assignment value, lowercase-hex segments only (uppercase hex is outside the benchmark claim), value on the line after an unrelated comment line (CRLF); uncertainty: Postman's docs state no PMAK- grammar (only the X-API-Key header and a postman-api-key variable). Its own Insights Agent redaction config fixes PMAK- + 24 lowercase hex + '-' + 34 lowercase hex, matching gitleaks and Nosey Parker, while trufflehog corroborates only the prefix and 59-byte width. The same Postman config also redacts a bare PMAK- + 24 hex value (L32). That pattern matches the id part of every full key, so the family reads it as a prefix-level scrub and keeps the 24-hex fragment as a non-credential near miss (settled by L72, a judgement). The benchmark claims only lowercase-hex keys. No issued key has been measured, and a second Postman credential (PMAT-) exists outside the contract (redact-secret#700). |
| postman | Collection access key | Stable · Empirically qualified | T2 | Independent tool/community corroboration | Empirical | postman-collection-access-key | supported contexts: access_key query parameter of a collection JSON URL, bare value (PMAT- plus 26 uppercase alphanumerics), double-quoted assignment value; uncertainty: Postman documents the collection access key as a read-only credential and renders it masked as PMAT- plus 26 characters; it states no alphabet. The uppercase alphanumeric body comes from GitLab's secret-detection rule alone, and the product also accepts lowercase. No provider-issued key has been captured through #205 intake. |
| pulumi | Organization access token | Stable · Provider documented | T1 | Provider documentation | Documented | pulumi-access-token | Pulumi's own Cloud REST API reference states, of the token-creation response: "The response includes the token ID and the tokenValue (prefixed with 'pul-')." Its personal-access-tokens sibling page carries the identical sentence. Neither page states a length or alphabet for the value that follows the prefix |
| pulumi | Personal access token | Stable · Provider documented | T1 | Provider documentation | Documented | pulumi-access-token | Pulumi's own Cloud REST API reference states, of the token-creation response: "The response includes the token ID and the tokenValue (prefixed with 'pul-')." Its personal-access-tokens sibling page carries the identical sentence. Neither page states a length or alphabet for the value that follows the prefix |
| pulumi | Team access token | Stable · Provider documented | T1 | Provider documentation | Documented | pulumi-access-token | Pulumi's own Cloud REST API reference states, of the token-creation response: "The response includes the token ID and the tokenValue (prefixed with 'pul-')." Its personal-access-tokens sibling page carries the identical sentence. Neither page states a length or alphabet for the value that follows the prefix |
| pypi | API token | Stable · Provider documented | T1 | Provider documentation | Documented | pypi-token | docs.pypi.org's own detection-format page publishes this contract's exact regex, pypi-[A-Za-z0-9-_]{85,}: the pypi- prefix, a "-" separator, a base64 PyMacaroon serialization, an 85-character floor and no ceiling |
| replicate | API token | Stable · Provider documented | T1 | Provider documentation | Documented | replicate-api-token | Replicate states that API tokens are 40-character strings that always start with r8_, fixing the prefix and a 37-character body. The page states no body alphabet. |
| sendgrid | API key | Stable · Provider documented | T1 | Provider documentation | Documented | sendgrid-token | the total key is documented as always 69 characters and never shorter; the SG. prefix, the dot-separated id/secret segments, the 22/43 character split between them and the alphabet are tool-corroborated, not this citation |
| sentry | Organization auth token | Stable · Empirically qualified | T2 | Independent tool/community corroboration | Empirical | sentry-org-auth-token | supported contexts: CLI flag, .sentryclirc/properties, YAML, Dockerfile ENV, JS config string, assignment/env value (SENTRY_AUTH_TOKEN=...), bare sntrys_eyJ...payload..._<43-char secret> token anywhere on a line; uncertainty: The format is stated in Sentry's merged RFC 0091 (its header still reads 'draft') and in Sentry server/CLI code, not on docs.sentry.io. Payload key order and a null or empty url on self-hosted installs are only provisional. The contract's eyJ anchor and {26,} payload floor are looser than every observed token. It has not been checked against a freshly issued token. |
| sentry | User auth token | Stable · Empirically qualified | T2 | Independent tool/community corroboration | Empirical | sentry-user-auth-token | supported contexts: CLI flag, Bearer header, YAML/INI/compose value, assignment/env value (SENTRY_AUTH_TOKEN=...), bare sntryu_ + 64 lowercase hex token anywhere on a line; uncertainty: No page on Sentry's own domains states the format. The shape rests on Sentry server code (secrets.token_hex(32) after sntryu_), sentry-cli and several scanner rules. It has not been checked against a freshly issued token (0 maintainer observations). Unprefixed pre-2024 user tokens (bare 64 hex) are still valid and fall outside this contract. |
| shopify | Custom app access token | Stable · Provider documented | T1 | Provider documentation | Documented | shopify-token | shpat_ and shppa_ prefixes; 32-hex body is tool-corroborated |
| shopify | Public app access token | Stable · Provider documented | T1 | Provider documentation | Documented | shopify-token | shpat_ and shppa_ prefixes; 32-hex body is tool-corroborated |
| slack | Bot token | Stable · Provider documented | T1 | Provider documentation | Documented | slack-token | prefixes and section structure with the secret last; numeric section widths and 24-character secret are tool-corroborated |
| slack | User token | Stable · Provider documented | T1 | Provider documentation | Documented | slack-user-token | the provider states user tokens begin with xoxp-, that tokens are dash-separated sections with the secret last, and shows a three-numeric-section example; it states no section widths or secret alphabet, says pre-August-2016 user tokens may carry 6- or 10-character secrets, and the 2016 token-lengthening changelog warns against relying on perceived token semantics |
| stripe | Live restricted key | Stable · Provider documented | T1 | Provider documentation | Documented | stripe-token | sk_live_/sk_test_/rk_live_/rk_test_ secret and restricted keys, pk_ publishable keys documented as safe to expose, sk_org_ organization keys; 32-character body is tool-corroborated |
| stripe | Test restricted key | Stable · Provider documented | T1 | Provider documentation | Documented | stripe-token | sk_live_/sk_test_/rk_live_/rk_test_ secret and restricted keys, pk_ publishable keys documented as safe to expose, sk_org_ organization keys; 32-character body is tool-corroborated |
| stripe | Live secret key | Stable · Provider documented | T1 | Provider documentation | Documented | stripe-token | sk_live_/sk_test_/rk_live_/rk_test_ secret and restricted keys, pk_ publishable keys documented as safe to expose, sk_org_ organization keys; 32-character body is tool-corroborated |
| stripe | Test secret key | Stable · Provider documented | T1 | Provider documentation | Documented | stripe-token | sk_live_/sk_test_/rk_live_/rk_test_ secret and restricted keys, pk_ publishable keys documented as safe to expose, sk_org_ organization keys; 32-character body is tool-corroborated |
| stripe | Webhook signing secret | Stable · Provider documented | T1 | Provider documentation | Documented | stripe-webhook-signing-secret | the whsec_ prefix and the per-endpoint configuration context; no body length or alphabet |
| supabase | Personal access token | Stable · Provider documented | T1 | Provider documentation | Documented | supabase-management-token | documents the sbp_ prefix by example (sbp_fc...) and the classic-vs-scoped distinction; states no exact body grammar |
| supabase | Project secret key | Stable · Provider documented | T1 | Provider documentation | Documented | supabase-token | the self-hosting page states that opaque keys are sb_secret_<22-char-random>_<8-char-checksum> and "use the same format as the platform", and supabase.com/docs/guides/api/api-keys documents the sb_secret_ prefix and sb_publishable_ as "safe to expose online"; the base64url body alphabet and the sha256 checksum construction are provider code (supabase/supabase docker/utils/add-new-auth-keys.sh), not documentation, and the hosted checksum input is unconfirmed |
| telegram | Bot token | Stable · Empirically qualified | T2 | Independent tool/community corroboration | Empirical | telegram-bot-token | supported contexts: SDK builder string, YAML/compose value, BotFather reply text, api.telegram.org/bot<token>/ URL path, assignment/env value (TELEGRAM_BOT_TOKEN=...), bare <digits>:<secret> token anywhere on a line; uncertainty: Telegram documents only example tokens, with no grammar. The delimiter and the no-'/' rule come from Telegram's Bot API server code on github.com. The secret body length is disputed: the doc examples show 34 characters, while most tools and community reports say 35. The leading AA that most scanners key on is unexplained by any source. It has not been checked against a freshly issued token. |
| travis-ci | API token | Stable · Empirically qualified | T2 | Independent tool/community corroboration | Empirical | travisci-api-token | supported contexts: Authorization: token <token> on a line naming the Travis API host, TRAVIS_TOKEN / TRAVIS_API_TOKEN assignment in env, shell export, CI env, JSON, same line as a case-insensitive travis keyword, travis CLI --token flag; uncertainty: Travis CI states no token length or alphabet; its documentation shows only a masked xxxxxxxxxxxx token in an Authorization: token header. The 22-character alphanumeric shape and the travis keyword gate come from two peer rules. The letter-and-digit requirement is a precision guard (about 2% of random tokens have no digit). No provider-issued token has been captured through #205 intake. |
| twilio | API key secret | Stable · Empirically qualified | T2 | Independent tool/community corroboration | Empirical | twilio-api-key-secret | supported contexts: same line as an API Key SID (SK + 32 hex), e.g. Client(SK..., secret), twilio profiles:create --api-key SK... --api-secret, Basic-auth -u SK...:secret, same line as the word twilio (e.g. TWILIO_API_SECRET=, twilio_api_secret:, log/prose mentioning Twilio); uncertainty: No Twilio source states the secret's length or alphabet. The 32-character alphanumeric shape rests on TruffleHog's twilioapikey detector plus 32-character placeholder masks in Twilio's own sample repos. Only the paired SK SID has a provider-documented grammar. The value has no marker, so it is claimed only beside same-line Twilio context. It has not been checked against an issued key. |
| twilio | Account auth token | Stable · Empirically qualified | T2 | Independent tool/community corroboration | Empirical | twilio-auth-token | supported contexts: same line as an Account SID (AC + 32 hex), e.g. Client(AC..., token) or Basic-auth -u AC...:token, same line as the word twilio (e.g. TWILIO_AUTH_TOKEN=, twilio_auth_token:, log/prose mentioning Twilio); uncertainty: Twilio documents no alphabet. Its own code checks only the length (32), and one Twilio sample accepts [a-z0-9], so lowercase hex rests on scanner rules. The value has no marker, so it is claimed only beside same-line Twilio context. Secondary, regional and test tokens have no stated shape. It has not been checked against an issued token. |
| xai | API key | Stable · Empirically qualified | T2 | Independent tool/community corroboration | Empirical | xai-api-key | supported contexts: bare value (the xai- prefix plus the exact 80-character body), double-quoted assignment value, value on the line after an unrelated comment line (CRLF); uncertainty: xAI documents the xai- prefix but states no length or alphabet in prose; the 80-character body rests on the provider's one example value in its API-key response schema, a third-party masked key and tool rules. The body alphabet is provisional (TruffleHog admits _, betterleaks _ and -), so positives use alphanumerics only and no fixture asserts silence on _ or -. No provider-issued key has been captured through #205 intake. |

### Provisional

| Provider | Family | Support | Evidence tier | Evidence basis | Qualification profile | Detector(s) | Reason |
| --- | --- | --- | --- | --- | --- | --- | --- |
| (format) | Bearer credential | Provisional · Project policy | T3 | Project policy | — | bearer-token | Detected under project policy rather than a provider format, so it is not eligible for stable qualification. |
| (format) | Connection-string password | Provisional · Project policy | T3 | Project policy | — | connection-string | Detected under project policy rather than a provider format, so it is not eligible for stable qualification. |
| (format) | OTP seed | Provisional · Project policy | T3 | Project policy | — | otpauth-uri | Detected under project policy rather than a provider format, so it is not eligible for stable qualification. |
| (format) | Unclassified assignment literal | Provisional · Project policy | T3 | Project policy | — | generic-token | Detected under project policy rather than a provider format, so it is not eligible for stable qualification. |
| github | Fine-grained personal access token | Provisional · Tool corroborated | T2 | Independent tool/community corroboration | — | github-fine-grained-pat | Not yet stable: needs independent corroboration of its format (several sources, or provider-issued keys), more benign controls, more near-miss twin pairs and a defined supported-context boundary with its uncertainty stated. |
| okta | Management API token (SSWS) | Provisional · Tool corroborated | T2 | Independent tool/community corroboration | — | okta-api-token | Not yet stable: needs its conflicting format evidence settled. |
| slack | App-level token | Provisional · Tool corroborated | T2 | Independent tool/community corroboration | — | slack-app-level-token | Not yet stable: needs independent corroboration of its format (several sources, or provider-issued keys), more benign controls, more near-miss twin pairs and a defined supported-context boundary with its uncertainty stated. |

### Pending

| Provider | Family | Support | Evidence tier | Evidence basis | Qualification profile | Detector(s) | Reason |
| --- | --- | --- | --- | --- | --- | --- | --- |
| vercel | Access token | Pending | T0 | None | — | vercel-token | No reviewed detection contract is available yet. |

### Unsupported

| Provider | Family | Support | Evidence tier | Evidence basis | Qualification profile | Detector(s) | Reason |
| --- | --- | --- | --- | --- | --- | --- | --- |
| atlassian | Access token | Unsupported | — | None | — | — | atlassian-api-token's contract review states the pinned TruffleHog detector targets the distinct ATCT access-token family, not the ATAT-prefixed API-token shape. |
| aws | Context-specific credential | Unsupported | — | None | — | — | Documented in the same IAM unique-identifier prefix table as AKIA/ASIA; not matched by the AKIA-only pattern. Provider source: https://docs.aws.amazon.com/IAM/latest/UserGuide/reference_identifiers.html#identifiers-prefixes. |
| aws | IAM user secret access key | Unsupported | — | None | — | — | The aws-access-key contract's companion field states a separate 40-character secret access key is required alongside the access key ID; this project has no dedicated detector for the secret half, which carries no stable prefix to anchor on. |
| aws | STS service bearer token | Unsupported | — | None | — | — | Documented in the same IAM unique-identifier prefix table as AKIA/ASIA; not matched by the AKIA-only pattern. Provider source: https://docs.aws.amazon.com/IAM/latest/UserGuide/reference_identifiers.html#identifiers-prefixes. |
| aws | STS temporary access key | Unsupported | — | None | — | — | aws-access-key's pattern is anchored to AKIA only (benchmarks/lib/assessment.ts contract), so ASIA values are not matched. classifyFixture already special-cases a bare AWS positive as policy, not must-redact, because "some legacy ASIA values also use digits outside the base32 alphabet". Provider source: https://docs.aws.amazon.com/IAM/latest/UserGuide/reference_identifiers.html#identifiers-prefixes. |
| gitlab | Routable personal access token | Unsupported | — | None | — | — | gitlab-token's contract review states explicitly that routable tokens are not covered by the 20-character-body pattern. Provider source: https://docs.gitlab.com/security/tokens/. |
| google | OAuth2 credential | Unsupported | — | None | — | — | google-api-key's contract review notes the pinned TruffleHog detectors separately cover Gemini and OAuth2 credentials, not this generic AIza API-key shape. |
| linear | OAuth access token | Unsupported | — | None | — | — | classifyFixture explicitly routes lin_oauth_ values to the "needs a separate format contract; related detector support is not evidence of parity" guard. Provider source: benchmarks/lib/assessment.ts (classifyFixture variant guard: "Variant support must not be inferred from a related family name."). |
| mailgun | Prefix-less 32-8-8 hex key triplet | Unsupported | — | None | — | — | Beta.8 context-gated arrival family mailgun-api-key-triplet (#259). Since redact-secret#701 (#773, registry pinned at 3144bb3) the product mailgun-api-key detector also reports this shape under mailgun_api_key, inside the shared detector. It is no longer treated as a superseded legacy signing key: no issued key has been observed and the role is recorded uncertainty. Provider source: https://github.com/gitleaks/gitleaks/blob/v8.30.1/config/gitleaks.toml, https://github.com/trufflesecurity/trufflehog/issues/3870. |
| mailgun | Public validation key (pubkey-) | Unsupported | — | None | — | — | Documented public per redact-secret#314's scope; gitleaks 8.30.1's mailgun-pub-key rule still flags it, which the mailgun-api-key public-id control records. Provider source: https://github.com/gitleaks/gitleaks/blob/v8.30.1/config/gitleaks.toml. |
| netlify | CLI, OAuth, app and build tokens (nfc_/nfo_/nfu_/nfb_) | Unsupported | — | None | — | — | Out of redact-secret#311's explicit scope; each is a distinct credential, so none is a control or positive for netlify:personal-access-token. Provider source: https://answers.netlify.com/t/change-to-the-netlify-authentication-token-format/106146. |
| notion | Integration token | Unsupported | — | None | — | — | notion-token's contract review states this current ntn_-prefixed format has no contract here; the pinned gitleaks rule targets it instead of the legacy secret_ shape. Measured as the Beta.8 arrival family notion-integration-token (#211, research #225), separate from notion:legacy-integration-token: the ntn_ prefix is provider-documented, the 11-digit + 35-character body is tool-only and provisional, and PAT/OAuth/org-bot roles that may share the prefix are not merged into it. Provider source: https://developers.notion.com/page/changelog. |
| npm | Legacy token | Unsupported | — | None | — | — | npm-token's own provider source is the changelog announcing the npm_ prefix scheme as a replacement, implying the prior UUID-format token predates it; that legacy shape is not matched by the npm_-prefixed pattern. Provider source: https://github.blog/changelog/2021-09-23-npm-has-a-new-access-token-format/. |
| openrouter | Management API key | Unsupported | — | None | — | — | Unsupported and unclaimed (#208, research #220): a real secret, but no body grammar is published and no product detector, contract or fixture targets it. It is never used as a benign control of openrouter:api-key. Provider source: https://openrouter.ai/docs/guides/overview/auth/management-api-keys, https://github.com/OpenRouterTeam/terraform-provider-openrouter. |
| pinecone | Legacy API key (bare UUID) | Unsupported | — | None | — | — | Beta.8 context-gated arrival family pinecone-api-key-legacy (#212, research #228); the UUID shape is corroborated by unpinned tools only. Owned by the product pinecone-api-key detector since product main 2420e80 (redact-secret#766 and its ADR 2026-09-24-claim-a-legacy-pinecone-uuid-key-only-under-its-api-key-name): the UUID is redacted at high confidence under a Pinecone API-key name (PINECONE_API_KEY, pinecone_api_key, PINECONE_KEY, or api_key/apiKey/Api-Key on a line naming pinecone), and a bare UUID stays unclaimed. The detector reports it with the same pinecone_api_key finding type as pcsk_, so detectors stays empty: mapping the family to pinecone-api-key would merge it into that contract (#253). Provider source: https://docs.pinecone.io/reference/api/authentication. |
| slack | Workflow webhook token | Unsupported | — | None | — | — | classifyFixture explicitly routes xwfp- values to the "needs a separate format contract" guard. Provider source: benchmarks/lib/assessment.ts (classifyFixture variant guard: "Variant support must not be inferred from a related family name."). |
| stripe | Organization API key | Unsupported | — | None | — | — | classifyFixture explicitly routes sk_org_ values to "this variant needs a separate format contract; related detector support is not evidence of parity", so it is not claimed by stripe-token's live/test secret-key pattern. Provider source: benchmarks/lib/assessment.ts (classifyFixture variant guard: "Variant support must not be inferred from a related family name."). |
